# 简历项目概述：领域知识增强的有损投机解码（Domain-Steered Decoding）

## 项目一句话
在不微调通用大模型（Target）的前提下，引入领域专家小模型（Draft）与基座小模型（Small-Base）做离线“领域增量”挖掘与蒸馏，在线用轻量 Gate 选择性放行 Draft token，实现**医疗领域准确率提升 + 投机解码加速**的统一。

---

## 背景与目标
- **问题**：通用大模型具备强推理/泛化能力，但在垂直领域（医疗）知识覆盖不足；纯领域模型更“懂行”，但综合能力与推理稳定性不如大模型。
- **目标**：
  - 不对 Target 做微调（零训练成本/零额外线上模型参数）
  - 保持通用能力不显著下降
  - 在医疗领域（MedQA 等）获得可观增益
  - 同时引入 speculative decoding 以提升推理吞吐（TPS）

---

## 系统设定（三模型协同）
- **Target（通用大模型）**：Qwen3-14B
- **Draft（领域专家小模型）**：II-Medical-8B
- **Small-Base（Draft 对应基座）**：Qwen3-8B-Base
- **核心约束**：
  - **线上推理**仅使用 Target + Draft 的隐藏状态（保证低开销）
  - Small-Base 仅用于**离线数据挖掘/特征蒸馏/分析**（不引入线上额外模型开销）

---

## 技术方案（从离线“上帝视角”到线上“零开销”）

### 1) 自动挖掘训练数据：定位“领域知识可修复点”
- 从样本中筛选 **Target 错、Draft 对** 的 case
- 沿生成前缀向后扫描，定位 Target 与 Draft 的**分歧 token**
- 将分歧 token 强行续写给 Target，观察是否能“拉回正确答案”
- 以结果导向形成硬标签：
  - **Label=1**：该 token 具备“修复 Target 错误”的关键作用
  - **Label=0**：放行无益或有害

### 2) 领域增量度量（输出空间对比）：Draft vs Small-Base
- 在同前缀下比较 Draft 与 Small-Base 的 token 概率差：
  - $\Delta \log P = \log P_d(y_d) - \log P_b(y_d)$
- 将 $\Delta \log P$ 映射为**样本权重**：差值越大，越可能是“纯领域增量知识”（专业术语/药名/检验指标等）

### 3) 特征蒸馏（特征空间对比）：Teacher → Student
- **Teacher（离线）**：输入三模型隐藏状态拼接 $[H_t, H_d, H_b]$，拟合硬标签（带样本权重）
- **Student（线上）**：输入双模型隐藏状态 $[H_t, H_d]$，用 KL 散度拟合 Teacher 软标签（再次加权），实现：
  - 线上推理时无需 Small-Base
  - Gate 仍具有“看过三模型对比信息”的决策能力

### 4) 线上推理：有损投机解码 + Gate 放行
- Draft 提案 1-step token
- Target 进行校验：
  - 一致：直接接受（等价无损）
  - 不一致：调用 Gate 输出 $p_{accept}$，若 $p_{accept} \ge \tau$ 则**放行 Draft token**，否则回退用 Target token
- 通过阈值 $\tau$ 扫描得到速度/精度的可控折中曲线

### 5) 直接放行策略（新 idea，快速验证用）
- 当 Target 拒绝 Draft token 时，若同时满足 **Draft top-1 与 Small-Base top-1 不一致**（表征领域知识驱动），则尝试 override 放行
- 支持从激进到保守：V0（硬规则）→ V1（差值阈值）→ V2（差值 + Target 反对度约束）

---

## 我完成的工作（可放简历的“动作”表述）
- 设计并实现**三模型协同**的离线数据挖掘流程：自动定位分歧 token、强行续写验证“修复性”，生成可训练监督信号
- 提出并落地“**输出空间对比加权**（Draft vs Small-Base 概率差）+ **特征空间蒸馏**（Teacher→Student）”的联合方案，将领域增量信息压缩进轻量 Gate
- 搭建最小可跑的**有损投机解码评测脚本**：支持 strict speculative / gate speculative、多阈值 sweep、统一日志落盘
- 设计并补齐关键指标与诊断日志（jsonl）：Acceptance/Override Rate、Override Precision/Harm、速度-精度 Pareto 分析所需字段
- 形成可复现实验矩阵：Baseline（target 原生/strict speculative/现有 gate）对照 + V0/V1/V2 网格参数搜索

---

## 成就与效果（不编造数据，提供可填模板）
> 建议你把跑出来的最好结果填进下列括号。

- 医疗领域（MedQA）：相对 baseline 提升 **（+X.XX%）**，在通用集下降控制在 **（-Y.YY%）** 内
- 推理吞吐（TPS）：相对 target-only **（+A% 或 -A%）**；相对 strict speculative **（+B%）**
- 可控性：通过阈值 $\tau$ 获得清晰的 speed–quality trade-off，并定位 Pareto 最优配置 **（tau=…）**
- 解释性：Override 日志可追踪到 token 级别决策与触发原因，便于错误分析与策略迭代

---

## 技术栈与工程要点
- Python、PyTorch/Transformers（模型加载/hidden states 提取/评测脚本）
- 投机解码（speculative decoding）
- 轻量分类器（Logistic / 1-layer MLP）、特征标准化、KL 蒸馏
- 实验工程：参数 sweep、结构化日志（jsonl）、离线分析脚本

---

## 简历可直接复制的版本（更短）
- 领域知识增强有损投机解码：在不微调通用大模型前提下，引入领域小模型与基座小模型做离线分歧挖掘、概率差加权与三模型特征蒸馏，在线用轻量 Gate 选择性放行 Draft token，实现医疗领域准确率提升并保持推理吞吐可控；构建端到端评测、阈值网格搜索与 token 级日志体系，支持 Override Precision/Harm 等关键诊断指标。
