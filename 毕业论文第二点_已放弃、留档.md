# 毕业论文第二点候选方案：隐空间对比干预与可校准接受决策

**副标题：** 以 Draft−Base 对比信号为纽带，将「表征层最小干预」与「验收层可信决策」作为第一点的延伸贡献

**与第一点关系（写作时建议放在摘要/引言末段）：**  
第一点已论证：在**不训练**前提下，仅通过投机解码验收链路中对 **logit / 概率域** 的局部校准（如 C3、C10 等），即可在领域任务上取得超越 Target 与 Draft **单独**水平的准确率，并维持投机解码的吞吐优势。其局限亦明确：**干预几乎完全发生在最后一层输出分布**，对「领域知识在大模型内部如何被表征与调用」缺乏机制层面的回答，且难以回答「何时该信软引导」这一工程落地问题。

本文档给出第二点的两条**可并行、也可二选一**的技术路线，并统一在一条论文主线下：**用 Draft−Base 的对比信号，把“领域知识”从词表概率层面，向上连接到隐藏态子空间与决策可信度层面。**

---

## 1. 论文主故事（Contribution narrative）

### 1.1 一句话贡献（可放摘要）

在保持**零权重更新**与**自回归手写解码**约束下，我们进一步证明：Draft−Base 对比信号不仅能用于输出层 PoE/补贴式验收（第一点），还可被提升为 **(i)** 在 Target 若干中间层的 **稀疏对比残差注入（Contrastive Residual Steering, CRS）**，以更低分布扭曲代价提升领域正确率；以及 **(ii)** 对软引导后验收概率进行 **事后校准（post-hoc calibration）** 并据此实现 **选择性预测（selective prediction）**，在几乎不牺牲峰值吞吐的前提下，显著降低「错误但高置信接受」的风险。

### 1.2 三个可写进贡献列表的 bullet

1. **机制层：** 提出以 $\Delta h_\ell = h^{\mathrm{draft}}_\ell - h^{\mathrm{base}}_\ell$ 为基的 **多层稀疏干预**，并与输出层 C10 的 $\Delta\text{logit}$ 形成互补：前者作用于表征几何，后者作用于似然分解；两者可解耦消融。  
2. **可信层：** 将投机解码中的 **接受概率** 视为随机决策分数，建立 **可靠性曲线 / ECE** 与 **代价敏感回退策略**（不确定则回退标准 SD 或纯 Target），给出「何时该信领域注入」的可操作判据。  
3. **方法论层：** 以 **反事实对照（counterfactual controls）** 固定混杂因素：随机方向、随机层、错配位置注入、以及「仅 Base / 仅 Draft」单边信号，用于支撑因果式表述而非单纯调参报告。

### 1.3 与答辩口径的对应

- **老师问：**「你们不还是启发式加向量吗？」  
  **答：** 向量不是任意选的，而是由 **同源架构下领域微调与否** 构成的 **对照差分** 诱导；并通过 **稀疏门控**（熵、$\Delta P$、$\Delta\text{logit}$ 等你方已有信号）限制干预频率；最后用 **反事实** 证明增益来自对比结构而非放大噪声。

- **老师问：**「第二点是不是又训练了？」  
  **答：** 全程 **零训练**；若出现极小线性标定（例如温度缩放或 Platt scaling），严格限定在 **验证集上拟合标定参数**，不对语言模型权重做更新，可归类为 **post-hoc calibration**（需在正文界定清楚，避免被误解为 fine-tune）。

---

## 2. 路线 A：Draft−Base 隐空间对比 Steering + 稀疏多层干预 + 反事实对照

### 2.1 核心思想

在某一解码步 $t$、某一层 $\ell$，记 Target 的隐藏状态为 $h^{\mathrm{target}}_{\ell,t}$（实现上可取 **残差流主路径** 上的张量，例如 block 输出或 MLP 后的残差，具体取决于 Qwen2.5 的 hook 点）。Draft 与 Base 在**同一前缀、同一位置**上得到 $h^{\mathrm{draft}}_{\ell,t}$、$h^{\mathrm{base}}_{\ell,t}$。

定义 **对比残差方向**：

$$
\Delta h_{\ell,t} \;=\; h^{\mathrm{draft}}_{\ell,t} - h^{\mathrm{base}}_{\ell,t}
$$

**直觉（论文写法）：** Base 与 Draft 共享预训练先验，差异主要来自 **领域微调所诱导的表示漂移**；因此 $\Delta h$ 更像「把通用表示拉向领域专家流形」的切向量，而不是无结构噪声。

**干预：**

$$
h^{\mathrm{target}}_{\ell,t} \;\leftarrow\; h^{\mathrm{target}}_{\ell,t} \;+\; m_t \cdot \beta_\ell \cdot \mathrm{Proj}\!\left(\Delta h_{\ell,t}\right)
$$

其中：

- $\beta_\ell$：层标量强度（可手工网格，也可在验证集上选一组少量超参）。  
- $m_t \in \{0,1\}$ 或 $[0,1]$：**稀疏门控**（见 2.3）。  
- $\mathrm{Proj}(\cdot)$：**子空间投影**（强烈建议有，否则高维直接加容易破坏尺度）；最简单为对 Target 该层激活范数做归一化匹配，或投影到以若干步上 $\Delta h$ 张成的低维子空间（工程更大）。

> **与 C10 的分工：** C10 在 logit 上等价于对输出似然乘上 $(P_d/P_b)^\alpha$ 的专家乘积；CRS 在更早层改变 **特征几何**，使 LM head 更易读出领域一致结构。论文可强调：**输出层 PoE 未必总能在不改变内部表征的情况下修复所有错误**（尤其多步推理或长程依赖）。

### 2.2 可行性分析（工程与科学）

**工程可行性：中高**

- 你们已具备 **三模型同卡**、**手写 decode**、**StaticCache**；CRS 需要额外能力：  
  - 对 Target / Draft / Base 在**指定层**注册 forward hook，缓存 $\Delta h$；  
  - 保证 **token 对齐** 与 **cache 更新顺序** 一致（投机窗口内尤其要注意：干预应施加在 **Target 验证路径** 上，而不是破坏 Draft 提案的独立性——具体实现需在「仅 Target forward」还是「同步三流」之间做清晰设计）。  
- **显存与算力：** 相比只取最后一层 logits，多层 hook 会增加中间张量保留与同步开销；通过 **稀疏门控**（仅少量步、少量层）可把平均开销压住。

**科学可行性：中**

- 正向结果：**可能**在「Target 已有相关知识但 logits 读出不一致」类题目上有效；对「Target 内部完全缺失知识」的题目，CRS 未必能奇迹式补全，但可作为 **与 logit 补贴正交的增益源**。  
- 主要风险：**分布偏移**（域内涨点、域外掉点）与 **训练-推理干预不匹配**（干预在推理时引入训练未见动力学）。缓解手段：稀疏、投影、以及强反事实对照。

### 2.3 稀疏门控（建议直接复用你们已有哲学）

任选其一或组合（与 C4/C5/C11 叙事一致）：

- **Token 级：** $\mathbb{I}(\Delta P(x)>\tau)$ 或 $\mathbb{I}(\Delta\text{logit}(x)>\tau)$。  
- **步级：** Target 熵 $H_t$ 高时允许 CRS（表示「模型不确定，才请专家改内部几何」）。  
- **预算级：** 每题最多干预 $K$ 步，避免全程污染。

### 2.4 反事实对照（必须写进方法节，否则创新性站不住）

对每个真实有效的 CRS 配置，至少报告以下控制：

| 对照名称 | 干预形式 | 目的 |
|---------|----------|------|
| Random direction | 用同范数高斯随机向量替换 $\Delta h$ | 排除「只要加噪声就会好」 |
| Swapped expert | 用 **Base−Draft** 或错领域 Draft 的 $\Delta h$ | 排除「任意差分都有效」 |
| Wrong position | 将 $\Delta h$ 加在错误层/错位时间步 | 排除实现 bug 带来的伪增益 |
| Scale ablation | $\beta\to 0$ | 与无 CRS 曲线应连续贴合 |

**通过标准：** 真实 $\Delta h$ 显著优于随机方向；且错配信号不提升或提升显著弱于正确信号。

### 2.5 相关工作（写作时可按主题引用检索）

> 说明：下列条目给出**检索关键词与代表方向**，具体 bib 需在你们学校模板下补全 DOI/venue；不要硬编不存在的论文信息。

1. **Inference-time representation editing / activation steering**  
   关键词：`activation steering`, `inference-time intervention`, `linear representation hypothesis`。  
   代表脉络：在隐藏态上加低秩或低维方向以改变行为；你们差异在于方向来自 **结构化对照（Draft−Base）** 而非单句对句子的差分向量。

2. **Contrastive / difference-of-experts 表示**  
   关键词：`expert contrast`, `difference vector`, `product of experts`。  
   你们已在 logit 域使用 $\log P_d-\log P_b$（C10）；CRS 是把同一哲学上推到 $h$ 域。

3. **Mechanistic interpretability / causal scrubbing（概念层）**  
   关键词：`causal mediation`, `activation patching`, `path patching`。  
   反事实对照与「patch 某层表示」在方法论上接近；你们可强调：**对照不是随机神经元，而是跨模型差分诱导的子空间**。

4. **Speculative decoding 原论文与后续改进**  
   关键词：`speculative decoding`, `Medusa`, `EAGLE`。  
   用于定位你们工作的系统层坐标：第二点仍在 **decode-loop** 内，而不是改模型结构。

---

## 3. 路线 B：接受决策的可校准性（Calibration）与选择性预测（Selective Prediction）

### 3.1 核心思想

软引导策略会改变验收分子（如 $P'_\text{accept}$ 或基于 $P_\text{steered}$ 的比值）。把它当作「模型对 **接受该 token 是否合理** 给出的分数」，自然会问：

> 当分数很高时，最终答案是否真的更可能对？

**校准**回答的是：**概率含义是否与实际频率一致**（reliability）。  
**选择性预测**回答的是：**当分数不可信时，是否允许系统拒绝投机、回退到安全策略**。

### 3.2 定义操作对象（避免概念漂移）

对每一步验收，记录：

- 决策分数 $s_t$：例如 $P'_\text{accept}$、或 $\log \frac{P'_\text{target}(x)}{P_\text{draft}(x)}$、或 steered 后的 log-odds。  
- 标签 $y_t$：该步选择是否最终导致 **题目正确**（题级标签可回传到关键步；更干净的是定义 **token 级代理标签**，如是否与 gold 解析路径一致——实现难度较高，建议以题级为主）。  

在验证集上绘制 **可靠性图（reliability diagram）**，并计算 **ECE**（Expected Calibration Error）等。

### 3.3 轻量校准（建议优先，工程量可控）

在 **不更新模型权重** 前提下，对 $s_t$ 做单调变换：

- **温度缩放 / Platt scaling（logistic）**：在验证集上拟合 1–2 个标量参数，使 ECE 下降。  
- **分桶 isotonic regression**：对分段常数校准，表达力强但需防过拟合（验证集要足够）。

> **写作边界：** 明确写「calibration 仅作用于决策分数，不对 LM 参数做梯度更新」，并在伦理/工程上强调这是 **部署期校准**。

### 3.4 选择性预测策略（与吞吐故事兼容）

定义拒绝函数：

$$
\text{Abstain at step } t \quad\text{if}\quad \widehat{\mathrm{Conf}}(s_t) < \kappa
$$

在 abstain 时执行：

- **回退 A：** 标准投机解码（关闭软引导补贴）；或  
- **回退 B：** 纯 Target 解码一步（最保守）；或  
- **回退 C：** 降低 $\gamma$ 或降低干预强度 $\alpha$。

**评价指标不能只报 acc：**

- **Risk–coverage 曲线：** coverage 下降时 risk（错误率）应显著下降；  
- **Tokens/sec vs coverage：** 展示不同 $\kappa$ 下的帕累托；  
- **Selective ECE：** 在「未 abstain」子集上再报校准误差。

### 3.5 可行性分析

**工程可行性：高**  
相比 CRS，路线 B 主要是 **日志、分桶统计、验证集拟合标量、策略回退**，与现有 Python 评测脚本体系高度兼容。

**科学可行性：中高**  
风险在于：题级正确率与逐步接受分数的 **因果链较长**，可能出现「校准改善有限」。缓解：同时报告 **步级代理指标**（例如接受 token 是否与参考答案首 token 一致）与题级指标，并在附录解释代理指标局限。

### 3.6 相关工作方向

1. **Calibration of modern neural networks**（温度缩放等）  
2. **Selective classification / risk-coverage**（Geifman & El-Yaniv 脉络）  
3. **Conformal prediction for LLM**（若你们愿意引入有限样本保证，工程量会上升；可作为扩展 future work）  
4. **Uncertainty in decoding / self-consistency**（与你们门控哲学相关）

---

## 4. 两条路线如何合成一个「第二点」（推荐叙事）

**推荐结构（二选一写作成本）：**

- **合成型（更强）：** CRS 作为 **Representation-side domain fusion**；Calibration/Selective 作为 **Decision-side trustworthiness**。两者共享同一实验平台与同一批日志。  
- **聚焦型（更稳）：** 主文只深挖 **路线 B**（更快出图），CRS 放附录或缩短为「探索性实验」。

**答辩逻辑：**  
第一点证明「能涨点」；第二点回答「**为什么该信**」与「**是否必须动最后一层**」——前者偏可信与部署，后者偏机制与表示。

---

## 5. 实验矩阵（建议直接落表跑）

### 5.1 数据集与主指标（与现有一致）

- **域内主指标：** Surgery 子集 Accuracy（或你们已固化的 n）。  
- **系统指标：** Tokens/sec（真实吞吐）、平均接受率、平均回退率。  
- **域外守护：** 任选 1–2 个轻量通用集或同 benchmark 的非 surgery 子域，报告 **退化量**（Δ）。

### 5.2 路线 A 实验（CRS）

| ID | 设置 | 目的 |
|----|------|------|
| A0 | 无 CRS + 你们最强输出层策略（如 C10/C11） | 强基线 |
| A1 | CRS：单层（扫 $\ell$） | 找敏感层 |
| A2 | CRS：Top-k 层组合（小网格） | 提升鲁棒性 |
| A3 | CRS + 稀疏门控（熵 / $\Delta P$） | 控制副作用 |
| A4 | 反事实：Random / Swapped / Wrong-pos | 因果支撑 |
| A5 | CRS 与 C10 **同时开/互斥开** | 是否互补 |

**预期结果形态（写结论用）：**

- **乐观：** A1/A2 在域内 acc 上相对 A0 有 **小幅但稳定** 提升，且 A4 中真实 $\Delta h$ 明显优于随机；与 C10 联用有 **叠加或至少不互害**。  
- **中性：** 域内提升不明显，但 A4 显示随机方向无效，从而支持「需要结构差分」；此时把贡献写成 **阴性发现 + 严谨对照** 仍可作为硕士论文合格第二点（需提前与导师对齐预期）。  
- **悲观：** 随机方向也涨——说明评测方差或数据泄漏，应回到实现与对照设计排查。

### 5.3 路线 B 实验（Calibration + Selective）

| ID | 设置 | 目的 |
|----|------|------|
| B0 | 未校准分数 + 固定策略 | 基线 |
| B1 | 验证集温度缩放 / Platt | 降 ECE |
| B2 | Risk–coverage：扫 $\kappa$ | 展示可选择可信 |
| B3 | 不同回退 A/B/C | 工程最优折中 |
| B4 | 与 CRS 叠加 | 部署组合策略 |

**预期结果形态：**

- ECE **显著下降**（哪怕 acc 不变），配合 risk–coverage：**coverage 略降，risk 大降**。  
- Tokens/sec：选择性回退会降吞吐；论文应给出 **与风险下降对应的吞吐代价曲线**，避免只报单点。

---

## 6. 预期论文结论（可直接改写进「结论章」的句式）

1. **表征侧：** Draft−Base 诱导的 $\Delta h$ 在稀疏门控下可对 Target 形成 **最小代价的领域几何修正**；反事实对照表明增益主要来源于 **对比结构** 而非简单扰动。  
2. **决策侧：** 软引导后的验收分数经轻量校准后 **更可靠**；选择性预测在可控吞吐损失下 **显著降低高置信错误接受**。  
3. **统一点：** 第二点将第一点的「输出分布融合」扩展为 **(表征, 决策) 双层面** 的免训练领域对齐框架，为投机式专家融合提供可部署的 **可信度与机制** 支撑。

---

## 7. 实现清单（便于拆任务/排期）

1. **日志协议：** 每步记录 $(s_t, \text{intervened?}, \text{layer}, \beta, \alpha, \text{accept}, \text{token id}, \text{qid})$。  
2. **CRS：** Target hook +（可选）Draft/Base 同步抽取；门控与反事实开关。  
3. **Calibration：** 验证集拟合脚本 + 测试集仅前向评估；保存校准参数 yaml。  
4. **Selective：** $\kappa$ sweep 脚本 + risk–coverage 绘图。  
5. **守护评测：** 域外轻量集固定化，避免「挑好的集」争议。

---

## 8. 风险与 Future work（诚实写反而加分）

- CRS 可能带来 **域外伤害**；Future work：自适应 $\beta_\ell(\text{domain detector})$。  
- 校准对 **题级标签** 较粗；Future work：更细粒度监督或 conformal。  
- 计算开销：Future work：蒸馏 $\Delta h$ 到低秩子空间、或仅在 reject 分支触发 CRS。

---

**文档版本：** v1（与《实验计划文档：基于对比置信度的领域知识挟持与软引导投机解码》符号体系对齐：$\Delta P$、$\Delta\text{logit}$、C10/C11 门控叙事可复用。）
