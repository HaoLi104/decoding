# PPT 内容规划：基于对比置信度的领域软引导投机解码

> **主实验：C9（二值 token 级门控 + 线性 ΔP）**  
> 共 **20 页**，分五章节

---

## 封面（第 1 页）

**标题**：基于对比置信度的领域知识注入与软引导投机解码

**副标题**：Domain-Steered Speculative Decoding via Contrastive Confidence Guidance

**要素**：姓名 · 指导教师 · 学院 · 日期

---

## 第一章 研究背景及现状（第 2–5 页，共 4 页）

---

### 第 2 页：大模型推理的速度困境

**标题**：大模型的通用能力与推理速度的根本矛盾

**核心内容（3 个要点）**：
- **能力与速度不可兼得**：Qwen2.5-32B 等大模型在外科、法律、金融等垂直领域展现出卓越的推理与答题能力，但单卡生成速度仅约 **27 tokens/s**，远不满足实时交互需求
- **投机解码的基本思路**：用轻量的"草稿生成器"（Draft，3B）批量提案 γ 个 token，再由大模型（Target，32B）一次 forward 并行验收，理论上可实现 2–4× 加速，且不改变输出分布
- **关键问题**：标准投机解码的验收准则 $P'_{\text{accept}} = \min(1, P_T/P_D)$ 依赖两个模型分布的相似度——**3B 与 32B 的体量差距（10×）导致接受率仅约 20%，tps 甚至低于单跑 32B**

**配图建议**：左图=标准 SD 流程图；右图=实测接受率柱状图（standard_sd ≈ 20%）

---

### 第 3 页：垂直领域的核心矛盾

**标题**：领域专家被通用大模型"拒之门外"

**核心内容**：
- **现象描述**：一个经过外科专项微调的 3B 小模型，能精准预测"laparoscopic cholecystectomy"、"condylar"、"mandibular fracture"等专业词汇；但 32B 通用大模型的概率分布对这些词几乎为零，因此**总是拒绝 Draft 的领域提案**
- **矛盾本质**：垂直领域正是投机解码**最需要**的地方（领域应用对延迟最敏感），却是它**失效最严重**的地方（领域词分布差距最大）
- **实验验证**：Draft-Surgery（3B 微调）在外科问答上 acc=56.5% < Target（32B）的 acc=64.5%，标准 SD 无法从微调小模型获益

**配图建议**：核心矛盾示意图——"Draft 提案领域词 → 32B 拒绝 → 浪费推理资源"

---

### 第 4 页：已有方法与不足

**标题**：现有方法的局限

**表格对比**：

| 方法类别 | 代表工作 | 核心思路 | 不足 |
|---|---|---|---|
| 标准投机解码 | Leviathan et al. 2023 | Draft 提案 + Target 验收 | 大体量差下接受率极低 |
| 领域微调 Target | LoRA / FFT | 直接提升大模型领域能力 | 计算成本极高，损害通用能力 |
| 知识蒸馏 | Hinton et al. | 将大模型知识迁移到小模型 | 无法在推理时动态注入 |
| 对比解码 | Li et al. 2022 | Logit 空间减去"弱"模型 | 用于生成质量提升，不结合 SD 加速 |
| **本工作** | — | **概率域软引导 + 动态门控** | — |

**关键空白**：**如何在不微调 Target 的前提下，让 Target"相信"领域专家的知识，同时保持通用能力、实现推理加速？**

---

### 第 5 页：本文切入点

**标题**：核心洞察：用 Base 模型精准定位领域知识

**核心内容**：
- **洞察**：Draft 对"the"和"surgery"都有高概率，无法区分领域知识与通用知识。引入**同体量、未微调的 Base 模型**作为"常识对照组"：

$$\Delta P(x) = P_{\text{expert}}(x) - P_{\text{base}}(x)$$

- **$\Delta P$ 的含义**：精准提取"因领域微调而增强的那部分概率质量"，消去通用成分，只留领域增量
- **三模型架构**：Target（32B 通用大模型）+ Expert/Draft（3B 微调领域专家）+ Base（3B 未微调常识对照）
- **设计原则**：$\Delta P > 0$ 的位置就是 Expert 相对 Base 的领域优势词——这是整个系统的精准信号来源

**配图建议**：三模型架构图，突出 $\Delta P$ 的提取方式

---

## 第二章 研究目标（第 6–7 页，共 2 页）

---

### 第 6 页：核心研究问题

**标题**：三个核心问题

**三栏式布局**：

**问题一（知识注入）**  
在不修改 Target 参数的前提下，如何将领域专家小模型的领域知识有效注入 Target 的生成过程，使其在领域问答上的准确率超过纯 Target？

**问题二（推理加速）**  
如何在知识注入的同时，复用投机解码框架实现推理加速，使系统吞吐量不低于（理想情况下优于）传统 SD？

**问题三（通用能力保持）**  
注入机制如何自适应地区分"领域词"和"通用词"，避免在通用任务上损害 Target 的原有流利度？

---

### 第 7 页：技术路线概览

**标题**：技术路线：从基础公式到软引导投机解码

**流程图式布局（从左到右）**：

```
[基础公式]                [嵌入SD框架]              [静态→动态α]             [双信号门控]           [门控精化]
P_steered =              C1/C3 验收公式            C4: Draft信号            C6: AND门控           C8/C9: 
P_target +               嵌入比值域加法             C5: Target熵信号          稀疏+精准            token级对齐
α·ΔP                                              各自动态α
```

**预期目标**：
- acc：Surgery 外科问答 ≥ 70%（vs pure_target baseline 65%，提升 ≥ 5%）
- tps：维持 10–15 tokens/s（0.4–0.6×），显著优于 C1 固定 α 方案的效率
- 通用能力（GSM8K）：不下降

---

## 第三章 研究内容（第 8–14 页，共 7 页）

---

### 第 8 页：统一出发点与三模型架构

**标题**：统一公式与系统架构

**左侧：核心公式**

$$P_{\text{steered}}(x) = P_{\text{target}}(x) + \alpha \cdot \underbrace{(P_{\text{expert}}(x) - P_{\text{base}}(x))}_{\Delta P(x)\text{：领域增量信号}}$$

**右侧：嵌入投机解码验收（C1 框架）**

$$P'_{\text{accept}} = \min\!\left(1,\ \frac{P_T(x)}{P_D(x)} + \alpha \cdot \Delta P(x)\right)$$

**底部说明**：
- 分子等价于 $P_{\text{steered}}(x) + \alpha \cdot \Delta P(x) \cdot (1 - P_D(x))$，比值域加法对 $P_D$ 小值不敏感，适应大体量差场景
- 三个模型共享同一个 prompt 上下文，KV Cache 同步推进

---

### 第 9 页：创新一——稀疏激活（C4）

**标题**：创新一：Draft 自信度门控（Sparse Activation）

**问题**：固定 $\alpha$ 对所有 token 一视同仁，"the"和"surgery"得到相同强度注入，通用词被过度干扰

**方案**：

$$\alpha_t = \alpha_{\text{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t$$
$$S_t = \max(P_{\text{expert}}) - \max(P_{\text{base}}) \quad\text{（步级 Draft 置信度优势）}$$

**直觉**：自动驾驶的"专家辅助"模式——只在遇到"领域急转弯"（$S_t > \tau$）时才触发，平直路段完全由 Target 自主驾驶

**效果**：acc 从 baseline 65% 提升至 67%（+2%）；接受率从 20% 提升至 80%+，tps 显著提升

---

### 第 10 页：创新二——不确定性路由（C5）

**标题**：创新二：Target 熵驱动路由（Uncertainty-Driven Routing）

**问题**：C4 只看 Draft 端，不知道 Target 是否真的需要帮助——Target 对某词已经很确定时仍然注入，是无效干扰

**方案**：

$$\alpha_t = \lambda \cdot \frac{H_t}{H_{\max}}, \quad H_t = -\sum_x P_T(x)\log P_T(x)$$

**直觉**：Target 的输出熵是其"知识盲区"的天然探测器——**Target 越困惑，注入越强；Target 越确定，注入自动退化为零**

**前期实验佐证**（`probe_entropy.py`）：
- Surgery 做错题平均熵（0.456）显著高于做对题（0.387），p=0.0008
- ΔP>0.05 的领域词位置熵比通用词高 **57%**
- Pharmacology（Target 掌握充分）：熵差不显著（p=0.12），α 自动接近 0

**效果**：C5 最优 acc=68.5%（+3.5%）

---

### 第 11 页：创新三——双信号联合门控（C6）

**标题**：创新三：双信号 AND 门控（Dual-Signal Gating）

**问题**：C4 和 C5 各自有单信号误触发——Draft 自信但 Target 不需要（C4 误触发）；Target 困惑但 Draft 也没优势（C5 误触发）

**方案（两个正交信号相乘）**：

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(S_t>\tau)\cdot S_t}_{\text{C4：专家主动举手}} \cdot \underbrace{\frac{H_t}{H_{\max}}}_{\text{C5：学生主动求助}}$$

**逻辑**："专家有把握"AND"Target 遇到盲区"——两者同时成立，才强力注入；任一不满足，自动降为零

**效果**：C6 最优 acc=69%（+4%），tps=11.5（比 C1 相同 acc 时提升 **67%**）

---

### 第 12 页：创新四——token 级门控对齐（C8→C9）

**标题**：创新四：token 级门控（Token-Level Gate Alignment）

**问题**：C6 的 $S_t = \max(P_D) - \max(P_B)$ 是**步级**信号——Draft 在 token A 上高置信时，对 token B 的验收也被激活，存在跨 token 结构性误触发

**C8 方案**：

$$\alpha_t^{C8} = \lambda \cdot \mathbb{I}(\Delta P(x) > \tau) \cdot \Delta P(x) \cdot \frac{H_t}{H_{\max}} \quad\Rightarrow\quad \text{bonus} \propto (\Delta P)^2$$

将 $S_t$ 替换为 $\Delta P(x)$（token 级），门控与验收决策完全对齐，但引入了隐式平方

**C9 方案（主实验）**：

$$\alpha_t^{C9} = \lambda \cdot \underbrace{\mathbb{I}(\Delta P(x) > \tau)}_{\text{纯二值，是/否}} \cdot \frac{H_t}{H_{\max}} \quad\Rightarrow\quad \text{bonus} \propto \Delta P\text{（线性）}$$

将门控（是否激活）与幅度（激活多强）解耦，$\Delta P(x)$ 线性出现一次

**效果对比**：C8 acc=70.0%；C9（待测）

---

### 第 13 页：方法演进全景

**标题**：从基础公式到 C9 的七步创新

**演进表（竖向时间轴）**：

| 版本 | 核心改动 | 解决的难题 | 关键词 | 最优 acc |
|------|---------|-----------|--------|---------|
| C3 | $P_{\text{steered}}$ 嵌入 SD 分子 | 如何与 SD 结合 | 概率域补贴 | 66.0% |
| C1 | 改为比值域加法 | 大体量差下鲁棒性 | 比值域加法 | 69.0% |
| C4 | 步级 Draft 门控 | 固定 α 全程注入 | 稀疏激活 | 67.0% |
| C5 | Target 熵权重 | 不知道 Target 是否需要帮助 | 不确定性路由 | 68.5% |
| C6 | C4 × C5 乘积 | 单信号误触发 | 双信号 AND 门控 | 69.0% |
| C8 | 步级 → token 级门控 | 跨 token 结构性误触发 | token 级对齐 | **70.0%** |
| **C9** | 门控与幅度解耦 | $(\Delta P)^2$ 隐式平方副产品 | 二值门控+线性 | （待测） |

---

### 第 14 页：前期因果验证实验

**标题**：前期探针实验：验证"高熵位置 = 领域盲区"

**两列布局**：

**左列：熵分析（`probe_entropy.py`）**
- Surgery 做错题平均 H = 0.456 vs 做对题 0.387（+18%，p=0.0008）
- ΔP>0.05 领域词位置的熵比通用词高 57%
- Draft 延续词探针：在 Target 最困惑处，Draft 的 3-token 延续出现 condylar（髁突）、mandible fracture site（下颌骨骨折位点）等外科专知词汇，直接证明"Target 高熵 = 领域盲区 = Draft 有知识"的因果链

**右列：替换实验（`probe_substitution.py`）**

| τ（阈值） | acc | 替换率 | vs baseline |
|---|---|---|---|
| pure_target | 64.5% | 0% | — |
| 0.5 | 60.5% | 34% | −4 pt |
| 1.0 | 66.0% | 18% | +1.5 pt |
| 2.0 | **67.0%** | 1.6% | **+2.5 pt** |
| pure_draft | 56.5% | 100% | −8 pt |

结论：稀疏高熵替换有效（+2.5 pt）；粗替换有害（−4 pt）→ 支持"软引导优于硬替换"

---

## 第四章 目前进展（第 15–18 页，共 4 页）

---

### 第 15 页：主实验结果汇总

**标题**：主实验结果（MedMCQA Surgery，n=200，Qwen2.5-32B Target）

**核心结果表**：

| 策略 | 最优 acc | tps | acc\_rate | speedup |
|------|---------|-----|----------|---------|
| pure_target | 65.0% | 27.4 | 100% | 1.00× |
| standard_sd | ~65% | ~5 | ~20% | ~0.18× |
| C1（固定 α） | **69.0%** | 6.9 | 33% | 0.25× |
| C4（Draft 门控） | 67.0% | 14.5 | 77% | 0.54× |
| C5（熵路由） | 68.5% | 9.9 | 47% | 0.36× |
| C6（双信号） | **69.0%** | **11.5** | 60% | **0.42×** |
| C7（C3+C6） | 66.0% | 5.7 | 27% | 0.21× |
| **C8（token 级）** | **70.0%** | **12.2** | **59%** | **0.44×** |
| **C9（线性解耦）** | （待测） | — | — | — |

**关键结论**：C8 实现全系列最高 acc=70.0%（+5%），tps=12.2；C6 是 acc 与效率的最优平衡点

---

### 第 16 页：Pareto 分析

**标题**：acc-tps Pareto 前沿：C8 突破新边界

**配图（散点图，横轴 tps，纵轴 acc）**：

- 标注 pure_target（右上角，tps=27.4，acc=65%）
- 标注各策略散点，用折线连出 Pareto 前沿
- 突出 **C8（acc=70%，tps=12.2）** 为当前 Pareto 最优点

**文字说明**：
- **C1 vs C6**：同样 acc=69%，C6 的 tps=11.5（0.42×）比 C1（tps=6.9，0.25×）高 67%——动态 α 在相同准确率下实现更高吞吐
- **C8**：在 C6 的基础上 acc 再提升 1 pt，tps 不降——token 级门控是进一步消除误触发的关键
- **standard_sd**：处于 Pareto 劣势区（acc 未提升，tps 极低），证明大体量差的根本矛盾

---

### 第 17 页：消融实验验证设计有效性

**标题**：消融实验：每步设计的必要性验证

**三栏消融总结**：

**消融①：C3 vs C1（概率域 vs 比值域）**
- C3 最高 acc=66%，有效窗口极窄；C1 最高 acc=69%，稳定鲁棒
- 结论：比值域加法在大体量差下更优

**消融②：C7 vs C6（去掉 $P_d$ 隐式缩放）**
- C7 最高 acc=66%（−3 pt）；acc\_rate 仅 27%（−33 pt）
- 结论：$P_d(x)$ 隐式缩放是有益的天然稳压器，不是冗余

**消融③：C8 vs C6（步级 vs token 级门控）**
- C8 acc=70%（+1 pt），tps 相当
- 结论：门控信号与验收 token 对齐显著减少误触发

---

### 第 18 页：已完成工作清单

**标题**：已完成工作

**三栏布局**：

**数据与模型**
- ✅ 微调 Draft-Surgery（3B，MedMCQA 全参微调）
- ✅ 三模型单卡内聚架构（H200 BF16）
- ✅ MedMCQA 评测流水线（Surgery/Pharmacology/Anatomy）
- ✅ 数据加载与提示模板标准化

**核心算法**
- ✅ 手写 auto-regressive 解码循环（禁用 pipeline/generate）
- ✅ StaticCache KV Cache 显式管理
- ✅ C1–C9 全系列验收策略实现
- ✅ 工厂函数统一管理，CLI 参数控制

**前期探针实验**
- ✅ `probe_entropy.py`：Target 熵分布分析（Surgery/Pharmacology/Anatomy 三科目）
- ✅ `probe_substitution.py`：高熵位置直接替换因果验证（200 题）
- ✅ Draft 延续词探针（C4 阶段，验证领域词识别）

**待完成**
- ⬜ C9 主实验评测
- ⬜ GSM8K 通用能力验证
- ⬜ 完整 Pareto 可视化图
- ⬜ 论文写作

---

## 第五章 进度安排（第 19–20 页，共 2 页）

---

### 第 19 页：后续实验计划

**标题**：待完成实验与优先级

**甘特图式布局（按优先级排序）**：

| 实验 | 预计时间 | 优先级 | 说明 |
|------|---------|--------|------|
| C9 主实验评测 | 1–2 天 | ⭐⭐⭐ | λ_grid={5,10,20,50,100}，τ=0.05，Surgery 200题 |
| GSM8K 通用能力验证 | 1 天 | ⭐⭐⭐ | 验证注入机制不损害数学推理能力 |
| Pareto 可视化（完整） | 0.5 天 | ⭐⭐ | 整合 C1–C9 绘制 acc vs tps 散点图 |
| Law 领域泛化实验 | 2–3 天 | ⭐⭐ | 验证方法在 JEC-QA 司法考试上的泛化性 |
| 论文一稿 | 2 周 | ⭐⭐⭐ | 按毕业论文模板写作 |

---

### 第 20 页：毕业论文写作计划与时间轴

**标题**：时间安排与论文结构

**时间轴（横向）**：

```
4月中旬           5月初             5月中旬           6月初            6月中旬
   │                 │                  │                │                │
C9/C法实验        论文初稿           导师审阅修改      论文定稿         答辩
补全实验          第1-3章            补充实验数据       提交盲审         PPT定稿
```

**毕业论文章节规划**：

| 章节 | 内容 | 对应本工作 |
|------|------|-----------|
| 第 1 章 绪论 | 研究背景、意义、现状综述 | §背景 |
| 第 2 章 相关工作 | SD、领域适配、对比解码 | 文献综述 |
| 第 3 章 方法 | 三模型架构 + C1→C9 演进 | 核心贡献 |
| 第 4 章 实验 | 主实验 + 消融 + 探针实验 | 全部实验结果 |
| 第 5 章 结论与展望 | 总结三条设计原则 + Future Work | §11.8 |

**两个创新点（毕业论文框架）**：
- **创新点一**：领域引导解码（无需微调 Target，$\Delta P$ 信号 + 动态门控注入领域知识，保持通用能力）
- **创新点二**：将创新点一结合投机解码，同时实现知识注入与推理加速，token 级门控提升接受率精准性

---

*本 PPT 内容规划文档，最终 PPT 制作时各页可配合适当图表、公式框与色块排版。*
