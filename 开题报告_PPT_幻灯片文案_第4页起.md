开题报告 PPT 幻灯片文案（第 4 页起）

说明：第 1～3 页为你已完成的「研究背景及现状」。本文档从第 4 页起提供可直接粘贴进幻灯片的文字、图/表占位、以及 PowerPoint 公式可用的 LaTeX（插入 → 公式 → 在公式框内用 LaTeX 输入或粘贴；若版本不支持，可用 MathType 或在线转 OMML）。

────────────────────────────────────────

全局图号对照（与 开题报告_文档.md 一致时可统一引用）

图 F1：垂直领域下标准 SD 低接受/负加速示意（Target 否决领域 token）
图 F2：总体架构：DSSD 在线引导 → DAF 离线吸收闭环
图 F3：三模型拓扑 + ΔP 信号流（Draft / Base / Target）
图 F4：验收策略谱系：Standard → Hard Override → Soft-Guidance
图 F5：双信号门控 / C6 类机制示意（Draft 自信 × Target 不确定性）
图 F6：Accuracy–Tokens/s 帕累托或热图
图 F7：Target 熵/不确定性分布对比（路由直觉）
图 F8：DAF 飞轮：Decode → FDLP → Train → Re-decode

────────────────────────────────────────

章节 1｜研究背景及现状（补页）

第 4 页｜国内外研究现状与本课题切入点

章节条（页眉）：研究背景及现状

标题：研究现状综述：从「能加速」到「在垂直领域仍可用」

正文要点：

- 投机解码（SD）：Draft 提案、Target 并行验收；分布对齐时可在经典无偏框架下提升吞吐，工程上已广泛落地。
- 垂直领域 Draft：微调强化专有术语置信度，但与通用 Target 分布差增大，验收式 P_accept=min(1,P_T/P_D) 易表现为低接受甚至负加速。
- 分布修正与对比解码：提示可用「相对差异」抽取领域信号，但多数工作未与 SD 验收链路端到端对齐。
- PEFT 与选层：降低训练成本常见，但选层多依赖任务级损失或粗粒度统计，与「哪些 token 决策需改写」绑定不足。
- 本课题切口：Draft−Base 对比置信度 ΔP + 验收侧软引导（DSSD）；解码期 flip → FDLP 选层 + LoRA 的 DAF 闭环。

【此处补图】可选：图 F1（与前三页表格形成「问题可视化」）；或省略以控制篇幅。

【此处补表】可选：一页「方法谱系」小表（SD / 对比解码 / PEFT 选层 / 本文）——非必须。

────────────────────────────────────────

章节 2｜研究目标

第 5 页｜研究目标（模仿学院模板：总述段 + 首先/其次/最终 + 三条加粗目标）

版式建议（对齐你提供的范例）：顶部深蓝条白字「研究目标」；其下一段完整叙述（不要用零散 bullet 拆碎）；再下三条目标用主题蓝 + 加粗（字号略大于正文），版式可与范例「研究内容一/二/三」一致，但本章建议写「研究目标一/二/三」以免与第 3 章标题混淆。需要图示时：左侧或下半区放「路线图」、右侧放文字（参考你第二张范例的信息分区）。

章节条（页眉）：研究目标

（本页可不设单独大标题；若需要一行副标题，可用）面向垂直领域大模型推理的协同加速与能力沉淀

【正文整段，直接粘贴进文本框】

本研究旨在面向垂直领域大模型推理（主线任务：MedMCQA‑Surgery），在通用 Target 与领域专家 Draft 能力互补的前提下，解决投机解码在分布失配场景下常见的接受率塌陷与吞吐负加速问题，并进一步回答：推理期可观测、可记录的领域信号能否稀疏、可控地沉淀为 Target 的参数增量，从而形成「研究目标一：对比置信度软引导投机解码」与「研究目标二：领域信号锚定 LoRA + 解码—微调迭代闭环」相衔接的一体化路径。研究在统一工程约束下展开：手写自回归解码、显式 KV（如 StaticCache）、bfloat16、单卡 device_map="cuda:0"，模型矩阵为 Target（32B）+ Draft（3B 领域）+ Base（3B 同源对照）。

首先，构建三模型协同投机解码框架，基于 Draft−Base 对比置信度 ΔP 刻画候选 token 的相对领域化强度，并在验收链路设计强度连续可调的软引导机制（DSSD），在避免「硬拧分布」失控风险的同时，提高领域高价值 token 的可接受性与有效吞吐，追求 Accuracy 与真实 Tokens/s 的帕累托改进。

其次，在投机解码产生稳定可复用解码日志的前提下，提取可锚定训练的领域信号（例如 flip 等事件），用于 LoRA 层位选择与训练样本构造（FDLP），再通过多轮「解码—微调—再解码」形成闭环（DAF），将投机解码带来的在线增益沉淀为 Target 参数增量，从而降低长期多模型常驻部署成本。（机制与实现细节见本章「研究内容二」。）

最终，在统一评测口径下完成可复现的对照与消融，形成从现象—机制—指标的完整证据链：不仅报告任务准确率与 Tokens/s，还系统刻画接受率/有效步长、ΔP 触发与门控稳定性、flip rate 与层热点的跨轮演化，并对通用域守护、事件可替代性、飞轮是否退化为单轮等关键命题给出可证伪的检验与风险预案，为垂直领域 LLM 的工程化部署提供可审查、可复制的技术方案。

【页底三条：主题色加粗，对齐范例视觉层级；若模板只放两条，可删「研究目标三」】

研究目标一：基于对比置信度的领域知识注入与软引导投机解码。

研究目标二：以领域信号为锚点完成 LoRA 层位选择，并通过解码—微调迭代闭环将投机解码的在线增益沉淀为 Target 参数增量。

研究目标三（可选）：建立主任务精度、真实吞吐与解码事件统计的一体化评测与消融口径，并给出可核对、可复现的实验记录方式。

【此处补图】建议：图 F2（DSSD → DAF 总闭环），放在文字左侧或段落下方通栏，版式参考你第二张范例「左图右文」。

【此处补公式（可选，小字号）】验收基线（点题用，非必须）

LaTeX（粘贴到 PPT 公式框）：

P_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)

────────────────────────────────────────

第 6 页｜研究目标（答辩补充）：可验证指标与交付物（可选页）

说明：若学院模板要求「研究目标」仅占 1 页，可将本页删除或并入第 7 页（研究内容总览）；若评委偏好「目标可度量」，保留本页作为指标清单。

章节条：研究目标

副标题（黑体）：关键指标与可交付证据（与日志字段对齐）

正文要点：

- 任务精度：Surgery 子集 Accuracy；对比纯 Target、标准 SD、DSSD、DAF 固化 Target（LoRA merge）。
- 真实吞吐：Tokens/s；并报告接受率 / 平均接受步长（口径与实现日志一致）。
- DSSD 机制侧：ΔP 触发覆盖率、软引导强度、熵路由/门控稳定性（防过引导）。
- DAF 机制侧：flip rate 随轮次、Top‑层集合跨轮一致性、通用守护集监控（防 Target≈Draft 复制）。
- 工程一致性：H200 单卡、bfloat16、显式 KV、与实验室解码规范一致。

【此处补表】强烈建议：指标 × 含义 × 数据来源（日志字段）一页表（答辩「可复现」用）。

────────────────────────────────────────

章节 3｜研究内容

第 7 页｜总体技术路线（承接研究目标一、二）

章节条：研究内容

标题：总体框架：在线软引导投机解码（目标一）→ 解码—微调闭环沉淀参数（目标二）

正文要点：

- 研究内容一（对应研究目标一）：用 Draft 相对 Base 的对比置信度 ΔP 做领域知识注入，在投机解码验收链路上做软引导，缓解垂直域低接受与吞吐负加速。
- 研究内容二（对应研究目标二）：从解码日志中抽取领域信号，锚定 LoRA 层位与训练构造（FDLP），用多轮解码—微调闭环（DAF）把在线增益写入 Target 参数。
- 递进关系：先完成「推理侧可用」与评测闭环，再完成「参数侧可沉淀」与部署降本叙事。

【此处补图】图 F2（若第 5 页未用，此页必补）；或图 F8 预览飞轮。

────────────────────────────────────────

研究内容一（对应研究目标一：基于对比置信度的领域知识注入与软引导投机解码）

以下第 8～14 页按四个角度展开（每角度约 1～2 页），细节与符号与论文第一个点实验文档一致；口头可简称 DSSD 主线。

────────────────────────────────────────

第 8 页｜研究内容一（角度 1）：为何引入 Base，对比置信度 ΔP 是什么

章节条：研究内容一

标题：三模型与 ΔP：把「领域专知」从背景概率里抠出来

正文要点：

- 仅用 Target+Draft：领域 token 上常出现 Draft 高置信、Target 概率低，标准投机解码用 P_target/P_draft 验收会系统性拒绝「对的领域词」，接受率塌陷。
- 引入与 Draft 同结构、但未做领域微调的 Base：二者共享通用先验，差异主要来自领域适配。用 Draft 相对 Base 在同一候选 token x、同一固定温度下的概率差作为对比置信度 ΔP，刻画「相对领域化强度」，避免把全局温度采样带来的平滑误当成领域信号。
- 工程上仍由 Target 做最终验收；ΔP 只作为注入项，不替代 Target 分布。
- 领域触发条件（与实验脚本一致）：P_draft(x) 高于 θ_high 且 ΔP 高于 τ 时，才允许软引导，压低伪领域尖峰。

【此处补图】图 F3（Target / Draft / Base + ΔP 箭头）。

（本页可不贴长公式；ΔP 定义见第 9 页。）

────────────────────────────────────────

第 9 页｜研究内容一（角度 2，上）：基线验收与 ΔP 进入框架的入口

章节条：研究内容一

标题：标准投机解码 + 领域门控：改的是「验收分子」不是「改 Draft」

正文要点：

- 基线：对候选 token x 的标准接受概率（与 Leviathan 等一致）。
- 软引导思路：在保持「由谁提案、由谁验收」的前提下，把 ΔP 以加权形式进入 P'_accept 或先校准 P_target 再验收（两套骨架：比值域 C1 系、概率域 C3 系）。
- Hard Override 仅作消融上界，答辩主线讲软引导。

标准验收 — PPT LaTeX：

P_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)

ΔP 定义（固定温度 T_fixed，与采样温度解耦）— PPT LaTeX：

\Delta P=
\mathrm{softmax}\!\left(\frac{\mathrm{logit}_{\mathrm{draft}}}{T_{\mathrm{fixed}}}\right)\![x]
-
\mathrm{softmax}\!\left(\frac{\mathrm{logit}_{\mathrm{base}}}{T_{\mathrm{fixed}}}\right)\![x]

（若公式框不支持 \mathrm{softmax}，可改为 \operatorname{softmax}。）

────────────────────────────────────────

第 10 页｜研究内容一（角度 2，下）：固定强度 α 的两种注入骨架（C1 / C3）

章节条：研究内容一

标题：先把 ΔP 写进式子：固定 α 的 C1 与 C3

正文要点：

- C1（比值域加项）：在 P_target/P_draft 上直接加 α·ΔP，实现简单，但 α 全程常数，容易「该强不强、不该强又偏强」。
- C3（概率域两步）：先把 Target 对 x 的概率加上 α·ΔP，再代回标准比值验收，分子分母都是概率，量纲一致；同样受「固定 α」限制。
- 后续动态 α_t 两条创新分别作用在 Draft 侧门控与 Target 侧熵权，可嵌回 C1 或 C3 骨架（论文中 C6 主写在 C1 骨架，C7 为 C3+同构 α_t 的框架消融）。

C1 — PPT LaTeX：

P'_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}+\alpha\cdot\Delta P\right)

C3 Step 1 / Step 2 — PPT LaTeX：

P'_{\mathrm{target}}(x)=P_{\mathrm{target}}(x)+\alpha\cdot\Delta P

P'_{\mathrm{accept}}=\min\left(1,\frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)

【此处补图】可选：图 F4（从 Standard → C1/C3 的路线图）。

────────────────────────────────────────

第 11 页｜研究内容一（角度 3，上）：固定 α 的问题与 Draft 侧创新（C4）

章节条：研究内容一

标题：固定 α 不随步变化：专家不该「全程喊话」

正文要点：

- 垂直域里步与步之间，领域信号强弱差很大；常数 α 要么拖累吞吐（α 大则接受率虚高、失真风险），要么抬不动 acc（α 小则该救的领域步救不到）。
- C4（Draft 侧门控）：用步级自信度优势 S_t 衡量「这一拍专家是否明显比通用对照更自信」；仅当 S_t 超过阈值 τ 才打开注入，且强度随 S_t 缩放，实现稀疏激活（专家举手才说话）。

S_t — PPT LaTeX：

S_t=\max P_{\mathrm{draft}}-\max P_{\mathrm{base}}

（max 表示对该步词表分布取最大概率；与论文记号一致。）

C4 动态 α — PPT LaTeX：

\alpha_t=\alpha_{\mathrm{base}}\cdot\mathbb{I}(S_t>\tau)\cdot S_t

验收仍可用 C1 形式，把 α 换成 α_t（下同）。

────────────────────────────────────────

第 12 页｜研究内容一（角度 3，下）：Target 侧创新（C5，香农熵）

章节条：研究内容一

标题：Target 侧：用香农熵衡量「学生有多懵」，再决定听多少专家

正文要点：

- 仅 C4：专家一举手就可能注入；若 Target 本步已极度自信（分布极尖），仍可能被多余补贴扰动。
- C5：用 Target 本步预测分布的香农熵 H_t 作不确定性；H_t 相对最大熵 H_max=log V 归一化，熵越高注入权重越大，熵趋零则注入关闭（学生越懵越求助）。

香农熵与归一化 — PPT LaTeX：

H_t=-\sum_x P_{\mathrm{target}}(x)\log P_{\mathrm{target}}(x)

H_{\max}=\log V

C5 动态 α — PPT LaTeX：

\alpha_t=\lambda\cdot\frac{H_t}{H_{\max}}

C5 下验收（C1 骨架）— PPT LaTeX：

P'_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}+\alpha_t\cdot\Delta P\right)

【此处补图】图 F7（H_t 分布或路由示意），建议。

────────────────────────────────────────

第 13 页｜研究内容一（角度 4，上）：双信号联合（C6）与最终 α_t

章节条：研究内容一

标题：C6：专家举手 ∧ 学生求助，才打开注入

正文要点：

- C4 与 C5 相乘（AND）：既要 Draft 相对 Base 有显著自信优势，又要 Target 本步足够「不确定」，避免盲目补贴。
- 论文叙事：C4 侧为专家主动举手，C5 侧为学生主动求助（熵权）；最终动态强度由 λ 封顶。
- 同构 α_t 可接到 C3 概率域得到 C7；token 级门控 C8/C9 等为后续消融，答辩可一句带过。

最终 α_t（C6）— PPT LaTeX：

\alpha_t=\lambda\cdot\underbrace{\mathbb{I}(S_t>\tau)\cdot S_t}_{\text{专家主动举手}}\cdot\underbrace{\frac{H_t}{H_{\max}}}_{\text{学生主动求助}}

配套验收（C1 骨架，与实验主表一致）— PPT LaTeX：

P'_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}+\alpha_t\cdot\Delta P\right)

超参数：λ（命令行 --alpha）、τ（--c4_tau，实验表一例取 τ=0.05）。

【此处补图】图 F5（双门控示意）。

────────────────────────────────────────

第 14 页｜研究内容一（角度 4，下）：策略演进与主实验表（MedMCQA Surgery，贪婪解码）

章节条：研究内容一

标题：从固定 α 到双门控：acc、tps、接受率一锅端

正文要点：

- 演进顺序（答辩可画一条轴）：Standard SD → 固定 α（C1/C3）→ Draft 门控 C4 → 熵权 C5 → 联合 C6（及 C7 框架消融）。
- 实验设置摘要：Surgery 子集；Target 32B、Draft Surgery 微调、Base 3B；贪婪采样 T_sample=0；n=200 与论文 8.2 节一致；指标 acc、tps、mean acceptance rate、相对 pure_target 的 speedup。
- 单模型参照（全量 val n=249）：Pure Target acc≈0.650；Draft ckpt-1676 acc≈0.574；Base≈0.494。

【此处补表】建议直接做一页 PPT 表（摘自论文 8.2，便于对照）：

策略 | 关键设置 | acc | tps | acc_rate | 备注
pure_target | — | 0.650 | 27.3 | 1.000 | 速度上限参照
standard_sd | Draft-Surgery | 0.650 | 5.3 | 0.212 | 接受率低、加速失效
C1 | α=0.10 | 0.690 | 6.9 | 0.334 | acc 高、tps 仍低
C5 | λ=5 | 0.685 | 8.2 | 0.367 | 熵路由改善帕累托
C6 | τ=0.05, λ=50 | 0.690 | 11.5 | 0.600 | 双门控，acc 与 tps 更均衡

口播结论：C6 在 acc 打平或优于 C1 高点的同时显著提高 tps；相对纯 Target 仍有 speedup<1 的代价，叙事重点在垂直域「可接受、可加速」相对 Standard SD 的帕累托改进。

【此处补图】图 F6（帕累托散点或热图）。

────────────────────────────────────────

研究内容二（对应研究目标二：以领域信号为锚点的 LoRA 层位选择 + 解码—微调迭代闭环）

以下第 15～19 页属于研究内容二的展开；主线仍记 DAF（飞轮）+ FDLP（选层），领域信号以解码日志中的 flip 等事件为主。

────────────────────────────────────────

第 15 页｜研究内容二：动机与问题（为何需要「沉淀」）

章节条：研究内容二

标题：研究目标二在解决什么：从「在线增益」到「参数增量」

正文要点：

- 背景：研究内容一的软引导投机解码往往仍依赖 Target+Draft（+Base）协同，工程上希望把可重复出现的在线增益写回 Target。
- 锚点：领域信号来自解码过程（例如 flip：Target 原偏好与最终落盘 token 不一致，且最终 token 来自被接受的 Draft 提案），比纯任务级 loss 更贴近「哪里真需要改」。
- 子问题一：如何用领域信号选择 LoRA 挂载层与 rank 预算（FDLP）。
- 子问题二：如何用「解码—微调—再解码」迭代，让在线增益逐步变成 Target 参数能力，并给出停止判据（DAF）。

【此处补图】图 F8（飞轮闭环），强烈建议。

────────────────────────────────────────

第 16 页｜研究内容二：领域信号（flip）定义、日志与双重用途

章节条：研究内容二

标题：领域信号怎么用：既选 LoRA 层位，也看飞轮是否该停

正文要点：

- 信号定义（主线）：flip 事件在解码日志中可逐 token 记录（与 TelemetryLogger 字段对齐）。
- 用途一（FDLP）：在 flip 样本上对「把概率推向最终 token」的损失做梯度敏感度累计，得到层分数 → Top‑K 层 + rank 分配。
- 用途二（DAF）：统计 flip rate 随迭代轮次变化，作为闭环收敛/早停的外显指标，减少只盯准确率带来的争议。
- 采集建议：飞轮实验固定同一套软引导策略（如 C9），避免「换策略等于换信号分布」。

【此处补表】建议：日志字段 qid, step, A, B, F, ΔP, … 一页表（答辩「可复现」用）。

Flip 加权损失（口播支撑）— PPT LaTeX：

\mathcal{L}(p_t,B_t)=-\log P_{\theta}(B_t\mid p_t)

层/模块敏感度（示意，F 范数与归一化可按论文主符号）— PPT LaTeX：

s_{\ell,m}=\mathbb{E}_{t:F_t=1}\!\left[
\frac{\left\lVert \nabla_{W_{\ell,m}}\mathcal{L}\right\rVert_F}
{\sqrt{\lVert W_{\ell,m}\rVert_0}+\varepsilon}
\right]

────────────────────────────────────────

第 17 页｜研究内容二：FDLP（单轮 LoRA 层位选择）

章节条：研究内容二

标题：以领域信号为锚点：从 flip 到「挂哪些层、给多少 rank」

正文要点：

- 打分：flip 处梯度 → 层分数 S_ℓ；Top‑K 层 + 总 rank 预算按比例分配。
- 验证（可选）：activation patching 峰值层与排序一致性（小样本）。
- 数据：flip 前缀监督 B_t；配平非 flip；可保留少量通用锚点。

【此处补图】建议：层分数条形图或 Transformer 层示意图 + Top‑K 高亮。

────────────────────────────────────────

第 18 页｜研究内容二：DAF 主循环与判据（解码—微调迭代闭环）

章节条：研究内容二

标题：Decode → Place → Train → Re‑decode：把投机解码在线增益写成参数增量

正文要点：

- 每轮：DSSD 采集事件与 flip rate → FDLP → LoRA+SFT → 更新 Target。
- 停止：相邻轮 flip rate 差低于阈值，或相对首轮吸收比例达标；设 K_max（如 ≤ 3）。
- 累积：建议主线 merge 后重选层（部署友好）。
- 风险：通用域守护，防 Target≈Draft 复制。

【此处补图】图 F8（若第 15 页未展开细节，此页放「流程图 + 判据框」）。

【此处补表】建议：每轮 flip rate、层集合、Surgery acc、通用集 acc 跟踪表（占位即可）。

────────────────────────────────────────

第 19 页｜研究内容二：创新边界与可证伪门槛（答辩防御）

章节条：研究内容二

标题：研究目标二「新」在哪里：组合边界 + 主动可证伪

正文要点：

- 不单点宣称：梯度选层、token 加权 SFT、迭代微调各自已有大量工作。
- 组合宣称：SD 内生 flip 同时服务 PEFT placement 与飞轮收敛判据。
- 可证伪：非 flip 事件能否同样收敛？是否一轮饱和？是否通用域显著退化？

【此处补图】可选：「门槛 A/B/C」三块图标式排版（与 DAF 文档 limitations 一致）。

────────────────────────────────────────

章节 4｜目前进展

第 20 页｜已完成工作

章节条：目前进展

标题：已完成：研究内容一主线（Draft 领域化 + 对比置信软引导投机解码）

正文要点：

- Draft‑Surgery 微调与 checkpoint 选择（如 checkpoint‑1676 等，以实测表为准）。
- 评测管线：Surgery；Accuracy、Tokens/s、接受率；解码循环与缓存工程规范落地。
- DSSD：ΔP、过滤、多策略软引导；帕累托证据链整理中/已定稿。
- 日志可扩展至 flip 字段，为 DAF 铺路。

【此处补表】强烈建议：主实验结果小表（基线 vs 代表软引导，两行 Accuracy + tps）。

────────────────────────────────────────

第 21 页｜进行中与风险

章节条：目前进展

标题：进行中：研究内容二（领域信号锚定 LoRA + DAF 飞轮与证据链）

正文要点：

- FDLP 打分与 patching 对照；DAF 多轮曲线与层热点稳定性。
- 部署叙事：merge‑LoRA 后推理配置对比（目标）。
- 风险：事件稀疏、算力预算、训练—推理动力学不一致；预案：采样与 K_max。

【此处补图】若有：flip rate vs 轮次曲线（占位）。

【此处补表】实验 Todo 清单（可选）。

────────────────────────────────────────

章节 5｜进度安排

第 22 页｜工作计划

章节条：进度安排

标题：论文工作时间表（按学院模板改月份）

正文要点（示例节点）：

- 完成 DAF 多轮实验与消融（flip vs 其他事件；K 敏感性）。
- 机制图与主表定稿；论文撰写与统稿。
- 脚本/配置/日志 schema 整理；预答辩—修改—正式答辩。

【此处补图】甘特图（时间轴）：若学校模板自带则优先用模板。

────────────────────────────────────────

附录：PPT 常用公式速查（可直接复制）

标准 SD 接受率：

P_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)

ΔP（与研究内容一第 9 页相同）：

\Delta P=
\mathrm{softmax}\!\left(\frac{\mathrm{logit}_{\mathrm{draft}}}{T_{\mathrm{fixed}}}\right)\![x]
-
\mathrm{softmax}\!\left(\frac{\mathrm{logit}_{\mathrm{base}}}{T_{\mathrm{fixed}}}\right)\![x]

C3 两步：

P'_{\mathrm{target}}(x)=P_{\mathrm{target}}(x)+\alpha\cdot\Delta P

P'_{\mathrm{accept}}=\min\left(1,\frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)

C1（可选）：

P'_{\mathrm{accept}}=\min\left(1,\frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}+\alpha\cdot\Delta P\right)

Draft 自信度优势 S_t（C4 叙事用，可选）：

S_t=\max P_{\mathrm{draft}}-\max P_{\mathrm{base}}

────────────────────────────────────────

页数压缩建议（若必须控制总页数）

- 研究内容一：可合并第 9～10 页（基线+C1/C3），或合并第 11～12 页（C4+C5）。
- 研究内容二：可合并第 18～19 页（飞轮 + 可证伪三句话）。
- 第 4 页与第 7 页二选一保留「现状」或「总框架」详细版。

────────────────────────────────────────

文档生成说明：与 开题报告_文档.md、论文第一个点:基于对比置信度的领域知识挟持与软引导投机解码.md、毕业论文第二点_领域吸收飞轮(DAF).md 叙事对齐；实验数字以最终汇总表为准。
