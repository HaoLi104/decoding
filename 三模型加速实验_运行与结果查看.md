# 三模型投机解码加速实验：运行与结果查看

## 1) 运行实验

```bash
export CUDA_VISIBLE_DEVICES=0,1
bash scripts/run_three_model_spec_accel.sh
```

> 说明：脚本会依次运行  
> `target baseline` -> `draft-only baseline` -> `standard speculative` -> `divergence_v2`，并在最后产出核心对比表。

---

## 2) 查看结果（推荐流程）

### 2.1 找到最新实验目录

```bash
ls -lt logs/three_model_spec
```

如果使用默认 `RUN_TAG`，通常目录是：

```bash
logs/three_model_spec/three_model_spec_accel
```

---

### 2.2 查看核心对比结果（最重要）

```bash
cat logs/three_model_spec/three_model_spec_accel/core_comparison.md
```

这个表会对比：
- `target_baseline`
- `draft_only`
- `standard_speculative`
- `divergence_v2`

关键指标：
- `accuracy`
- `tokens_per_sec_end_to_end`

---

### 2.3 查看每个实验 JSON 的 summary（详细）

```bash
python - <<'PY'
import json
from pathlib import Path

run_dir = Path("logs/three_model_spec/three_model_spec_accel")
for p in sorted(run_dir.glob("*.json")):
    data = json.loads(p.read_text(encoding="utf-8"))
    s = data.get("summary", {})
    print("=" * 80)
    print(p.name)
    for k in [
        "mode",
        "accuracy",
        "tokens_per_sec_end_to_end",
        "override_calls",
        "accepted_override",
        "small_base_calls",
        "v2_precheck_skips",
        "small_base_call_rate_per_override_call",
    ]:
        if k in s:
            print(f"{k}: {s[k]}")
PY
```

---

## 3) 若你使用了自定义 RUN_TAG

例如：

```bash
RUN_TAG=my_run CUDA_VISIBLE_DEVICES=0,1 bash scripts/run_three_model_spec_accel.sh
```

则结果目录是：

```bash
logs/three_model_spec/my_run
```

查看命令对应替换目录即可。

---

## 4) 建议发给我做分析的文件

请把以下内容贴给我（至少前两项）：

1. `logs/three_model_spec/<run_tag>/core_comparison.md`
2. `logs/three_model_spec/<run_tag>/divergence_v2_*.json` 里的 `summary`
3. （可选）`standard_speculative_*.json` 与 `target_baseline_*.json` 的 `summary`

我会基于这些数据给你做：
- 加速是否成立（相对 baseline）
- 三模型创新是否带来净收益（accuracy vs TPS）
- 下一轮参数建议（K、tau_delta、tau_target_opp）
