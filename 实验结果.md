# 实验结果汇总

## 1. 本轮分类器训练所使用的数据划分

### 1.1 数据来源
本轮 8 个分类器实验，全部使用同一份自动挖掘得到的数据集：

- `logs/mined_full_test_qwen_dp.features.npz`
- `logs/mined_full_test_qwen_dp.info.json`
- `logs/mined_full_test_qwen_dp.meta.jsonl`

这份数据集本身来自 **MedQA test split**，挖掘流程如下：

1. 先在 MedQA test 上筛选满足 `target model 错误 && draft model 正确` 的 case；
2. 在这些 case 上，沿着 target 的生成前缀逐 token 寻找所有分歧点；
3. 对每个分歧点执行“强行接受 draft token 后让 target 继续生成”的过程；
4. 根据 target 是否被纠正回正确答案，得到硬标签 `important=True/False`；
5. 保存对应特征（hidden states 拼接）与 `delta_p` 等信息。

### 1.2 训练集 / 验证集到底是什么
本轮分类器训练 **没有使用单独的外部测试集**。

具体来说：

- 输入总样本集：`mined_full_test_qwen_dp`
- 总样本数：`6759`
- 标签分布：
  - 正样本：`1924`
  - 负样本：`4835`

训练脚本 `train_token_classifiers.py` 的做法是：

- 在这 **同一份 mined_full_test_qwen_dp 数据内部** 做分层切分（stratified split）；
- 默认参数 `--val_ratio 0.2`，即：
  - 约 `80%` 作为训练集
  - 约 `20%` 作为验证集
- 切分是按标签分层的，因此训练/验证集中的正负比例大致一致。

因此，**之前 8 个实验里所谓的“训练集”和“验证集”其实都是从 MedQA test 挖掘结果内部切出来的**，并不是：

- “MedQA train mined data 训练 + MedQA test mined data 测试” 的严格泛化评估；
- 也不是单独保留的第三份真正意义上的 test set。

### 1.3 这意味着什么
这组结果可以很好地说明：

1. 自动挖掘数据流程是有效的；
2. 分类器确实学到了稳定信号；
3. `teacher/student` 架构在当前数据分布上是可行的。

但是，这组结果 **还不能作为最终泛化结论**，因为：

- 数据来自 `MedQA test`；
- 模型评估是在同一份 mined 数据内部切分完成的；
- 目前更准确地说，这是一组 **开发阶段验证结果（development-time validation）**。

因此，后续若要形成更严格的结论，应该进一步做：

1. 挖掘 `MedQA train` 得到训练数据；
2. 用 `mined_train` 训练分类器；
3. 用 `mined_test` 做真正 hold-out evaluation。

---

## 2. 8 个实验结果总表

### 2.1 第一组：带 `delta_p` 加权的实验

| 实验编号 | 模式 | 分类器 | 是否蒸馏 | 是否使用 `delta_p` 加权 | Val AUC | Val ACC |
|---|---|---|---|---|---:|---:|
| 1 | Teacher | Logistic | 否 | 是 | 0.7721 | 0.6886 |
| 2 | Teacher | MLP | 否 | 是 | 0.7765 | 0.7507 |
| 3 | Student | Logistic | 是 | 是 | 0.7787 | 0.6893 |
| 4 | Student | MLP | 是 | 是 | 0.7854 | 0.7633 |

### 2.2 第二组：不使用 `delta_p` 加权的实验 / Baseline 与对照实验

| 实验编号 | 模式 | 分类器 | 是否蒸馏 | 是否使用 `delta_p` 加权 | Val AUC | Val ACC |
|---|---|---|---|---|---:|---:|
| 5 | Student Hard Baseline | Logistic | 否 | 否 | 0.7747 | 0.7019 |
| 6 | Student Hard Baseline | MLP | 否 | 否 | 0.7830 | 0.7611 |
| 7 | Teacher | MLP | 否 | 否 | 0.7783 | 0.7626 |
| 8 | Student | MLP | 是 | 否 | **0.7875** | 0.7567 |

---

## 3. 按研究问题组织的结果解读

## 3.1 分类器路线是否成立？

结论：**成立，而且信号明显。**

依据：

- 在 mined 数据分析阶段，单独使用 `delta_p` 作为分数时，AUC 只有：
  - `delta_p_auc = 0.5163`
- 而分类器训练后的结果能稳定达到：
  - `0.7721 ~ 0.7875`

这说明：

- 真正有效的信息主要存在于 hidden states 特征中；
- “在分歧点处使用模型内部表征判断该 token 是否重要”这条路线是有效的；
- 自动挖掘得到的 hard label 足以支持稳定训练。

---

## 3.2 只使用线上可用的 `[H_target, H_draft]`，效果是否足够好？

结论：**是的，而且已经相当强。**

最直接的证据来自 baseline：

- `Student Hard Baseline + Logistic`: `AUC = 0.7747`
- `Student Hard Baseline + MLP`: `AUC = 0.7830`

这说明即使：

- 不使用 `small_base`；
- 不做蒸馏；
- 只用线上可用的 `[H_target, H_draft]` 特征；

分类器本身也已经具备较强判别力。

这对项目目标非常重要，因为这说明：

> 最终线上部署时，只用 target + draft 的隐藏状态，确实有机会获得较好的 accept/reject 判别能力，而不需要在线额外运行 small_base。

---

## 3.3 引入 `small_base` 后，teacher/student 蒸馏是否有效？

结论：**有效，但当前收益幅度不算大。**

关键比较如下：

### Logistic 路线
- Baseline (`student_hard logistic`, no-weight): `0.7747`
- Student distill (`student logistic`, weighted): `0.7787`

蒸馏后提升约：
- `+0.0040`

### MLP 路线
- Baseline (`student_hard mlp`, no-weight): `0.7830`
- Student distill (`student mlp`, no-weight): `0.7875`

蒸馏后提升约：
- `+0.0045`

因此可以得到阶段性判断：

1. `small_base` 提供的信息并非完全无用；
2. 通过 teacher → student 的蒸馏，确实能给线上 student 模型带来小幅收益；
3. 但目前提升仍然比较有限，说明：
   - 当前蒸馏方案已经有效；
   - 但还有优化空间。

也就是说，**方案 B（特征空间蒸馏）是成立的**。

---

## 3.4 `delta_p` 加权是否有效？

结论：**以当前映射方式看，暂时没有验证出收益。**

### Teacher MLP 对比
- `Teacher MLP, weighted`: `0.7765`
- `Teacher MLP, no-weight`: `0.7783`

### Student MLP 对比
- `Student MLP, weighted distill`: `0.7854`
- `Student MLP, no-weight distill`: `0.7875`

两条对比都表明：

- **不加权反而略优于加权**。

这与前面 mined 数据的统计是一致的：

- `delta_p_auc = 0.5163`，单独几乎没有判别力；
- 一些极高 `delta_p` 的样本实际上是噪声 token，例如 `"<"` 这类符号；
- 说明当前的 `delta_p -> sample weight` 映射仍然过于粗糙。

因此，目前更合理的结论不是“`delta_p` 完全没用”，而是：

1. `delta_p` **不适合直接当作分类分数**；
2. 当前这版权重映射（例如 `one_plus_relu`）**还没有带来收益**；
3. 若后续继续探索，应考虑：
   - 更强的截断（clipping）；
   - rank/quantile weight，而不是简单线性映射；
   - 仅对某类 token 使用权重；
   - 结合 token 类型过滤后再做加权。

也就是说，**方案 A（`delta_p` 加权）在当前版本下暂不成立**。

---

## 3.5 Logistic 还是 MLP 更适合？

结论：**MLP 效果更好，但 logistic 仍然是很强的轻量基线。**

### MLP 通常优于 Logistic
- Teacher：
  - Logistic weighted: `0.7721`
  - MLP weighted: `0.7765`
- Baseline：
  - Student hard logistic: `0.7747`
  - Student hard mlp: `0.7830`
- Student：
  - Student logistic weighted distill: `0.7787`
  - Student mlp no-weight distill: `0.7875`

因此：

- 如果优先追求效果，当前首选是 **MLP**；
- 如果后续非常强调极简部署和最小额外开销，**logistic** 仍然值得保留作为对照和部署备选。

---

## 4. 当前最佳模型

从这 8 组实验来看，当前最佳结果是：

### **Student + MLP + Distillation + No Weight**

- 模式：`student`
- 结构：`mlp`
- 蒸馏：是
- `delta_p` 加权：否
- **Val AUC = 0.7875**
- **Val ACC = 0.7567**

这个结果最符合项目目标，因为：

1. 它是线上可部署结构：只用 `[H_target, H_draft]`；
2. 它保留了来自 teacher 的软知识；
3. 它在当前所有实验中取得了最高 AUC。

因此，若要继续推进线上 accept/reject 判别器，**当前首选应是该模型**。

---

## 5. 阶段性实验结论

基于当前 8 组结果，可以给出以下阶段性结论：

### 结论 1：自动挖掘数据算法是有效的
自动挖掘得到的分歧点 + 强行续写打标方式，能够构造出足以训练高质量分类器的数据集。

### 结论 2：hidden state 分类器路线成立
分类器性能显著高于单独使用 `delta_p`，说明分歧点处的隐藏状态确实蕴含“该 token 是否重要”的结构化信息。

### 结论 3：只用 `[H_target, H_draft]` 已经可以做出很强的线上分类器
说明你设想的“零额外线上推理开销”路线是现实可行的。

### 结论 4：特征蒸馏（方案 B）成立
引入 `small_base` 训练 teacher，再蒸馏到 student，带来了小幅但稳定的收益。

### 结论 5：当前版本的 `delta_p` 加权（方案 A）尚未验证出收益
因此当前不应把 `delta_p` 加权当作核心成功点，而应将其视为待进一步优化的组件。

---

## 6. 对项目主线的直接判断

如果把当前结果放回项目目标中，最重要的判断是：

> **你的主线方案已经被验证“有希望成立”：即使用离线阶段的多模型信息训练一个轻量 student 分类器，在在线阶段仅依赖 `[H_target, H_draft]`，仍然可以较好地判断哪些分歧 token 应该被接受。**

这意味着项目已经可以从“离线分类器可不可行”转向：

1. 更严格的数据划分验证；
2. 真正接入 decoding 流程，验证最终 accuracy / latency trade-off。

---

## 7. 下一步建议

### 7.1 必须做的下一步：严格 train/test 分离
当前 8 个实验都还是：

- 基于 `mined_full_test_qwen_dp`
- 在其内部进行分层切分得到 train/val

因此还不能作为最终泛化结论。

建议下一步立即做：

1. 挖掘 `MedQA train split` 得到 `mined_full_train_qwen_dp`；
2. 用 `mined_full_train_qwen_dp` 训练 teacher/student；
3. 用 `mined_full_test_qwen_dp` 做真正 hold-out evaluation。

### 7.2 最值得继续推进的线上模型
优先推进：

- `student_mlp`
- 蒸馏版
- 暂时 **不使用 `delta_p` 加权**

### 7.3 若继续做消融优化
建议按优先级尝试：

1. `teacher/student` 在 train mined 上训练、test mined 上评估；
2. 调整蒸馏超参：
   - `temperature`
   - `distill_alpha`
3. 再尝试更稳健的 `delta_p` 权重映射；
4. 最后将最佳 `student_mlp` 接入真实的有损投机解码流程。

---

## 8. 最终一句话总结

> 当前实验已经证明：**基于自动挖掘分歧点数据、使用 hidden states 训练 accept/reject 分类器** 这条路线是有效的；其中 **仅使用 `[H_target, H_draft]` 的 student MLP 蒸馏模型** 是当前最优选择，而 **`delta_p` 加权在当前版本下尚未体现收益**。下一步应转向 **严格 train/test 分离** 与 **真实 decoding 接入验证**。
