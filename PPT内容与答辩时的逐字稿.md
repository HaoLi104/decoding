基于投机解码框架的领域知识注入方法研究

硕士研究生开题汇报
汇报人： 李昊
日期： 2026年4月
学校： 东南大学 (Southeast University)

目录

研究背景及现状

研究目标

研究内容

目前进展

进度安排

1. 研究背景及现状

通用大模型的优势与局限（大模型不足）

通用大模型的现状：以 GPT、Qwen、LLaMA 为代表的通用大语言模型（LLMs）凭借大规模预训练获得强大的语言理解与跨任务推理能力，能完成多样化下游任务。

局限性：内部知识分布对具体垂直领域缺乏精细建模。在医学、法律等高专业性场景中，常表现为：领域事实错误 / 规范违背 / 幻觉问题。

模型微调的兴起：现有研究通常在领域数据上进行全参微调（FFT）或参数高效微调（LoRA）。

挑战：微调成本与模型规模成正比，直接微调大模型代价极高。

结论：通用大模型泛化能力强，但领域可靠性不足。

领域专家小模型与通用大模型的能力互补性

领域小模型的局限：规模限制了其复杂任务理解与推理能力；在面对开放式推理、多步逻辑时明显弱于大模型。

能力互补维度对比：

维度

通用大模型

领域小模型

复杂推理与任务理解

✅ 强

❌ 弱

语言生成流利度

✅ 强

❌ 弱

领域事实与规范一致性

❌ 不足

✅ 强

分布外泛化

✅ 强

❌ 弱

推理速度

❌ 慢

✅ 快

过程级协作框架：投机解码 (Speculative Decoding)

核心思想：大模型负责复杂推理与泛化，小模型负责领域事实与速度。

工作机制：Draft 模型快速生成候选 token $\rightarrow$ Target 模型一次并行验证 $\rightarrow$ 按接受概率规则决定接受或回退。

面临问题：

接受率塌陷：专业知识导致的分布偏移会让接受率变低，导致“负加速”。

性能上限：标准投机解码是无损的，能力上限受限于 Target 模型。

2. 研究目标

本研究旨在解决投机解码在垂直领域下的接受率塌陷与吞吐负加速问题，并探索推理增强路径。

研究目标一：基于对比概率差的领域知识注入与软引导投机解码。

研究目标二：基于投机解码框架的 Target 精准微调。

研究目标三：基于投机解码框架的知识注入飞轮系统。

3. 研究内容

研究内容一：基于对比概率差的领域知识注入与软引导投机解码

1. 模型架构设置

Target: 通用大模型（例：Qwen2.5-32B-base）

Draft: 领域专家小模型（例：MedicalQwen2.5-3B）

Base: Draft 对应的基座模型（例：Qwen2.5-3B-base）

注：三模型应同系列同词表。

2. 概率差 $\Delta P$ 的定义（领域知识探针）

$$\Delta P = \text{softmax}\left(\frac{\text{logit}_{\text{draft}}}{T_{\text{fixed}}}\right)[x] - \text{softmax}\left(\frac{\text{logit}_{\text{base}}}{T_{\text{fixed}}}\right)[x]$$

逻辑：$\Delta P$ 代表了微调注入的相对领域化强度。

3. 知识注入投机解码框架 (C3)

$$P'_{\text{accept}} = \min\left(1, \frac{P_{\text{target}}(x) + \alpha \cdot \Delta P(x)}{P_{\text{draft}}(x)}\right)$$

4. 注入强度 $\alpha$ 的优化策略

C4（基于 Draft 自信度的门控稀疏激活）：


$$\alpha_t = \alpha_{\text{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t, \quad S_t = P_{\text{expert}} - P_{\text{base}}$$

意义：实现“按需注入”，保护非领域位置的流利度。

C5（基于 Target 香农熵的门控稀疏激活）：


$$\alpha_t = \alpha_{\text{base}} \cdot \frac{H_t}{H_{\text{max}}}, \quad H_t = -\sum_{x} P_{\text{target}}(x) \log P_{\text{target}}(x)$$

逻辑：高熵代表 Target 处于知识盲区，需增强注入。

5. 已有实验结果

策略

关键设置

acc (准确率)

acc_rate (接受率)

pure_target

—

0.650

1.000

standard_sd

Draft-Surgery

0.650

0.212

C9 (C4+C5)

$\tau=0.05, \alpha_{\text{base}}=100.00$

0.690

0.768

研究内容二：投机解码-Target 精准微调（迭代闭环）

核心动机：投机解码不仅是加速工具，更是“自动数据标注器”。

Token 翻转事件 (Token Flip Event)：

定义：Target 原本想输出的 token 与最终写入（来自 Draft 且被 Target 验收通过）的 token 不一致。

解释：这类位置正是领域知识缺失的锚点，将概率从 A 推向 B 即为注入领域知识。

精准选层微调：

定义损失函数：$L(p_t, B) = -\log P_\theta(B | p_t)$

采用梯度 Frobenius 范数打分，选取 Top-K 层作为 LoRA 候选层，实现极小参数量的精准更新。

研究内容三：基于投机解码框架的知识注入飞轮系统

投机解码找到“token 翻转”事件。

收集数据，使用梯度打分选取 Top-K 层。

LoRA 微调 Target 模型的 Top-K 层。

将新 Target 模型替换进框架，循环迭代。

4. 目前进展

完成前期调研与基座模型就绪。

实现基于对比置信度的领域知识注入与软引导投机解码。

已实现 Token 翻转事件收集、FDLP 层位打分与 rank 预算分配算法。

5. 进度安排

2025.10 ~ 2026.01：前期调研与基座就绪

2026.01 ~ 2026.03：软引导投机解码研究

2026.03 ~ 2026.04：迭代闭环架构检验

2026.06 ~ 2026.10：领域迁移验证与消融实验

2026.11 ~ 2026.12：系统整合

2027.01 ~ 2027.04：毕业论文撰写与答辩

感谢！欢迎批评指正

答辩时的逐字稿：
开题报告讲稿
各位老师好，我是李昊，我的题目是基于投机解码框架的领域知识注入方法研究。
我将从以下几个方面进行讲解

现有的通用大模型普遍存在一个问题，虽然通用能力强，但在垂直领域内的表现不佳。
一个普遍的做法是微调大模型，但是微调成本往往和模型参数量成正比。其次微调数据的获取也是一个问题。
市面上可以找到很多别人微调好的领域专家小模型，他们具备自身的领域的知识和能力，但是受限于模型规模，其综合能力、cot能力不强。这里有一张表，直观的展现了领域专家小模型和通用大模型存在的能力互补情况。因此，我们就想能不能通过某种方式，直接把这种别人微调好的专业小模型中的领域知识，通过某种方式，注入到大模型中。取长补短，结合两者各自的长处，实现1+1大于2的效果。

两模型协作的框架很火的很自然的就会想到投机解码，这里简单讲一下投机解码，就是小模型快速生成n个token，大模型并行验证，实现加速，验证公式如下。
但是若直接应用投机解码框架会存在两个问题：
1.若把专家小模型直接设置为draft，垂直领域恰恰是传统 SD 最失效的地方，因为专业知识导致的分布偏移，会让投机解码的接受率变低，进而不能实现加速，
2.投机解码是一种无损的方式，其能力上限是target模型（大模型）。

基于这些问题，我们的研究目标是 期望能够找到一种面向不同专业领域的通用方法
，将知识领域注入。
我们的第一个研究目标是找到一种零训练的在推理阶段的方法，注入领域知识。第二个目标是一种如果要微调大模型，怎么只选取特定层精准微调，并在结构飞轮中迭代升级。


在研究内容一中，我们做的是一种基于对比概率差的软引导投机解码。还是用的之前讲的投机解码框架，target模型就是通用大模型，draft就是领域专家小模型。
先解释什么是概率差，这里有一个公式。我们引入了一个新的模型，我们这里称其为base模型，它是领域专家小模型对应的基座模型。一个核心思想就是，如果我们发现draft和base想要输出的token不一致了，那么这个token就是和领域知识相关的。因为两个模型唯一的不同就是经过了微调或者预训练的领域知识注入。所以当发生“想输出token不一致”这个事件时，就是一个强烈的领域知识信号和注入时机标识。
那么我们如何将找到的领域知识通过投机解码解码框架注入呢，我们这里运用的是这个公式。
同时这个注入强度alpha不应该是一个固定的，我们想找到一种更智能的方式，在这方面我们进行了一定的探索，并通过实验找到了基于draft端的门控、基于target香农熵、基于draft自身概率等有效的方法，动态调节注入强度。这里我们直接看实验结果，我们成功的让acc做题准确率上升，比任意当个模型的正确率都高，同时acc_rate接受率也很高，这完全是一个可工业级落地的优秀水平。

接下来讲内容二，我们在做第一个点的时候，已经发现在三模型框架的投机解码的链路中，会不断暴露出「Target 本来不会选、但领域专家 Draft 提议且被验收通过」的位置，这些位置天然携带「大模型里领域知识缺失」的信息。
研究内容二要做的就是，能否利用这些领域位置信息，尽可能小参数量的微调，把领域能力稳定写进 Target，让之后推理只靠 Target 也能拿到大部分增益。
我们首先看怎么找，我们在第一个点中会出现一个标志性的事件--token翻转，也就是本来target想输出A，但是draft想输出B，最后输出B，这个事情。我们收集这些位置，把对应的前缀输入给target模型，做一次对Btoken的loss计算，然后算梯度，哪些层的梯度大，就说明这些层对领域知识最敏感。我们选取top-k的层，只对这些层做微调，比如可以用lora的方法。然后做实验与全量微调、全层微调做对比，性能相近就说明我们的方法是有效的。
于此同时，我们经过精准微调后的大模型，可以替换原本的投机解码框架中的target模型，让准确率、接受率更高。然后可以投机解码-精准微调-再投机解码，不断循环形成一个飞轮，直至收敛或达到一个最优的性能。
这是我目前的进展和进度安排，第一个点已经做完并做了大量的实验，第二个点的代码已经写完，进行了数据收集和候选层的选取确定，也已经选层微调过了，正在做对比实验。