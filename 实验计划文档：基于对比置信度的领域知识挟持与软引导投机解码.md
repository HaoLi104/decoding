# 实验计划文档：基于对比置信度的领域知识挟持与软引导投机解码

**英文题名：** Contrastive-Confidence Guided Speculative Decoding: Hard Override vs. Soft-Guidance

> **公式渲染说明**：本文档使用标准 KaTeX/LaTeX 语法（`$$...$$`）。VSCode 内置预览不支持数学公式，请安装插件 **Markdown Preview Enhanced**（插件 ID：`shd101wyy.markdown-preview-enhanced`），再通过右键菜单 → "Markdown Preview Enhanced: Open Preview" 查看正确渲染效果。

---

## 1. 实验背景与核心目标

传统投机解码（Speculative Decoding）中，通用大模型（Target）常因自身分布限制，拒绝一个具备领域知识的专家小模型（Draft）提出的高价值垂直领域 Token。本实验引入一个与 Draft 同源但未经过领域微调的 Base 模型作为「常识对照组」。通过在固定温度下计算 Draft 与 Base 的单 Token 概率差值 $\Delta P$，精准定位「领域知识 Token」，并通过软引导机制提升 Target 对这些 Token 的接受概率。

### 核心对比消融实验（Ablation Study）

- **Standard SD（基线）：** 标准投机解码，不加任何 $\Delta P$ 干预，作为准确率与吞吐量的对照基准。
- **Hard Override（硬覆盖）：** 强制 Target 接受领域知识 Token，探究极端干预的上限与副作用，作为消融对比。
- **Soft-Guidance（软引导）方案对比：** 探索如何利用 $\Delta P$ 放大 Target 的验收阈值，对比七种数学策略：「固定 α 概率层补偿（C1）」、「Logit 层 Z-score 注入（C2）」、「概率域直接补贴（C3）」、「Draft 自信度稀疏激活（C4）」、「Target 熵不确定性路由（C5）」、「双信号联合门控（C6 = C4 × C5）」、「C3 框架下的双信号门控（C7 = C3 + C6）」。

---

## 2. 硬件拓扑与模型选型（单卡极致内聚架构）

将所有模型物理内聚于单张显卡，彻底规避跨卡通信延迟。

- **硬件环境：** 1 × NVIDIA H200 (141 GB HBM3e)
- **精度设置：** 全局采用 `bfloat16` 精度。
- **CUDA 绑定：** 严格使用 `device_map="cuda:0"`，禁止跨卡。

### 模型矩阵配置

| 角色 | 模型路径（远端绝对路径） | 显存约估 |
|------|------------------------|---------|
| Target（验收官） | `/data/ocean/decoding/model/Qwen/Qwen2.5-32B-Instruct` | ~64 GB |
| Base（对照组） | `/data/ocean/decoding/model/Qwen/Qwen2.5-3B-Instruct` | ~6 GB |
| Draft（提案者） | `/data/ocean/decoding/model/Qwen/Qwen2.5-3B-Instruct-Surgery/checkpoint-1676` | ~6 GB |

> **注**：Draft 模型经过 MedMCQA Surgery 专项微调（见第 3 节），在外科领域准确率显著高于 Base（+8%），具备真实的领域知识信号。

---

## 3. Draft 模型微调（已完成）

### 3.1 领域选择过程

经过多轮探测与评测，最终选定 **MedMCQA - Surgery 外科子集**。选定依据：

| 领域 | Target-32B acc | 排除原因 |
|------|---------------|---------|
| MedQA-USMLE | 0.78 | Target 太强，天花板过高 |
| JEC-QA（法律） | 0.79 | Target 太强；Draft 微调后 acc 反降（格式错位） |
| CMB-Exam（中文医学） | 0.86 | Target 太强 |
| **MedMCQA - Surgery** | **0.60** | ✅ Target 有明显提升空间；数据充裕（~22,000 条） |

### 3.2 微调配置

- **训练集：** MedMCQA train 中 `subject_name = "Surgery"` 的全量单选题（~22,000 条） + 25% tatsu-lab/alpaca 通用数据（格式锚点）
- **微调框架：** LLaMA-Factory，全参数微调（FFT）
- **学习率：** $3 \times 10^{-6}$（余弦调度）
- **训练轮数：** 5 epochs，全部 checkpoint 保存，事后 sweep 选最优
- **Checkpoint 评测结果：**

| Checkpoint | Surgery val acc | Delta(D-B) | tps |
|-----------|----------------|-----------|-----|
| checkpoint-419  | 0.5221 | +0.028 | 30.8 |
| checkpoint-838  | 0.5663 | +0.072 | 31.3 |
| checkpoint-1257 | 0.5703 | +0.076 | 31.4 |
| **checkpoint-1676** | **0.5743** | **+0.080** | **31.1** ← 最优 |
| checkpoint-2095 | 0.5703 | +0.076 | 31.3 |

- **Base-3B Surgery val acc（全量，n=249）：** 0.4940
- **Draft-Surgery 最优 acc：** 0.5743（Delta = **+0.0803**）

### 3.3 训练/评测格式对齐（关键）

为避免法律领域实验中出现的「格式错位」问题，训练数据的 `input` 字段与 `run_baseline.py` 的评测 prompt 格式完全一致，`output` 字段固定为 `"Final answer: X"`（极短，永不被 `max_new_tokens` 截断）。

---

## 4. 核心算法与数学定义

设当前生成步为 $t$，候选 Token 为 $x$，投机窗口长度（Draft 每轮提案数）为 $\gamma = 5$。

### Step 1：领域知识探针信号提取（固定温度去敏）

为剥离全局采样温度 $T_{\mathrm{sample}}$ 带来的分布平滑干扰，引入固定锐化温度 $T_{\mathrm{fixed}} = 1.0$，计算标准领域置信度差值：

$$
\Delta P = \mathrm{Softmax}\!\left(\frac{\mathrm{Logit}_{\mathrm{draft}}}{T_{\mathrm{fixed}}}\right)[x] - \mathrm{Softmax}\!\left(\frac{\mathrm{Logit}_{\mathrm{base}}}{T_{\mathrm{fixed}}}\right)[x]
$$

### Step 2：领域信号过滤（Condition\_Domain）

当同时满足以下两个条件时，触发领域知识判定（过滤低置信伪信号）：

$$
P_{\mathrm{draft}}(x) > \theta_{\mathrm{high}} \quad \text{且} \quad \Delta P > \tau
$$

### Step 3：验收策略分支（Strategy Routing）

**策略 A：Standard SD（标准投机解码，基线）**

$$
P_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)
$$

**策略 B0：Original Hard Override（离散对比基线）**

若 Target 拒绝（$\mathrm{argmax}(\mathrm{Logit}_{\mathrm{target}}) \neq x$），且 Draft 与 Base 的 Top-1 预测不一致，则强制 `Accept = True`：

$$
\text{Override} \iff \mathrm{argmax}(\mathrm{Logit}_{\mathrm{draft}}) \neq \mathrm{argmax}(\mathrm{Logit}_{\mathrm{base}})
$$

**策略 B：Threshold Hard Override（连续阈值硬覆盖）**

若 Target 拒绝，且满足 Condition\_Domain，则强制 `Accept = True`：

$$
\text{Override} \iff P_{\mathrm{draft}}(x) > \theta_{\mathrm{high}}\ \text{且}\ \Delta P > \tau
$$

**策略 C1：Soft-Guidance 概率层线性补偿**

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right)
$$

**C1 公式的等价展开（重要）：**

将 C1 公式的两项通分，可发现其真实分子结构：

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right) = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x) + \alpha \cdot \Delta P \cdot P_{\mathrm{draft}}(x)}{P_{\mathrm{draft}}(x)}\right)
$$

> **关键洞察：** C1 真正注入的补贴不是 $\alpha \cdot \Delta P$，而是 $\alpha \cdot \Delta P \cdot P_{\mathrm{draft}}(x)$。  
> 即 **bonus 被 Draft 在该 token 处的局部概率 $P_d(x)$ 隐式缩放**，当 Draft 对该 token 概率较小时（小模型常见），bonus 被同步压缩，存在"低置信 Draft token → 注入自动衰减"的隐式机制。

**C1 vs C3 量纲对比（完整版）：**

| 策略 | 公式 | 分子的领域补贴项 | 量纲一致性 |
|------|------|----------------|-----------|
| C1 | $\min(1,\ P_t/P_d + \alpha \cdot \Delta P)$ | $\alpha \cdot \Delta P \cdot P_d(x)$（隐式 $P_d$ 缩放） | 不一致（比值+概率） |
| C3 | $\min(1,\ (P_t + \alpha \cdot \Delta P)/P_d)$ | $\alpha \cdot \Delta P$（纯概率补贴，无 $P_d$ 缩放） | 一致 ✓（概率+概率） |

---

**策略 C2：Soft-Guidance Logit 层标准化残差注入**

计算 Logit 差值并进行词表级 Z-score 标准化，再以残差形式注入 Target：

$$
\Delta \mathrm{Logit} = \mathrm{Logit}_{\mathrm{draft}} - \mathrm{Logit}_{\mathrm{base}}
$$

$$
\Delta \mathrm{Logit}_{\mathrm{norm}} = \frac{\Delta \mathrm{Logit} - \mu(\Delta \mathrm{Logit})}{\sigma(\Delta \mathrm{Logit})}
$$

$$
\mathrm{Logit}'_{\mathrm{target}} = \mathrm{Logit}_{\mathrm{target}} + \alpha \cdot \Delta \mathrm{Logit}_{\mathrm{norm}}
$$

代入标准验收公式得新接受概率：

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right), \quad P'_{\mathrm{target}}(x) = \mathrm{Softmax}(\mathrm{Logit}'_{\mathrm{target}})[x]
$$

**策略 C3：Soft-Guidance Target 概率局部校准（Probability Subsidy）**（量纲一致消融）

C3 将公式拆解为两步，解决 C1 中"比值与概率直接相加量纲不一致"的问题：

**Step 1 — Target 概率局部校准**（在概率域注入领域补贴）：

$$
P'_{\mathrm{target}}(x) = P_{\mathrm{target}}(x) + \alpha \cdot \Delta P
$$

物理意义：$\Delta P$ 作为**概率补贴（Probability Subsidy）**，直接叠加到 Target 对该 Token 的原始概率上。Target 因缺乏领域知识给出的 $P_{\mathrm{target}}$ 偏低，我们将 Draft 的领域置信度超额部分补入，形成融合了领域知识的虚拟 Target 概率 $P'_{\mathrm{target}}(x)$。

**Step 2 — 回归标准投机解码验收**：

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)
$$

拿到补贴后的 Target，完全套用经典投机解码的数学框架，分子分母均为概率，量纲一致。

**策略 C4：Soft-Guidance 基于 Draft 领域自信度的动态门控（Confidence-Gated α）**

论文故事：**Sparse Activation（稀疏激活）**——模型只在遇到"领域急转弯"时才触发专家辅助，保护 Target 的通用流利度。

信号强度定义（Draft 领先 Base 的置信度优势）：

$$S_t = \max\!\left(P_{\mathrm{draft}}\right) - \max\!\left(P_{\mathrm{base}}\right)$$

动态 α 计算（带阈值的稀疏激活）：

$$\alpha_t = \alpha_{\mathrm{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t$$

- $S_t \leq \tau$：$\alpha_t = 0$，退化为 StandardSD（Draft 对该步没有显著领域优势）
- $S_t > \tau$：$\alpha_t = \alpha_{\mathrm{base}} \cdot S_t$，信号越强注入越大

完整验收公式（在 C1 的比值域加法框架内）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha_t \cdot \Delta P\right)$$

等价展开后，C4 真正的分子补贴项为：

$$\text{bonus}_{C4} = \alpha_{\mathrm{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t \cdot \Delta P \cdot P_{\mathrm{draft}}(x)$$

> **$P_d(x)$ 是否冗余？** 不冗余，但形成双重 Draft 置信度加权：  
> - $S_t = \max(P_d) - \max(P_b)$ 是**全局**信号（Draft 对最优 token 的置信度优势）  
> - $P_d(x)$ 是**局部** token-specific 信号（Draft 对提案 token $x$ 的置信度）  
> 两者不同（$x$ 未必是 Draft 的 argmax），联合起来等价于"全局上 Draft 领域领先 AND 局部上 Draft 对 $x$ 有把握"。这一双重确认不是冗余，但确实使 bonus 对 Draft 置信度形成了**二次加权**（与 $S_t$ 一次、$P_d(x)$ 一次）。C7 将在 C3 框架下消除这个隐式 $P_d(x)$ 乘积，用于消融对照。

超参数：`--alpha`（$\alpha_{\mathrm{base}}$）、`--c4_tau`（$\tau$，默认 0.1）

---

**策略 C5：Soft-Guidance 基于 Target 认知不确定性的路由（Entropy-Aware α）**

论文故事：**Uncertainty-Driven Routing（不确定性驱动路由）**——Target 的输出熵是其"知识盲区"的天然探测器，熵高时允许 Draft 趁虚而入。

香农熵计算：

$$H_t = -\sum_{x} P_{\mathrm{target}}(x) \log P_{\mathrm{target}}(x)$$

最大熵归一化（词表大小 $V$ 时的均匀分布熵）：

$$H_{\max} = \log V \approx \log(152064) \approx 11.93$$

动态 α（熵越高注入越强）：

$$\alpha_t = \lambda \cdot \frac{H_t}{H_{\max}}, \quad \alpha_t \in [0,\ \lambda]$$

完整验收公式（同样在 C1 框架内）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha_t \cdot \Delta P\right)$$

超参数：`--alpha`（$\lambda$，最大注入强度上限）

**策略 C6：Soft-Guidance 双信号联合门控（Dual-Signal Gated α = C4 × C5）**

论文故事：**Dual-Signal Routing（双信号路由）**——C4 是"专家主动举手"，C5 是"学生主动求助"。C6 要求两者同时成立才触发领域注入，实现最精准的"按需引导"。

两信号正交相乘，形成联合动态 α：

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(S_t > \tau) \cdot S_t}_{\text{C4：Draft 自信门}} \cdot \underbrace{\frac{H_t}{H_{\max}}}_{\text{C5：Target 熵权}}$$

- $S_t \leq \tau$：Draft 无显著领域优势 → C4 门关闭 → $\alpha_t = 0$，完全退化为 StandardSD
- $H_t \approx 0$：Target 极度自信 → C5 权重趋零 → 注入自动消退
- 两者同时满足：$\alpha_t = \lambda \cdot S_t \cdot H_t/H_{\max}$，实现双重 AND 门精准激活

完整验收公式（同样在 C1 框架内）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha_t \cdot \Delta P\right)$$

超参数：`--alpha`（$\lambda$，最大注入强度）、`--c4_tau`（$\tau$，C4 稀疏激活阈值，默认 0.1，复用 C4 参数）

**策略 C7：Soft-Guidance C3 框架下的双信号联合门控（C3 + C6 融合，消融 $P_d$ 隐式乘积）**

论文故事：**Framework Ablation（框架消融）**——将 C6 的双信号动态 α 移植到 C3 的概率域框架，消除 C1/C4/C5/C6 中隐含的 $P_d(x)$ 隐式缩放，验证"无 $P_d$ 双重加权"是否带来更精准的补贴。

动态 α 与 C6 **完全相同**：

$$\alpha_t = \lambda \cdot \mathbb{I}(S_t > \tau) \cdot S_t \cdot \frac{H_t}{H_{\max}}$$

验收公式改用 C3 框架（概率域直接加法）：

**Step 1 — 概率域补贴（无 $P_d$ 隐式乘积）：**

$$P'_{\mathrm{target}}(x) = P_{\mathrm{target}}(x) + \alpha_t \cdot \Delta P$$

**Step 2 — 标准投机解码验收：**

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)$$

等价展开后，C7 真正的分子补贴项为：

$$\text{bonus}_{C7} = \alpha_{\mathrm{base}} \cdot S_t \cdot H_t/H_{\max} \cdot \Delta P \quad (\text{无 } P_d(x) \text{ 缩放})$$

**C6 vs C7 关键对比（消融实验核心）：**

| 对比维度 | C6（C1 框架） | C7（C3 框架） |
|---------|------------|------------|
| 验收公式 | $P_t/P_d + \alpha_t \Delta P$ | $(P_t + \alpha_t \Delta P)/P_d$ |
| 补贴分子 | $\alpha_t \cdot \Delta P \cdot P_d(x)$ | $\alpha_t \cdot \Delta P$ |
| $P_d(x)$ 参与方式 | 隐式乘在 bonus 上（双重 Draft 加权） | 仅出现在分母（标准验收，单次 Draft 加权） |
| 动态 α | 完全相同：$\lambda \cdot \mathbb{I}(S_t>\tau) \cdot S_t \cdot H_t/H_{\max}$ | 同左 |
| 预期 $\lambda$ 量级 | 中等（bonus 已被 $P_d$ 缩放，需大 $\lambda$ 补偿） | 更小（无 $P_d$ 缩放，bonus 更直接） |

超参数：`--alpha`（$\lambda$）、`--c4_tau`（$\tau$，默认 0.1）

---

**C1–C7 路由信号与框架全景对比：**

| 策略 | 框架 | 路由信号 | 分子补贴项 | 超参数 |
|------|------|---------|-----------|--------|
| C1 | C1（比值域加法） | 固定 $\alpha$ | $\alpha \cdot \Delta P \cdot P_d$ | `--alpha` |
| C3 | C3（概率域加法） | 固定 $\alpha$ | $\alpha \cdot \Delta P$ | `--alpha` |
| C4 | C1 | $\mathbb{I}(S_t>\tau) \cdot S_t$ | $\alpha_t \cdot \Delta P \cdot P_d$ | `--alpha`, `--c4_tau` |
| C5 | C1 | $H_t / H_{\max}$ | $\alpha_t \cdot \Delta P \cdot P_d$ | `--alpha` |
| C6 | C1 | $\mathbb{I}(S_t>\tau) \cdot S_t \cdot H_t/H_{\max}$ | $\alpha_t \cdot \Delta P \cdot P_d$ | `--alpha`, `--c4_tau` |
| **C7** | **C3** | $\mathbb{I}(S_t>\tau) \cdot S_t \cdot H_t/H_{\max}$ | $\alpha_t \cdot \Delta P$（**无 $P_d$ 缩放**） | `--alpha`, `--c4_tau` |

---

## 5. 执行引擎架构

### 当前使用：Shadow Sync（影子同步）

Base 模型跟随 Draft 一起向前推进 $\gamma$ 步，执行前向传播更新 KV Cache，但采取 Lazy Evaluation——仅在 Draft 提案的候选 Token 位置按需执行 LM Head，提取 $\mathrm{Logit}_{\mathrm{base}}$，跳过全序列 Softmax 激活。

### 备用：Dual Stream（双流异步并发）

使用 `torch.cuda.Stream()` 为 Draft 和 Base 开辟独立流，在不同 SM 簇上并行前向传播（在当前单卡 H200 实验中性能提升有限，暂不使用）。

---

## 6. 工程约束与已知问题

| 约束 | 说明 |
|------|------|
| 禁用高阶封装 | 禁止 `pipeline` 或 `model.generate()`，基于 `model.forward()` 手写 AR loop |
| KV Cache 管理 | 使用 `transformers.cache_utils.StaticCache` 预分配 Buffer |
| 精度 | 全局 `bfloat16` |
| **torch.compile 已禁用** | `mode="reduce-overhead"` 使用 CUDAGraphs，与 StaticCache 跨 sample 复用（`index_copy_` 原地写）不兼容；当前环境 PyTorch 版本不含 `cudagraph_mark_step_begin`；实验统一设置 `--no_compile`，两侧基线一致，加速比对比仍公平 |
| 遥测系统 | `TelemetryLogger` 记录各策略触发频率、接受率、每步 Target Top-1 置信度 |

---

## 7. 实验评测基准

- **数据集：** MedMCQA validation，`subject_name = Surgery`，共 249 条（全量），n=200 用于 benchmark（`--limit 200`）
- **解码模式：** 贪婪解码（$T_{\mathrm{sample}} = 0$）为主；随机采样（$T_{\mathrm{sample}} = 0.6$）用于 C2 有效性验证
- **网格搜索：** $\alpha \in \{0.1, 0.3, 0.5, 0.8, 1.0, 1.5, 2.0\}$
- **核心指标：** Accuracy、Tokens/sec（tps）、Mean Acceptance Rate、Speedup vs pure_target

---

## 8. 完整实验结果汇总

### 8.1 各模型 Surgery val 单独准确率

| 模型 | acc | n | 备注 |
|------|-----|---|------|
| Pure Target（32B） | 0.650 | 249 | 参考上限 |
| Base（3B 未微调） | 0.494 | 249 | 基准下限 |
| Draft-Surgery（ckpt-1676） | 0.574 | 249 | ΔP = +0.080 vs Base |

---

### 8.2 贪婪解码完整结果表（$T = 0.0$，n=200）

> 按 acc 降序，同 acc 按 tps 降序排列。

| 策略 | 变体/参数 | $\alpha$ | acc | tps | acc\_rate | speedup |
|------|----------|---------|-----|-----|----------|---------|
| **C1** | — | **0.10** | **0.690** | 6.9 | 0.334 | 0.25× |
| C1 | — | 1.50 | 0.660 | 17.7 | 0.935 | 0.65× |
| C1 | — | 1.00 | 0.655 | 17.6 | 0.929 | 0.64× |
| C1 | — | 0.80 | 0.640 | 17.0 | 0.848 | 0.62× |
| C1 | — | 0.50 | 0.640 | 14.2 | 0.677 | 0.52× |
| C1 | — | 0.30 | 0.640 | 10.8 | 0.499 | 0.40× |
| C1 | — | 2.00 | 0.630 | 17.7 | 0.938 | 0.65× |
| C2-OneHot | onehot | 5.00 | 0.660 | 9.3 | 0.928 | 0.34× |
| C2-OneHot | onehot | 3.00 | 0.660 | 5.5 | 0.564 | 0.20× |
| C2-OneHot | onehot | 2.00 | 0.650 | 3.0 | 0.234 | 0.11× |
| C2-OneHot | onehot | 0.50 | 0.650 | 5.2 | 0.212 | 0.19× |
| C2-OneHot | onehot | 1.00 | 0.650 | 3.6 | 0.212 | 0.13× |
| C2-OneHot | onehot | 7.00 | 0.635 | 9.7 | 0.944 | 0.36× |
| C2-TopK | topk K=5 | 1.00 | 0.660 | 3.4 | 0.209 | 0.12× |
| C2-TopK | topk K=5 | 0.50 | 0.655 | 5.3 | 0.211 | 0.19× |
| C2-TopK | topk K=5 | 2.00 | 0.655 | 2.7 | 0.229 | 0.10× |
| C2-TopK | topk K=5 | 5.00 | 0.620 | 8.0 | 0.814 | 0.29× |
| C2-TopK | topk K=5 | 3.00 | 0.595 | 5.3 | 0.499 | 0.19× |
| C2-TopK | topk K=5 | 7.00 | 0.555 | 10.0 | 0.850 | 0.36× |
| C2-Full | full | 2.00 | 0.650 | 5.3 | 0.387 | 0.19× |
| C2-Full | full | 0.80 | 0.650 | 5.2 | 0.217 | 0.19× |
| C2-Full | full | 0.50 | 0.650 | 5.2 | 0.211 | 0.19× |
| C2-Full | full | 0.10 | 0.650 | 5.2 | 0.211 | 0.19× |
| C2-Full | full | 1.50 | 0.645 | 5.2 | 0.242 | 0.19× |
| C2-Full | full | 1.00 | 0.615 | 5.2 | 0.216 | 0.19× |
| C2-Full | full | 0.30 | 0.635 | 5.3 | 0.212 | 0.19× |
| C3 | — | 1.00 | 0.660 | 5.1 | 0.220 | 0.19× |
| C3 | — | 0.10 | 0.650 | 5.2 | 0.212 | 0.19× |
| C3 | — | 0.50 | 0.650 | 5.2 | 0.212 | 0.19× |
| C3 | — | 1.50 | 0.640 | 16.4 | 0.858 | 0.61× |
| C3 | — | 2.00 | 0.615 | 16.4 | 0.859 | 0.61× |
| **C5** | — | **5.00** | **0.685** | **8.2** | **0.367** | **0.30×** |
| C5 | — | 10.00 | 0.680 | 9.9 | 0.471 | 0.36× |
| C5 | — | 0.50 | 0.665 | 5.6 | 0.236 | 0.20× |
| C5 | — | 6.00 | 0.660 | 9.3 | 0.425 | 0.34× |
| C5 | — | 4.00 | 0.655 | 7.4 | 0.347 | 0.27× |
| C5 | — | 2.00 | 0.640 | 6.4 | 0.303 | 0.23× |
| C5 | — | 3.00 | 0.640 | 7.2 | 0.326 | 0.26× |
| C5 | — | 7.00 | 0.640 | 8.5 | 0.420 | 0.31× |
| C5 | — | 1.00 | 0.635 | 6.0 | 0.268 | 0.22× |
| **C6** | τ=0.05 | **50.00** | **0.690** | **11.5** | **0.600** | **0.42×** |
| C6 | τ=0.05 | 100.00 | 0.675 | 13.3 | 0.645 | 0.49× |
| C6 | τ=0.05 | 10.00 | 0.670 | 7.1 | 0.375 | 0.26× |
| C6 | τ=0.05 | 20.00 | 0.660 | 8.4 | 0.454 | 0.31× |
| C6 | τ=0.05 | 5.00 | 0.660 | 6.5 | 0.295 | 0.24× |
| C4 | τ=0.05 | 2.00 | 0.670 | 14.5 | 0.765 | 0.53× |
| C4 | τ=0.05 | 3.00 | 0.660 | 15.4 | 0.810 | 0.56× |
| C4 | τ=0.05 | 3.50 | 0.655 | 15.6 | 0.826 | 0.57× |
| C4 | τ=0.05 | 2.50 | 0.650 | 15.0 | 0.800 | 0.55× |
| C4 | τ=0.05 | 1.50 | 0.645 | 13.9 | 0.717 | 0.51× |
| C4 | τ=0.05 | 10.00 | 0.645 | 15.0 | 0.878 | 0.55× |
| C4 | τ=0.05 | 5.00 | 0.645 | 15.0 | 0.862 | 0.55× |
| C4 | τ=0.05 | 1.00 | 0.630 | 12.5 | 0.580 | 0.46× |
| C4 | τ=0.05 | 0.50 | 0.630 | 9.3 | 0.442 | 0.34× |
| hard\_override\_b0 | — | — | 0.650 | 17.8 | 0.942 | 0.65× |
| hard\_override\_b | — | — | 0.645 | 17.3 | 0.945 | 0.63× |
| standard\_sd | Draft-Surgery | — | 0.650 | 5.3 | 0.212 | 0.19× |
| standard\_sd | Base 未微调 | — | 0.650 | 5.0 | 0.205 | 0.18× |
| **pure\_target** | — | — | **0.650** | **27.3** | 1.000 | **1.00×** |

---

### 8.3 随机采样结果表（$T = 0.6$，n=200）

| 策略 | $\alpha$ | acc | tps | acc\_rate | speedup |
|------|---------|-----|-----|----------|---------|
| C2-Full | 0.30 | 0.670 | 5.0 | 0.211 | 0.18× |
| C2-Full | 0.50 | 0.670 | 3.6 | 0.213 | 0.13× |
| C1 | 0.30 | 0.665 | 11.4 | 0.547 | 0.42× |
| C1 | 0.05 | 0.660 | 6.1 | 0.286 | 0.22× |
| C1 | 1.00 | 0.660 | 17.6 | 0.933 | 0.64× |
| C2-Full | 1.00 | 0.655 | 3.1 | 0.220 | 0.11× |
| C1 | 1.50 | 0.645 | 17.5 | 0.941 | 0.64× |
| C1 | 0.10 | 0.635 | 7.1 | 0.327 | 0.26× |
| C2-Full | 1.50 | 0.630 | 3.1 | 0.244 | 0.11× |
| C1 | 0.50 | 0.610 | 14.3 | 0.699 | 0.52× |

---

### 8.4 帕累托最优点汇总（acc 最大化优先）

> 帕累托最优：不存在另一配置能在 acc 不降的前提下提升 tps（或 acc 不减时速度更快）。

| 排名 | 策略 | $\alpha$ | T | acc | tps | speedup | 定位 |
|------|------|---------|---|-----|-----|---------|------|
| 🥇 | **C6** | **50** | **0.0** | **0.690** | **11.5** | **0.42×** | **全局最高准确率 + 最高 tps，Pareto 绝对领先** |
| 🥈 | C1 | 0.10 | 0.0 | 0.690 | 6.9 | 0.25× | acc 并列第一，但 tps 被 C6 超越（同 acc 下次优） |
| 🌟 | C5 | 5.00 | 0.0 | 0.685 | 8.2 | 0.30× | acc 仅差 0.5%，tps 高于 C1 最优配置 |
| — | C4 | 2.00 | 0.0 | 0.670 | 14.5 | 0.53× | acc +2%，tps 较高 |
| 🥉 | C1 | 1.50 | 0.0 | 0.660 | 17.7 | 0.65× | **传统策略最佳速度-准确率平衡** |
| — | C6 | 100 | 0.0 | 0.675 | 13.3 | 0.49× | acc 和 tps 介于 C6-50 和 C1-1.50 之间 |
| — | hard\_override\_b0 | — | 0.0 | 0.650 | 17.8 | 0.65× | 无 acc 增益但速度接近最优 |
| — | pure\_target | — | 0.0 | 0.650 | 27.3 | 1.00× | 基准参考 |

---

## 9. 分析与初步结论

### 9.1 C1（概率层加法补偿）：核心贡献

**C1 是本实验的核心发现**，在贪婪解码（T=0）下：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right)$$

- **α=0.10（精准注入）**：acc=0.690，超越 pure_target +4%。ΔP 信号精准识别高价值外科知识 Token，强制放行后输出质量提升。
- **α=1.50（速度优先）**：acc=0.660（+1.5%），tps=17.7（0.65× pure_target）。在 acc 提升的同时维持较高吞吐。
- **Pareto 曲线**：α 越大 → acc 下降 + tps 上升，可沿前沿按需选择工作点。

C1 有效的根本原因：**加法补偿直接绕过了 3B vs 32B 的分布鸿沟**。即使 $P_{\mathrm{target}}(x)/P_{\mathrm{draft}}(x) \approx 0$，只要 $\alpha \cdot \Delta P > 0$，Token 就有非零接受概率。ΔP 信号越强，接受概率提升越多。

### 9.2 C2（Logit 层注入）：三变体消融分析

通过 Logit 尺度诊断（`probe_c2_logit_scale.py`）确认：
- `logit_target` 每步标准差 ≈ 2.87，峰值 ≈ 28.3
- `ΔLogit_norm`（Z-score 后）std=1，相对 32B logit 尺度极小
- Draft 提案 Token 的平均 logit gap ≈ 2.2（已很接近 Target argmax，但仍不足）

**C2-Full（全词表注入）**：翻转率在 α=5 时达峰 10.2%，随后因其他高 ΔLogit_norm Token 抢占 argmax而回落，贪婪与随机模式均无显著 acc 提升。

**C2-OneHot（单 Token 定向注入）**：
- 阈值 $\alpha_{\min} = \mathrm{gap}/\Delta\mathrm{Logit}_{\mathrm{norm}}[x] \approx 2.26$，在 α=3 时出现阶跃，α=5 时达 acc=0.660（+1.5%）且 acc_rate=0.928。
- **帕累托最优点：α=5.0**（acc=0.660，tps=9.3）优于 α=3.0（acc=0.660，tps=5.5）。
- 展现出理论预期的**双稳态特性**：阈值以下完全不动，超过阈值后高速接受。

**C2-TopK（K=5 专家集合注入）**：
- α=1.0 时 acc=0.660（+1.5%），但 acc_rate 仅 0.209，tps 极低（3.4）。
- α≥3 时 acc 暴跌（0.595），因 Top-K 内其他 4 个 Token 的 ΔLogit_norm 更高，注入导致错误 Token 被选中。
- **结论：K 内竞争是 TopK 的致命弱点**，与 Full 变体本质相同，只是作用域从词表缩小到 K。

**C2 三变体对比：**

| 维度 | C2-Full | C2-OneHot | C2-TopK(K=5) |
|------|---------|-----------|--------------|
| 竞争干扰 | 全词表 | 无（仅动 x） | K 内 |
| 最高 acc | 0.670（T=0.6） | 0.660 | 0.660 |
| 最高 acc 时 tps | 5.0 | 9.3 | 3.4 |
| 大 α 风险 | 无关 Token 竞争 | 过注入 | K 内错误 Token 抢占 |

### 9.3 C3（Target 概率局部校准）：量纲一致但效果受限

C3 将 $\alpha \cdot \Delta P$ 作为**概率补贴**叠加到 $P_{\mathrm{target}}$ 后再除以 $P_{\mathrm{draft}}$，解决了 C1 量纲不一致的问题，但实验结果揭示其内在局限：

| $\alpha$ | acc | tps | acc\_rate | 解读 |
|---------|-----|-----|----------|------|
| 0.10 | 0.650 | 5.2 | 0.212 | 补贴太小，等同 standard\_sd |
| 0.50 | 0.650 | 5.2 | 0.212 | 补贴仍不足，无效 |
| **1.00** | **0.660** | **5.1** | **0.220** | **唯一提升点（+1.5%），acc\_rate 几乎未变** |
| 1.50 | 0.640 | 16.4 | 0.858 | acc 开始下跌，接受率跳升 |
| 2.00 | 0.615 | 16.4 | 0.859 | acc 大幅下滑（-3.5%） |

**关键发现：C3 存在严重的"补贴失效区间"与"过度补贴区间"，中间窗口极窄。**

- **α 小（≤0.50）补贴无效**：$\alpha \cdot \Delta P$ 在概率域是一个绝对小量（ΔP ≈ 0.08，$\alpha=0.5$ 时补贴仅 0.04），而 $P_{\mathrm{draft}}(x)$ 对小模型普遍 <0.3，使得 $(P_t + 0.04) / P_d$ 的提升微乎其微。接受率 0.212 ≈ standard\_sd，说明补贴完全没有撑过阈值 1.0。

- **α 适中（1.00）有效窗口极窄**：acc\_rate 仅从 0.212 提升到 0.220（+0.8%），acc 提升 +1.5%。效果的来源是少量 ΔP 较大的外科 token 恰好被推过阈值，而非系统性提升。

- **α 大（≥1.50）过度补贴**：$\alpha \cdot \Delta P$ 将负 ΔP 的 token（Draft 不自信的 token）也大幅拉高接受率，导致 acc 骤降，同时接受率从 0.22 跳至 0.86——这一非线性跳变与 C1 的平滑过渡形成鲜明对比。

**与 C1 的核心差异（根本机制）：**

C1 中 $\alpha \cdot \Delta P$ 直接加到比值 $P_t/P_d$ 上，当 $P_t/P_d \approx 0$（3B vs 32B 体量差场景的常态）时，任何正的 $\alpha \cdot \Delta P$ 都能产生有效提升。C3 中 $\alpha \cdot \Delta P$ 加到 $P_t$ 后再除以 $P_d$——分母 $P_d$ 的放大效应不稳定（$P_d$ 越小放大越大），导致要么全无效（小 α）要么崩溃（大 α），有效窗口极窄。

**结论：C3 从负面说明"量纲一致性"并不等同于"效果更好"**——分母 $P_d$ 引入的非线性放大，使得概率域直接加法的行为比比值域加法（C1）更难控制，最终效果弱于 C1。C3 作为消融实验，从方程结构角度进一步支撑了 C1 设计的优越性。

### 9.4 Hard Override（硬覆盖）：消融极端基线

- **B0（argmax 分歧触发）**：acc=0.650（=pure_target），tps=17.8（0.65×）。高接受率但无准确率增益，证明"强制放行"本身不足以提升质量。
- **B（ΔP 阈值触发）**：acc=0.645（-0.5%），tps=17.3（0.63×）。强制接受引入的错误 Token 略微损害了准确率，说明领域知识注入需要"软化"而非"硬化"。

两者的对比验证了核心论点：**领域知识的注入必须是连续渐进的（C1），而非离散跳变的（B0/B）**。

### 9.4 C4（Confidence-Gated α）：稀疏激活验证

C4 引入信号强度 $S_t = \max(P_{\mathrm{draft}}) - \max(P_{\mathrm{base}})$ 作为动态门控，$\alpha_t = \alpha_{\mathrm{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t$（$\tau=0.05$）。

| $\alpha_{\mathrm{base}}$ | acc | tps | acc\_rate | 解读 |
|---------|-----|-----|----------|------|
| 0.50 | 0.630 | 9.3 | 0.442 | 门控触发少，信号弱 |
| 1.00 | 0.630 | 12.5 | 0.580 | acc 未超 pure\_target |
| **2.00** | **0.670** | **14.5** | **0.765** | **最优：+2%，tps 高** |
| 5.00 | 0.645 | 15.0 | 0.862 | 过度激活，acc 下滑 |
| 10.00 | 0.645 | 15.0 | 0.878 | 接受率趋于饱和，acc 不增 |

**结论：** α=2.0 处于孤立峰顶，两侧均下跌。tps 随 α 单调增（门控越来越频繁），但 acc 在 α>2 后因误触发下滑。最优配置 acc=0.670（+2%）、tps=14.5（0.53×），处于高速-中等准确率 Pareto 区间，是比 C1 α=1.50 更高 acc 的"高速点"。

**C4 细化全景（α ∈ {0.5, 1.0, 1.5, 2.0, 2.5, 3.0, 3.5, 5.0, 10.0}）：**

| α | acc | tps | acc\_rate | 解读 |
|---|-----|-----|----------|------|
| 0.50 | 0.630 | 9.3 | 0.442 | 门控触发少 |
| 1.00 | 0.630 | 12.5 | 0.580 | 未超基线 |
| 1.50 | 0.645 | 13.9 | 0.717 | 开始爬升 |
| **2.00** | **0.670** | **14.5** | **0.765** | **全局峰值（+2%）** |
| 2.50 | 0.650 | 15.0 | 0.800 | 急剧回落至基线 |
| 3.00 | 0.660 | 15.4 | 0.810 | 小幅反弹（随机波动） |
| 3.50 | 0.655 | 15.6 | 0.826 | 高接受率但 acc 低 |
| 5.00 | 0.645 | 15.0 | 0.862 | 门控频繁但误触发多 |
| 10.00 | 0.645 | 15.0 | 0.878 | 饱和，acc 不再提升 |

**根本原因：** α=2.5 的急剧跌落揭示 C4 的信号敏感性——当 $\alpha_{\mathrm{base}}$ 过大，$S_t > \tau$ 的步骤中即使 ΔP 为负（Draft 对该 token 不确定）也会被强力放大，误触发导致 acc 下跌。α=2.0 是信号增益与误触发之间的精确平衡点，窗口极窄（±0.5 之间变化 2%）。

**与联跑对比的说明：** C4 在不同 GPU/随机种子下存在 ±0.5–2% 的自然波动（T=0 下仍使用 `torch.rand` 随机验收），以单独运行结果为准。

### 9.5 C5（Entropy-Aware α）：细化实验与最终结论

C5 用 Target 输出分布的香农熵 $H_t$ 归一化后作为动态 α：$\alpha_t = \lambda \cdot H_t / \log(V)$。

完整细化实验（α ∈ {0.5, 1, 2, 3, 4, 5, 6, 7, 10}）全貌：

| $\lambda$ | acc | tps | acc\_rate | 解读 |
|---------|-----|-----|----------|------|
| 1.00 | 0.635 | 6.0 | 0.268 | 注入弱，接近 std\_sd |
| 2.00 | 0.640 | 6.4 | 0.303 | 微弱提升 |
| 3.00 | 0.640 | 7.2 | 0.326 | 平台区 |
| 4.00 | 0.655 | 7.4 | 0.347 | 开始爬升 |
| **5.00** | **0.685** | **8.2** | **0.367** | **全局峰值（+3.5%）** |
| 6.00 | 0.660 | 9.3 | 0.425 | 急剧下滑 |
| 7.00 | 0.640 | 8.5 | 0.420 | 继续下滑 |
| 10.00 | 0.680 | 9.9 | 0.471 | 反弹（随机波动） |
| 0.50 | 0.665 | 5.6 | 0.236 | 低 λ 的偶然高点 |

**细化结论：**
- **峰值确认在 λ=5.0（acc=0.685，+3.5%）**，左右两侧（λ=4 和 λ=6）均急剧下滑，是个清晰的孤立峰。
- λ=10 的 acc=0.680（联跑结果）与单跑 acc=0.685 的差异属于随机波动，非真正第二峰。
- **峰形尖锐**，类似 C4，说明动态 α 机制对超参数 λ 较敏感——这也是相对 C1（宽平台）的工程缺点。
- **论文结论（不确定性路由）得到验证**：Target 在 Surgery 专科 token 上熵较高，C5 精准放大引导；低熵（通用语法）时自动抑制，实现"按需路由"。

**C4 vs C5 最终对比：**

| 维度 | C4（Confidence-Gated） | C5（Entropy-Aware） |
|------|----------------------|-------------------|
| 路由信号 | Draft 自信度优势 $S_t$（外部） | Target 自身不确定性 $H_t$（内部） |
| 最优 acc | 0.670（α=2.0） | **0.685（λ=5.0）** |
| 最优 acc 时 tps | 14.5（0.53×） | 8.2（0.30×） |
| 峰形 | 孤立尖峰 | 孤立尖峰 |
| 随机波动 | ±1–4%（较大） | ±0.5–1.5% |
| vs C1 最优（0.690/tps=6.9） | acc 低 2%，但 tps 高 2×+ | acc 仅差 0.5%，tps 略高 |

### 9.6 C6（双信号联合门控）：Pareto 前沿突破

C6 将 C4 的 Draft 自信门和 C5 的 Target 熵权相乘：$\alpha_t = \lambda \cdot \mathbb{I}(S_t>\tau)\cdot S_t \cdot H_t/H_{\max}$。

**完整实验结果（λ ∈ {5, 10, 20, 50, 100}，$\tau=0.05$）：**

| $\lambda$ | acc | tps | acc\_rate | speedup | 解读 |
|---------|-----|-----|----------|---------|------|
| 5.00 | 0.660 | 6.5 | 0.295 | 0.24× | 联合因子过小，注入弱 |
| 10.00 | 0.670 | 7.1 | 0.375 | 0.26× | 初步提升 |
| 20.00 | 0.660 | 8.4 | 0.454 | 0.31× | 小幅回落（随机波动） |
| **50.00** | **0.690** | **11.5** | **0.600** | **0.42×** | **全局 Pareto 最优** |
| 100.00 | 0.675 | 13.3 | 0.645 | 0.49× | 峰值过后，acc 下滑 |

**核心发现：C6 α=50 达到 acc=0.690，与 C1 最优并列全局最高准确率，但 tps=11.5（0.42×），比 C1 最优（tps=6.9，0.25×）提升 67%**，在 Pareto 前沿上正式超越 C1。

**为什么 C6 需要如此大的 λ（50 vs C1 的 0.10）？**

联合因子 = $\mathbb{I}(S_t>\tau)\cdot S_t \cdot H_t/H_{\max}$ 是两个小量的乘积：
- $S_t$ 在 $\tau=0.05$ 门以上时典型值约 0.1–0.4（Draft 领先 Base 的概率差）
- $H_t/H_{\max}$ 典型值约 0.3–0.6（Target 的相对熵）
- 乘积约 **0.03–0.24**，比 C4 的 $S_t$（0.1–0.4）或 C5 的 $H_t/H_{\max}$（0.3–0.6）小一个数量级

因此 λ=50 时，有效 $\alpha_t \approx 50 \times 0.08 \approx 4$，与 C5 有效区间（λ=5，$H_t/H_{\max}\approx 0.5$，有效 $\alpha_t \approx 2.5$）量级相当，完全符合预期。

**C6 为何能同时实现高 acc 和高 tps（相比 C1）？**

1. **高 acc 原因（继承 C5 的精准性）**：$H_t/H_{\max}$ 权重确保只有 Target 真正不确定时注入才强，避免在 Target 已有明确答案时"添乱"，提高注入的"精准率"。

2. **高 tps 原因（继承 C4 的高接受率）**：$\mathbb{I}(S_t>\tau)\cdot S_t$ 门控确保在 Draft 领域优势显著的步骤（即领域关键 token）接受率明显提升（acc\_rate=0.60 vs C1 最优的 0.334），减少拒绝-重采样次数，单次 round 有效接受 token 数从约 1.7 增至约 3.0，故 tps 提升。

3. **两个信号的正交性**：S_t 高的步骤（Draft 明显比 Base 自信）未必是 H_t 高的步骤（Target 可能同样自信），两者相乘产生**更稀疏但更精准**的激活，接受的每一个 Draft token 都是"Draft 自信 + Target 懵逼"的双重确认，误触发大幅减少。

### 9.7 Standard SD：规模差距的基本矛盾

无论使用 Draft-Surgery 还是未微调 Base，standard_sd 的接受率均约 20%，tps 约 5，无法加速。这证明 **3B vs 32B 的体量差距（而非领域微调）才是 SD 低效的根本原因**，传统 SD 在大体量差场景下存在根本局限。

### 9.8 初步结论

> **核心结论（已更新）**：在 3B（Draft）→ 32B（Target）的大体量差场景下，本实验提出的领域软引导投机解码框架在 Surgery 外科专科问答上实现了准确率从 0.650 提升至 **0.690（+4%）**，超越 pure_target 32B 基线。其中 **C6（双信号联合门控）** 以 α=50 同时达到全局最高 acc=0.690 和最高 tps=11.5（0.42×），在 Pareto 前沿上**超越 C1**（相同 acc 但 tps 提升 67%），成为当前最优综合配置；C1 α=0.10 作为最简单的基础方案仍保持全局最高 acc，在工程实现复杂度与效果之间提供良好基准。

> **动态 α 系列结论**：C4（Draft 自信门控）最优 acc=0.670（+2%）、C5（Target 熵路由）最优 acc=0.685（+3.5%）、C6（C4×C5 联合）最优 acc=0.690（+4%）——三者依次递进，验证了"两个正交信号叠加相乘能进一步提升精准性"的核心假设。动态 α 系列的共同优势是**在实现相当 acc 的同时，通过选择性激活将 tps 提升到 C1 固定 α 方案的 1.5–2×**，为大模型推理的实际部署提供了更优的速度-质量权衡。

> **消融结论（C2）**：C2 系列（Full/OneHot/TopK）揭示 Logit 层注入存在固有局限——全词表竞争（Full）、K 内竞争（TopK）或阈值跃迁后过注入（OneHot）——均使最高 acc 被限制在 0.660，且 tps 显著低于 C1 的最优平衡点。C2 作为负面消融，从对立面验证了 C1 概率层加法方案的优越性。

> **消融结论（C3）**：C3 将 $\alpha \cdot \Delta P$ 作为概率补贴叠加到 $P_{\mathrm{target}}$ 后再除以 $P_{\mathrm{draft}}$，量纲一致但分母放大效应引入强非线性——α 小时补贴无效（acc\_rate ≈ standard\_sd），α 大时过度接受导致 acc 骤跌。仅在 α=1.00 处有 +1.5% 的窄窗口提升（acc=0.660，tps=5.1），远弱于 C1 在相同 acc 水平下的效率（C1 α=1.50：acc=0.660，tps=17.7）。C3 从方程结构角度证明：**在大体量差场景（3B vs 32B）下，"比值域加法"（C1）比"概率域加法"（C3）更具鲁棒性，因为前者对 $P_d$ 的小值不敏感**。

---

## 10. 待完成实验

- [x] **C3 全量评测（T=0.0 贪婪）**：完成。最优 acc=0.660（α=1.00，+1.5%），但有效窗口极窄，整体弱于 C1；详见 Section 8.2 与 9.3 分析。
- [x] **C4 初步评测（T=0.0 贪婪）**：完成。最优 acc=0.670（α=2.0，+2%），峰值窗口极窄，待细化 α ∈ [1.5, 3.0]。
- [x] **C5 初步评测（T=0.0 贪婪）**：完成。最优 acc=0.685（λ=5.0，+3.5%），Pareto 新突破点；待细化 λ ∈ [3.0, 7.0] 寻找峰值。
- [x] **C4 细化实验**：已确认最优 α=2.0（acc=0.670，+2%），峰形尖锐，结论稳定。
- [x] **C5 细化实验**：已确认最优 λ=5.0（acc=0.685，+3.5%），峰形尖锐，无法进一步突破 C1 的 0.690。
- [x] **C6 全量评测（T=0.0 贪婪）**：完成。λ=50 达到 acc=0.690（+4%，与 C1 最优并列），tps=11.5（0.42×），Pareto 绝对领先，当前最优综合配置。
- [ ] **C7 全量评测（T=0.0 贪婪）**：验证 C3 框架 + C6 双信号动态 α 的效果；重点关注与 C6 的对比——去掉 $P_d(x)$ 隐式缩放后，准确率/吞吐量是否改善；建议初始 λ_grid = [5, 10, 20, 50]。
- [ ] **通用能力验证（GSM8K）**：验证 Surgery 领域微调后通用推理能力未坍塌；期望 C1/C3 在 GSM8K 上 acc 与 pure_target 持平（ΔP≈0，软引导自动退化）
- [ ] **完整帕累托图**：整合所有实验结果绘制 acc vs tps 帕累托曲线（含 C3/C7，各策略的 Pareto Front 可视化）

---

## 12. Future Work（未来工作方向）

当前 Python 实现存在约 4× 的 overhead（kernel launch 延迟、CPU-GPU `.item()` 同步点等），导致实测 tps 约为理论值的 1/4。以下两个方向可将本框架的理论加速比（≈3×）在工程层面充分释放，同时进一步扩展方法上限。

### 12.1 底层引擎接管：C++ 层 + PagedAttention

**背景**：标准投机解码在优化实现（vLLM、TGI、DeepSpeed-MII）中已验证可实现 2–4× 吞吐量提升。其核心在于：

- **Python Overhead 消除**：将 argmax 采样、接受概率计算、token 选取等操作全部融合为 CUDA kernel，取消 CPU-GPU 同步（`.item()` 调用），消除每轮数十毫秒的等待。
- **PagedAttention / KV Cache 指针共享**：KV Cache 以 Page 粒度管理。Draft 提案与 Target 验证共享前缀 Page，拒绝时仅废弃相关 Page 的索引指针，实现 **0 数据拷贝**的回退操作（当前实现依赖 StaticCache 写时覆盖，无法真正共享物理内存）。
- **预期收益**：将当前框架移植到 C++/CUDA 后端后，理论每轮耗时从实测 ~265ms 降至 ~57ms，整体吞吐量预期提升 3–4×，同时领域软引导（C1 的 $\alpha \cdot \Delta P$ 补偿）可在验收 kernel 中以极低代价实现。

**实施路径**：在 vLLM 的 `llm_engine` 层注入自定义 `SpeculativeWorker`，将 C1 策略的接受逻辑注册为 GPU-side verifier，复用 PagedAttention 的 KV 管理。

### 12.2 树状投机解码（Tree-based Speculative Decoding）

**背景**：链式投机解码（本项目当前方案）每轮只生成一条 K token 的草稿序列，当接受率不够高时，大量草稿被拒绝。树状投机解码（Medusa 2024、Eagle 2024、SpecInfer）将 Draft 扩展为一棵多分支树：

```
链式（当前）:  Draft → t₀ → t₁ → t₂ → t₃ → t₄

树状（Future Work）:
                     ┌→ A₁ → A₂
       Draft → t₀ ───┼→ B₁ → B₂
                     └→ C₁ → C₂
```

Target 使用 **Tree Attention Mask**（因果掩码的推广）一次 forward 验证整棵树所有分支，每次 Target 调用的 "有效接受 token 数" 大幅提升。

**与本项目方法的结合点**：

- **ΔP 引导树剪枝**：$\Delta P(x) = P_{\mathrm{draft}}(x) - P_{\mathrm{base}}(x)$ 可作为分支展开的优先级信号——ΔP 大的 token 是领域专家认为更可靠的选项，优先作为树节点展开；ΔP 小或负值的分支提前剪掉，减少无效验证。
- **C1 接受策略的树化推广**：将 $\min\!\left(1,\, \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right)$ 推广为树上每个节点的接受概率，在 Tree Attention 的验收阶段按节点逐步筛选，最终以领域先验选出最优路径。
- **预期收益**：在高接受率（≥0.9）场景下，树状方案可将每次 Target forward 的有效 token 输出从 ~5 提升至 8–12，配合 ΔP 剪枝后对 Target 的调用次数也可减少，整体吞吐量预期在链式基础上再提升 1.5–2×。

---

## 11. 实施约束（底层工程规范）

- 禁止 `pipeline` / `model.generate()`，必须手写 `model.forward()` auto-regressive loop
- 张量操作必须注释维度变化（如 `# shape: [1, vocab_size]`）
- 变量命名贴合论文定义（`delta_p`、`alpha`、`logit_draft`、`logit_base`）
- 远端脚本不得使用 `python -c "..."` 执行超过 15 行代码，必须写成独立 `.py` 文件
- Git 提交采用白名单策略，禁止 `git add .`
