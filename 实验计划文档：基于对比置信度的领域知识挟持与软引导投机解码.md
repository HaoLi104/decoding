# 实验计划文档：基于对比置信度的领域知识挟持与软引导投机解码

**英文题名：** Contrastive-Confidence Guided Speculative Decoding: Hard Override vs. Soft-Guidance

> **公式渲染说明**：本文档使用标准 KaTeX/LaTeX 语法（`$$...$$`）。VSCode 内置预览不支持数学公式，请安装插件 **Markdown Preview Enhanced**（插件 ID：`shd101wyy.markdown-preview-enhanced`），再通过右键菜单 → "Markdown Preview Enhanced: Open Preview" 查看正确渲染效果。

---

## 1. 实验背景与核心目标

传统投机解码（Speculative Decoding）中，通用大模型（Target）常因自身分布限制，拒绝一个具备领域知识的专家小模型（Draft）提出的高价值垂直领域 Token。本实验引入一个与 Draft 同源但未经过领域微调的 Base 模型作为「常识对照组」。通过在固定温度下计算 Draft 与 Base 的单 Token 概率差值 $\Delta P$，精准定位「领域知识 Token」，并通过软引导机制提升 Target 对这些 Token 的接受概率。

### 核心对比消融实验（Ablation Study）

- **Standard SD（基线）：** 标准投机解码，不加任何 $\Delta P$ 干预，作为准确率与吞吐量的对照基准。
- **Hard Override（硬覆盖）：** 强制 Target 接受领域知识 Token，探究极端干预的上限与副作用，作为消融对比。
- **Soft-Guidance（软引导）方案对比：** 探索如何利用 $\Delta P$ 放大 Target 的验收阈值，对比七种数学策略：「固定 α 概率层补偿（C1）」、「Logit 层 Z-score 注入（C2）」、「概率域直接补贴（C3）」、「Draft 自信度稀疏激活（C4）」、「Target 熵不确定性路由（C5）」、「双信号联合门控（C6 = C4 × C5）」、「C3 框架下的双信号门控（C7 = C3 + C6）」。

---

## 2. 硬件拓扑与模型选型（单卡极致内聚架构）

将所有模型物理内聚于单张显卡，彻底规避跨卡通信延迟。

- **硬件环境：** 1 × NVIDIA H200 (141 GB HBM3e)
- **精度设置：** 全局采用 `bfloat16` 精度。
- **CUDA 绑定：** 严格使用 `device_map="cuda:0"`，禁止跨卡。

### 模型矩阵配置

| 角色 | 模型路径（远端绝对路径） | 显存约估 |
|------|------------------------|---------|
| Target（验收官） | `/data/ocean/decoding/model/Qwen/Qwen2.5-32B-Instruct` | ~64 GB |
| Base（对照组） | `/data/ocean/decoding/model/Qwen/Qwen2.5-3B-Instruct` | ~6 GB |
| Draft（提案者） | `/data/ocean/decoding/model/Qwen/Qwen2.5-3B-Instruct-Surgery/checkpoint-1676` | ~6 GB |

> **注**：Draft 模型经过 MedMCQA Surgery 专项微调（见第 3 节），在外科领域准确率显著高于 Base（+8%），具备真实的领域知识信号。

---

## 3. Draft 模型微调（已完成）

### 3.1 领域选择过程

经过多轮探测与评测，最终选定 **MedMCQA - Surgery 外科子集**。选定依据：

| 领域 | Target-32B acc | 排除原因 |
|------|---------------|---------|
| MedQA-USMLE | 0.78 | Target 太强，天花板过高 |
| JEC-QA（法律） | 0.79 | Target 太强；Draft 微调后 acc 反降（格式错位） |
| CMB-Exam（中文医学） | 0.86 | Target 太强 |
| **MedMCQA - Surgery** | **0.60** | ✅ Target 有明显提升空间；数据充裕（~22,000 条） |

### 3.2 微调配置

- **训练集：** MedMCQA train 中 `subject_name = "Surgery"` 的全量单选题（~22,000 条） + 25% tatsu-lab/alpaca 通用数据（格式锚点）
- **微调框架：** LLaMA-Factory，全参数微调（FFT）
- **学习率：** $3 \times 10^{-6}$（余弦调度）
- **训练轮数：** 5 epochs，全部 checkpoint 保存，事后 sweep 选最优
- **Checkpoint 评测结果：**

| Checkpoint | Surgery val acc | Delta(D-B) | tps |
|-----------|----------------|-----------|-----|
| checkpoint-419  | 0.5221 | +0.028 | 30.8 |
| checkpoint-838  | 0.5663 | +0.072 | 31.3 |
| checkpoint-1257 | 0.5703 | +0.076 | 31.4 |
| **checkpoint-1676** | **0.5743** | **+0.080** | **31.1** ← 最优 |
| checkpoint-2095 | 0.5703 | +0.076 | 31.3 |

- **Base-3B Surgery val acc（全量，n=249）：** 0.4940
- **Draft-Surgery 最优 acc：** 0.5743（Delta = **+0.0803**）

### 3.3 训练/评测格式对齐（关键）

为避免法律领域实验中出现的「格式错位」问题，训练数据的 `input` 字段与 `run_baseline.py` 的评测 prompt 格式完全一致，`output` 字段固定为 `"Final answer: X"`（极短，永不被 `max_new_tokens` 截断）。

---

## 4. 核心算法与数学定义

设当前生成步为 $t$，候选 Token 为 $x$，投机窗口长度（Draft 每轮提案数）为 $\gamma = 5$。

### Step 1：领域知识探针信号提取（固定温度去敏）

为剥离全局采样温度 $T_{\mathrm{sample}}$ 带来的分布平滑干扰，引入固定锐化温度 $T_{\mathrm{fixed}} = 1.0$，计算标准领域置信度差值：

$$
\Delta P = \mathrm{Softmax}\!\left(\frac{\mathrm{Logit}_{\mathrm{draft}}}{T_{\mathrm{fixed}}}\right)[x] - \mathrm{Softmax}\!\left(\frac{\mathrm{Logit}_{\mathrm{base}}}{T_{\mathrm{fixed}}}\right)[x]
$$

### Step 2：领域信号过滤（Condition\_Domain）

当同时满足以下两个条件时，触发领域知识判定（过滤低置信伪信号）：

$$
P_{\mathrm{draft}}(x) > \theta_{\mathrm{high}} \quad \text{且} \quad \Delta P > \tau
$$

### Step 3：验收策略分支（Strategy Routing）

**策略 A：Standard SD（标准投机解码，基线）**

$$
P_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)
$$

**策略 B0：Original Hard Override（离散对比基线）**

若 Target 拒绝（$\mathrm{argmax}(\mathrm{Logit}_{\mathrm{target}}) \neq x$），且 Draft 与 Base 的 Top-1 预测不一致，则强制 `Accept = True`：

$$
\text{Override} \iff \mathrm{argmax}(\mathrm{Logit}_{\mathrm{draft}}) \neq \mathrm{argmax}(\mathrm{Logit}_{\mathrm{base}})
$$

**策略 B：Threshold Hard Override（连续阈值硬覆盖）**

若 Target 拒绝，且满足 Condition\_Domain，则强制 `Accept = True`：

$$
\text{Override} \iff P_{\mathrm{draft}}(x) > \theta_{\mathrm{high}}\ \text{且}\ \Delta P > \tau
$$

**策略 C1：Soft-Guidance 概率层线性补偿**

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right)
$$

**C1 公式的等价展开（重要）：**

将 C1 公式的两项通分，可发现其真实分子结构：

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right) = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x) + \alpha \cdot \Delta P \cdot P_{\mathrm{draft}}(x)}{P_{\mathrm{draft}}(x)}\right)
$$

> **关键洞察：** C1 真正注入的补贴不是 $\alpha \cdot \Delta P$，而是 $\alpha \cdot \Delta P \cdot P_{\mathrm{draft}}(x)$。  
> 即 **bonus 被 Draft 在该 token 处的局部概率 $P_d(x)$ 隐式缩放**，当 Draft 对该 token 概率较小时（小模型常见），bonus 被同步压缩，存在"低置信 Draft token → 注入自动衰减"的隐式机制。

**C1 vs C3 量纲对比（完整版）：**

| 策略 | 公式 | 分子的领域补贴项 | 量纲一致性 |
|------|------|----------------|-----------|
| C1 | $\min(1,\ P_t/P_d + \alpha \cdot \Delta P)$ | $\alpha \cdot \Delta P \cdot P_d(x)$（隐式 $P_d$ 缩放） | 不一致（比值+概率） |
| C3 | $\min(1,\ (P_t + \alpha \cdot \Delta P)/P_d)$ | $\alpha \cdot \Delta P$（纯概率补贴，无 $P_d$ 缩放） | 一致 ✓（概率+概率） |

---

**策略 C2：Soft-Guidance Logit 层标准化残差注入**

计算 Logit 差值并进行词表级 Z-score 标准化，再以残差形式注入 Target：

$$
\Delta \mathrm{Logit} = \mathrm{Logit}_{\mathrm{draft}} - \mathrm{Logit}_{\mathrm{base}}
$$

$$
\Delta \mathrm{Logit}_{\mathrm{norm}} = \frac{\Delta \mathrm{Logit} - \mu(\Delta \mathrm{Logit})}{\sigma(\Delta \mathrm{Logit})}
$$

$$
\mathrm{Logit}'_{\mathrm{target}} = \mathrm{Logit}_{\mathrm{target}} + \alpha \cdot \Delta \mathrm{Logit}_{\mathrm{norm}}
$$

代入标准验收公式得新接受概率：

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right), \quad P'_{\mathrm{target}}(x) = \mathrm{Softmax}(\mathrm{Logit}'_{\mathrm{target}})[x]
$$

**策略 C3：Soft-Guidance Target 概率局部校准（Probability Subsidy）**（量纲一致消融）

C3 将公式拆解为两步，解决 C1 中"比值与概率直接相加量纲不一致"的问题：

**Step 1 — Target 概率局部校准**（在概率域注入领域补贴）：

$$
P'_{\mathrm{target}}(x) = P_{\mathrm{target}}(x) + \alpha \cdot \Delta P
$$

物理意义：$\Delta P$ 作为**概率补贴（Probability Subsidy）**，直接叠加到 Target 对该 Token 的原始概率上。Target 因缺乏领域知识给出的 $P_{\mathrm{target}}$ 偏低，我们将 Draft 的领域置信度超额部分补入，形成融合了领域知识的虚拟 Target 概率 $P'_{\mathrm{target}}(x)$。

**Step 2 — 回归标准投机解码验收**：

$$
P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)
$$

拿到补贴后的 Target，完全套用经典投机解码的数学框架，分子分母均为概率，量纲一致。

**策略 C4：Soft-Guidance 基于 Draft 领域自信度的动态门控（Confidence-Gated α）**

论文故事：**Sparse Activation（稀疏激活）**——模型只在遇到"领域急转弯"时才触发专家辅助，保护 Target 的通用流利度。

信号强度定义（Draft 领先 Base 的置信度优势）：

$$S_t = \max\!\left(P_{\mathrm{draft}}\right) - \max\!\left(P_{\mathrm{base}}\right)$$

动态 α 计算（带阈值的稀疏激活）：

$$\alpha_t = \alpha_{\mathrm{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t$$

- $S_t \leq \tau$：$\alpha_t = 0$，退化为 StandardSD（Draft 对该步没有显著领域优势）
- $S_t > \tau$：$\alpha_t = \alpha_{\mathrm{base}} \cdot S_t$，信号越强注入越大

完整验收公式（在 C1 的比值域加法框架内）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha_t \cdot \Delta P\right)$$

等价展开后，C4 真正的分子补贴项为：

$$\text{bonus}_{C4} = \alpha_{\mathrm{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t \cdot \Delta P \cdot P_{\mathrm{draft}}(x)$$

> **$P_d(x)$ 是否冗余？** 不冗余，但形成双重 Draft 置信度加权：  
> - $S_t = \max(P_d) - \max(P_b)$ 是**全局**信号（Draft 对最优 token 的置信度优势）  
> - $P_d(x)$ 是**局部** token-specific 信号（Draft 对提案 token $x$ 的置信度）  
> 两者不同（$x$ 未必是 Draft 的 argmax），联合起来等价于"全局上 Draft 领域领先 AND 局部上 Draft 对 $x$ 有把握"。这一双重确认不是冗余，但确实使 bonus 对 Draft 置信度形成了**二次加权**（与 $S_t$ 一次、$P_d(x)$ 一次）。C7 将在 C3 框架下消除这个隐式 $P_d(x)$ 乘积，用于消融对照。

超参数：`--alpha`（$\alpha_{\mathrm{base}}$）、`--c4_tau`（$\tau$，默认 0.1）

---

**策略 C5：Soft-Guidance 基于 Target 认知不确定性的路由（Entropy-Aware α）**

论文故事：**Uncertainty-Driven Routing（不确定性驱动路由）**——Target 的输出熵是其"知识盲区"的天然探测器，熵高时允许 Draft 趁虚而入。

香农熵计算：

$$H_t = -\sum_{x} P_{\mathrm{target}}(x) \log P_{\mathrm{target}}(x)$$

最大熵归一化（词表大小 $V$ 时的均匀分布熵）：

$$H_{\max} = \log V \approx \log(152064) \approx 11.93$$

动态 α（熵越高注入越强）：

$$\alpha_t = \lambda \cdot \frac{H_t}{H_{\max}}, \quad \alpha_t \in [0,\ \lambda]$$

完整验收公式（同样在 C1 框架内）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha_t \cdot \Delta P\right)$$

超参数：`--alpha`（$\lambda$，最大注入强度上限）

**策略 C6：Soft-Guidance 双信号联合门控（Dual-Signal Gated α = C4 × C5）**

论文故事：**Dual-Signal Routing（双信号路由）**——C4 是"专家主动举手"，C5 是"学生主动求助"。C6 要求两者同时成立才触发领域注入，实现最精准的"按需引导"。

两信号正交相乘，形成联合动态 α：

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(S_t > \tau) \cdot S_t}_{\text{C4：Draft 自信门}} \cdot \underbrace{\frac{H_t}{H_{\max}}}_{\text{C5：Target 熵权}}$$

- $S_t \leq \tau$：Draft 无显著领域优势 → C4 门关闭 → $\alpha_t = 0$，完全退化为 StandardSD
- $H_t \approx 0$：Target 极度自信 → C5 权重趋零 → 注入自动消退
- 两者同时满足：$\alpha_t = \lambda \cdot S_t \cdot H_t/H_{\max}$，实现双重 AND 门精准激活

完整验收公式（同样在 C1 框架内）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha_t \cdot \Delta P\right)$$

超参数：`--alpha`（$\lambda$，最大注入强度）、`--c4_tau`（$\tau$，C4 稀疏激活阈值，默认 0.1，复用 C4 参数）

**策略 C7：Soft-Guidance C3 框架下的双信号联合门控（C3 + C6 融合，消融 $P_d$ 隐式乘积）**

论文故事：**Framework Ablation（框架消融）**——将 C6 的双信号动态 α 移植到 C3 的概率域框架，消除 C1/C4/C5/C6 中隐含的 $P_d(x)$ 隐式缩放，验证"无 $P_d$ 双重加权"是否带来更精准的补贴。

动态 α 与 C6 **完全相同**：

$$\alpha_t = \lambda \cdot \mathbb{I}(S_t > \tau) \cdot S_t \cdot \frac{H_t}{H_{\max}}$$

验收公式改用 C3 框架（概率域直接加法）：

**Step 1 — 概率域补贴（无 $P_d$ 隐式乘积）：**

$$P'_{\mathrm{target}}(x) = P_{\mathrm{target}}(x) + \alpha_t \cdot \Delta P$$

**Step 2 — 标准投机解码验收：**

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P'_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)}\right)$$

等价展开后，C7 真正的分子补贴项为：

$$\text{bonus}_{C7} = \alpha_{\mathrm{base}} \cdot S_t \cdot H_t/H_{\max} \cdot \Delta P \quad (\text{无 } P_d(x) \text{ 缩放})$$

**C6 vs C7 关键对比（消融实验核心）：**

| 对比维度 | C6（C1 框架） | C7（C3 框架） |
|---------|------------|------------|
| 验收公式 | $P_t/P_d + \alpha_t \Delta P$ | $(P_t + \alpha_t \Delta P)/P_d$ |
| 补贴分子 | $\alpha_t \cdot \Delta P \cdot P_d(x)$ | $\alpha_t \cdot \Delta P$ |
| $P_d(x)$ 参与方式 | 隐式乘在 bonus 上（双重 Draft 加权） | 仅出现在分母（标准验收，单次 Draft 加权） |
| 动态 α | 完全相同：$\lambda \cdot \mathbb{I}(S_t>\tau) \cdot S_t \cdot H_t/H_{\max}$ | 同左 |
| 预期 $\lambda$ 量级 | 中等（bonus 已被 $P_d$ 缩放，需大 $\lambda$ 补偿） | 更小（无 $P_d$ 缩放，bonus 更直接） |

超参数：`--alpha`（$\lambda$）、`--c4_tau`（$\tau$，默认 0.1）

---

---

**策略 C8：token 级双信号门控（Token-Level Confidence Gate，消融全局 vs token 级门控）**

论文故事：**Token-Level Gate Ablation（门控粒度消融）**——C6 的门控信号 $S_t = \max(P_d) - \max(P_b)$ 是步级（step-level）全局信号，与被验收的 token $x$ 无直接绑定，可能在 Draft 对 token A 高置信时误触发 token B 的注入。C8 将门控粒度从步级降至 token 级，门控信号改为 $\Delta P(x) = P_d(x) - P_b(x)$，即与 bonus 使用完全相同的 token，消除该"跨 token 误触发"。

**门控信号的本质区别：**

| 信号 | 粒度 | 语义 | 问题 |
|------|------|------|------|
| $S_t = \max(P_d) - \max(P_b)$ | 步级（step-level） | Draft 整体上比 Base 更自信吗？ | Draft 在 token A 上高置信，但我们在验收 token B |
| $\Delta P(x) = P_d(x) - P_b(x)$ | token 级（token-level） | Draft 对这个具体 token $x$ 比 Base 更偏爱吗？ | 无——门控与验收决策直接对齐 |

动态 α（token 级门控 + C5 熵权）：

$$\alpha_t = \lambda \cdot \mathbb{I}(\Delta P(x) > \tau) \cdot \Delta P(x) \cdot \frac{H_t}{H_{\max}}$$

验收公式（C1 比值域加法框架，与 C6 相同）：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_t(x)}{P_d(x)} + \alpha_t \cdot \Delta P(x)\right)$$

等价展开后，C8 真正的分子补贴项（**$\Delta P$ 的平方自放大效应**）：

$$\text{bonus}_{C8} = \lambda \cdot \mathbb{I}(\Delta P(x)>\tau) \cdot (\Delta P(x))^2 \cdot \frac{H_t}{H_{\max}} \cdot P_d(x)$$

**C6 vs C8 关键对比（消融实验核心）：**

| 对比维度 | C6（步级门控） | C8（token 级门控） |
|---------|-------------|----------------|
| 门控信号 | $S_t = \max(P_d) - \max(P_b)$（与 $x$ 无关） | $\Delta P(x) = P_d(x) - P_b(x)$（与 $x$ 对齐） |
| 信号强度 | $S_t \cdot \Delta P(x)$（不同量的乘积） | $(\Delta P(x))^2$（同一量的平方自放大） |
| 误触发风险 | Draft 在 A 上高置信可误触发 B | 无，门控仅在 $x$ 本身有领域优势时激活 |
| 信号灵敏度 | 全局 $S_t$ 对任一 token 均有响应 | 仅对领域词（$\Delta P(x) > \tau$）响应，更稀疏 |
| 消融价值 | — | 若 C8 > C6：token 级更精准；若 C8 < C6：步级 $S_t$ 携带额外有用信息 |

超参数：`--alpha`（$\lambda$）、`--c4_tau`（$\tau$，此处作用于 $\Delta P(x)$，默认 0.05，比 C6 略低）

---

---

**策略 C10：Logit 域 Product of Experts（PoE）注入**

论文故事：**Product of Experts（专家乘积注入）**——最具理论基础的领域注入方式。在对数概率（logit）域做加法，等价于贝叶斯先验与领域似然比的乘积更新，天然保证输出分布合法。

注入公式（全词表向量操作）：

$$\text{logit}_{\text{steered}}(x) = \text{logit}_{\text{target}}(x) + \alpha \cdot \underbrace{(\text{logit}_{\text{draft}}(x) - \text{logit}_{\text{base}}(x))}_{\Delta\text{logit}(x) = \log(P_d/P_b)\text{：领域对数似然比}}$$

Softmax 后等价于 Product of Experts（Bayesian 乘积）：

$$P_{\text{steered}}(x) \propto P_{\text{target}}(x) \cdot \left(\frac{P_{\text{draft}}(x)}{P_{\text{base}}(x)}\right)^\alpha$$

验收公式（用 $P_{\text{steered}}$ 替代 $P_{\text{target}}$）：

$$P'_{\text{accept}} = \min\!\left(1,\ \frac{P_{\text{steered}}(x)}{P_{\text{draft}}(x)}\right)$$

**C10 与所有前序方法的关键区别：**

| 对比维度 | C1–C9（概率/比值域） | C10（logit 域） |
|---|---|---|
| 信号定义 | $\Delta P(x) = P_d(x) - P_b(x)$（概率差） | $\Delta\text{logit}(x) = \log(P_d(x)/P_b(x))$（对数似然比） |
| 理论基础 | 线性混合，无严格概率论对应 | Bayes 更新（Product of Experts），充分统计量 |
| 输出合法性 | 需手动处理归一化/负值 | Softmax 后天然归一化，无负概率 |
| 计算代价 | 标量操作（快） | 全词表向量加法（O(V)，略慢） |
| 拒绝时采样 | 从 $P_{\text{target}}$ 采样 | 从 $P_{\text{steered}}$ 采样（更一致） |

超参数：`--alpha`（$\alpha$，建议初始搜索 0.1, 0.3, 0.5, 1.0, 2.0，量纲与 C1 相近）

**C1–C10 路由信号与框架全景对比：**

| 策略 | 域 | 信号 | 接受公式等价分子 | 超参数 |
|------|------|---------|-----------|--------|
| C1 | 比值域 | 固定 $\alpha$ | $P_t + \alpha \cdot \Delta P \cdot P_d$ | `--alpha` |
| C3 | 概率域 | 固定 $\alpha$ | $P_t + \alpha \cdot \Delta P$ | `--alpha` |
| C4 | 比值域 | $\mathbb{I}(S_t>\tau) \cdot S_t$（步级） | $P_t + \alpha_t \cdot \Delta P \cdot P_d$ | `--alpha`, `--c4_tau` |
| C5 | 比值域 | $H_t / H_{\max}$ | $P_t + \alpha_t \cdot \Delta P \cdot P_d$ | `--alpha` |
| C6 | 比值域 | $\mathbb{I}(S_t>\tau) \cdot S_t \cdot H_t/H_{\max}$（步级） | $P_t + \alpha_t \cdot \Delta P \cdot P_d$ | `--alpha`, `--c4_tau` |
| C7 | 概率域 | $\mathbb{I}(S_t>\tau) \cdot S_t \cdot H_t/H_{\max}$（步级） | $P_t + \alpha_t \cdot \Delta P$ | `--alpha`, `--c4_tau` |
| C8 | 比值域 | $\mathbb{I}(\Delta P>\tau) \cdot \Delta P \cdot H_t/H_{\max}$（token 级） | $P_t + \alpha_t \cdot (\Delta P)^2 \cdot P_d$ | `--alpha`, `--c4_tau` |
| C9 | 比值域 | $\mathbb{I}(\Delta P>\tau) \cdot H_t/H_{\max}$（二值 token 级） | $P_t + \alpha_t \cdot \Delta P \cdot P_d$ | `--alpha`, `--c4_tau` |
| **C10** | **logit 域** | 固定 $\alpha$，全词表 | $P_t \cdot (P_d/P_b)^\alpha$（PoE 乘积） | `--alpha` |

---

## 5. 执行引擎架构

### 当前使用：Shadow Sync（影子同步）

Base 模型跟随 Draft 一起向前推进 $\gamma$ 步，执行前向传播更新 KV Cache，但采取 Lazy Evaluation——仅在 Draft 提案的候选 Token 位置按需执行 LM Head，提取 $\mathrm{Logit}_{\mathrm{base}}$，跳过全序列 Softmax 激活。

### 备用：Dual Stream（双流异步并发）

使用 `torch.cuda.Stream()` 为 Draft 和 Base 开辟独立流，在不同 SM 簇上并行前向传播（在当前单卡 H200 实验中性能提升有限，暂不使用）。

---

## 6. 工程约束与已知问题

| 约束 | 说明 |
|------|------|
| 禁用高阶封装 | 禁止 `pipeline` 或 `model.generate()`，基于 `model.forward()` 手写 AR loop |
| KV Cache 管理 | 使用 `transformers.cache_utils.StaticCache` 预分配 Buffer |
| 精度 | 全局 `bfloat16` |
| **torch.compile 已禁用** | `mode="reduce-overhead"` 使用 CUDAGraphs，与 StaticCache 跨 sample 复用（`index_copy_` 原地写）不兼容；当前环境 PyTorch 版本不含 `cudagraph_mark_step_begin`；实验统一设置 `--no_compile`，两侧基线一致，加速比对比仍公平 |
| 遥测系统 | `TelemetryLogger` 记录各策略触发频率、接受率、每步 Target Top-1 置信度 |

---

## 7. 实验评测基准

- **数据集：** MedMCQA validation，`subject_name = Surgery`，共 249 条（全量），n=200 用于 benchmark（`--limit 200`）
- **解码模式：** 贪婪解码（$T_{\mathrm{sample}} = 0$）为主；随机采样（$T_{\mathrm{sample}} = 0.6$）用于 C2 有效性验证
- **网格搜索：** $\alpha \in \{0.1, 0.3, 0.5, 0.8, 1.0, 1.5, 2.0\}$
- **核心指标：** Accuracy、Tokens/sec（tps）、Mean Acceptance Rate、Speedup vs pure_target

---

## 8. 完整实验结果汇总

### 8.1 各模型 Surgery val 单独准确率

| 模型 | acc | n | 备注 |
|------|-----|---|------|
| Pure Target（32B） | 0.650 | 249 | 参考上限 |
| Base（3B 未微调） | 0.494 | 249 | 基准下限 |
| Draft-Surgery（ckpt-1676） | 0.574 | 249 | ΔP = +0.080 vs Base |

---

### 8.2 贪婪解码完整结果表（$T = 0.0$，n=200）

> 按 acc 降序，同 acc 按 tps 降序排列。

| 策略 | 变体/参数 | $\alpha$ | acc | tps | acc\_rate | speedup |
|------|----------|---------|-----|-----|----------|---------|
| **C1** | — | **0.10** | **0.690** | 6.9 | 0.334 | 0.25× |
| C1 | — | 1.50 | 0.660 | 17.7 | 0.935 | 0.65× |
| C1 | — | 1.00 | 0.655 | 17.6 | 0.929 | 0.64× |
| C1 | — | 0.80 | 0.640 | 17.0 | 0.848 | 0.62× |
| C1 | — | 0.50 | 0.640 | 14.2 | 0.677 | 0.52× |
| C1 | — | 0.30 | 0.640 | 10.8 | 0.499 | 0.40× |
| C1 | — | 2.00 | 0.630 | 17.7 | 0.938 | 0.65× |
| C2-OneHot | onehot | 5.00 | 0.660 | 9.3 | 0.928 | 0.34× |
| C2-OneHot | onehot | 3.00 | 0.660 | 5.5 | 0.564 | 0.20× |
| C2-OneHot | onehot | 2.00 | 0.650 | 3.0 | 0.234 | 0.11× |
| C2-OneHot | onehot | 0.50 | 0.650 | 5.2 | 0.212 | 0.19× |
| C2-OneHot | onehot | 1.00 | 0.650 | 3.6 | 0.212 | 0.13× |
| C2-OneHot | onehot | 7.00 | 0.635 | 9.7 | 0.944 | 0.36× |
| C2-TopK | topk K=5 | 1.00 | 0.660 | 3.4 | 0.209 | 0.12× |
| C2-TopK | topk K=5 | 0.50 | 0.655 | 5.3 | 0.211 | 0.19× |
| C2-TopK | topk K=5 | 2.00 | 0.655 | 2.7 | 0.229 | 0.10× |
| C2-TopK | topk K=5 | 5.00 | 0.620 | 8.0 | 0.814 | 0.29× |
| C2-TopK | topk K=5 | 3.00 | 0.595 | 5.3 | 0.499 | 0.19× |
| C2-TopK | topk K=5 | 7.00 | 0.555 | 10.0 | 0.850 | 0.36× |
| C2-Full | full | 2.00 | 0.650 | 5.3 | 0.387 | 0.19× |
| C2-Full | full | 0.80 | 0.650 | 5.2 | 0.217 | 0.19× |
| C2-Full | full | 0.50 | 0.650 | 5.2 | 0.211 | 0.19× |
| C2-Full | full | 0.10 | 0.650 | 5.2 | 0.211 | 0.19× |
| C2-Full | full | 1.50 | 0.645 | 5.2 | 0.242 | 0.19× |
| C2-Full | full | 1.00 | 0.615 | 5.2 | 0.216 | 0.19× |
| C2-Full | full | 0.30 | 0.635 | 5.3 | 0.212 | 0.19× |
| C3 | — | 1.00 | 0.660 | 5.1 | 0.220 | 0.19× |
| C3 | — | 0.10 | 0.650 | 5.2 | 0.212 | 0.19× |
| C3 | — | 0.50 | 0.650 | 5.2 | 0.212 | 0.19× |
| C3 | — | 1.50 | 0.640 | 16.4 | 0.858 | 0.61× |
| C3 | — | 2.00 | 0.615 | 16.4 | 0.859 | 0.61× |
| **C5** | — | **5.00** | **0.685** | **8.2** | **0.367** | **0.30×** |
| C5 | — | 10.00 | 0.680 | 9.9 | 0.471 | 0.36× |
| C5 | — | 0.50 | 0.665 | 5.6 | 0.236 | 0.20× |
| C5 | — | 6.00 | 0.660 | 9.3 | 0.425 | 0.34× |
| C5 | — | 4.00 | 0.655 | 7.4 | 0.347 | 0.27× |
| C5 | — | 2.00 | 0.640 | 6.4 | 0.303 | 0.23× |
| C5 | — | 3.00 | 0.640 | 7.2 | 0.326 | 0.26× |
| C5 | — | 7.00 | 0.640 | 8.5 | 0.420 | 0.31× |
| C5 | — | 1.00 | 0.635 | 6.0 | 0.268 | 0.22× |
| **C6** | τ=0.05 | **50.00** | **0.690** | **11.5** | **0.600** | **0.42×** |
| C6 | τ=0.05 | 100.00 | 0.675 | 13.3 | 0.645 | 0.49× |
| C6 | τ=0.05 | 10.00 | 0.670 | 7.1 | 0.375 | 0.26× |
| C6 | τ=0.05 | 20.00 | 0.660 | 8.4 | 0.454 | 0.31× |
| C6 | τ=0.05 | 5.00 | 0.660 | 6.5 | 0.295 | 0.24× |
| C4 | τ=0.05 | 2.00 | 0.670 | 14.5 | 0.765 | 0.53× |
| C4 | τ=0.05 | 3.00 | 0.660 | 15.4 | 0.810 | 0.56× |
| C4 | τ=0.05 | 3.50 | 0.655 | 15.6 | 0.826 | 0.57× |
| C4 | τ=0.05 | 2.50 | 0.650 | 15.0 | 0.800 | 0.55× |
| C4 | τ=0.05 | 1.50 | 0.645 | 13.9 | 0.717 | 0.51× |
| C4 | τ=0.05 | 10.00 | 0.645 | 15.0 | 0.878 | 0.55× |
| C4 | τ=0.05 | 5.00 | 0.645 | 15.0 | 0.862 | 0.55× |
| C4 | τ=0.05 | 1.00 | 0.630 | 12.5 | 0.580 | 0.46× |
| C4 | τ=0.05 | 0.50 | 0.630 | 9.3 | 0.442 | 0.34× |
| hard\_override\_b0 | — | — | 0.650 | 17.8 | 0.942 | 0.65× |
| hard\_override\_b | — | — | 0.645 | 17.3 | 0.945 | 0.63× |
| standard\_sd | Draft-Surgery | — | 0.650 | 5.3 | 0.212 | 0.19× |
| standard\_sd | Base 未微调 | — | 0.650 | 5.0 | 0.205 | 0.18× |
| **pure\_target** | — | — | **0.650** | **27.3** | 1.000 | **1.00×** |

---

### 8.3 随机采样结果表（$T = 0.6$，n=200）

| 策略 | $\alpha$ | acc | tps | acc\_rate | speedup |
|------|---------|-----|-----|----------|---------|
| C2-Full | 0.30 | 0.670 | 5.0 | 0.211 | 0.18× |
| C2-Full | 0.50 | 0.670 | 3.6 | 0.213 | 0.13× |
| C1 | 0.30 | 0.665 | 11.4 | 0.547 | 0.42× |
| C1 | 0.05 | 0.660 | 6.1 | 0.286 | 0.22× |
| C1 | 1.00 | 0.660 | 17.6 | 0.933 | 0.64× |
| C2-Full | 1.00 | 0.655 | 3.1 | 0.220 | 0.11× |
| C1 | 1.50 | 0.645 | 17.5 | 0.941 | 0.64× |
| C1 | 0.10 | 0.635 | 7.1 | 0.327 | 0.26× |
| C2-Full | 1.50 | 0.630 | 3.1 | 0.244 | 0.11× |
| C1 | 0.50 | 0.610 | 14.3 | 0.699 | 0.52× |

---

### 8.4 帕累托最优点汇总（acc 最大化优先）

> 帕累托最优：不存在另一配置能在 acc 不降的前提下提升 tps（或 acc 不减时速度更快）。

| 排名 | 策略 | $\alpha$ | T | acc | tps | speedup | 定位 |
|------|------|---------|---|-----|-----|---------|------|
| 🥇 | **C6** | **50** | **0.0** | **0.690** | **11.5** | **0.42×** | **全局最高准确率 + 最高 tps，Pareto 绝对领先** |
| 🥈 | C1 | 0.10 | 0.0 | 0.690 | 6.9 | 0.25× | acc 并列第一，但 tps 被 C6 超越（同 acc 下次优） |
| 🌟 | C5 | 5.00 | 0.0 | 0.685 | 8.2 | 0.30× | acc 仅差 0.5%，tps 高于 C1 最优配置 |
| — | C4 | 2.00 | 0.0 | 0.670 | 14.5 | 0.53× | acc +2%，tps 较高 |
| 🥉 | C1 | 1.50 | 0.0 | 0.660 | 17.7 | 0.65× | **传统策略最佳速度-准确率平衡** |
| — | C6 | 100 | 0.0 | 0.675 | 13.3 | 0.49× | acc 和 tps 介于 C6-50 和 C1-1.50 之间 |
| — | hard\_override\_b0 | — | 0.0 | 0.650 | 17.8 | 0.65× | 无 acc 增益但速度接近最优 |
| — | pure\_target | — | 0.0 | 0.650 | 27.3 | 1.00× | 基准参考 |

---

## 9. 分析与初步结论

### 9.1 C1（概率层加法补偿）：核心贡献

**C1 是本实验的核心发现**，在贪婪解码（T=0）下：

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right)$$

- **α=0.10（精准注入）**：acc=0.690，超越 pure_target +4%。ΔP 信号精准识别高价值外科知识 Token，强制放行后输出质量提升。
- **α=1.50（速度优先）**：acc=0.660（+1.5%），tps=17.7（0.65× pure_target）。在 acc 提升的同时维持较高吞吐。
- **Pareto 曲线**：α 越大 → acc 下降 + tps 上升，可沿前沿按需选择工作点。

C1 有效的根本原因：**加法补偿直接绕过了 3B vs 32B 的分布鸿沟**。即使 $P_{\mathrm{target}}(x)/P_{\mathrm{draft}}(x) \approx 0$，只要 $\alpha \cdot \Delta P > 0$，Token 就有非零接受概率。ΔP 信号越强，接受概率提升越多。

### 9.2 C2（Logit 层注入）：三变体消融分析

通过 Logit 尺度诊断（`probe_c2_logit_scale.py`）确认：
- `logit_target` 每步标准差 ≈ 2.87，峰值 ≈ 28.3
- `ΔLogit_norm`（Z-score 后）std=1，相对 32B logit 尺度极小
- Draft 提案 Token 的平均 logit gap ≈ 2.2（已很接近 Target argmax，但仍不足）

**C2-Full（全词表注入）**：翻转率在 α=5 时达峰 10.2%，随后因其他高 ΔLogit_norm Token 抢占 argmax而回落，贪婪与随机模式均无显著 acc 提升。

**C2-OneHot（单 Token 定向注入）**：
- 阈值 $\alpha_{\min} = \mathrm{gap}/\Delta\mathrm{Logit}_{\mathrm{norm}}[x] \approx 2.26$，在 α=3 时出现阶跃，α=5 时达 acc=0.660（+1.5%）且 acc_rate=0.928。
- **帕累托最优点：α=5.0**（acc=0.660，tps=9.3）优于 α=3.0（acc=0.660，tps=5.5）。
- 展现出理论预期的**双稳态特性**：阈值以下完全不动，超过阈值后高速接受。

**C2-TopK（K=5 专家集合注入）**：
- α=1.0 时 acc=0.660（+1.5%），但 acc_rate 仅 0.209，tps 极低（3.4）。
- α≥3 时 acc 暴跌（0.595），因 Top-K 内其他 4 个 Token 的 ΔLogit_norm 更高，注入导致错误 Token 被选中。
- **结论：K 内竞争是 TopK 的致命弱点**，与 Full 变体本质相同，只是作用域从词表缩小到 K。

**C2 三变体对比：**

| 维度 | C2-Full | C2-OneHot | C2-TopK(K=5) |
|------|---------|-----------|--------------|
| 竞争干扰 | 全词表 | 无（仅动 x） | K 内 |
| 最高 acc | 0.670（T=0.6） | 0.660 | 0.660 |
| 最高 acc 时 tps | 5.0 | 9.3 | 3.4 |
| 大 α 风险 | 无关 Token 竞争 | 过注入 | K 内错误 Token 抢占 |

### 9.3 C3（Target 概率局部校准）：量纲一致但效果受限

C3 将 $\alpha \cdot \Delta P$ 作为**概率补贴**叠加到 $P_{\mathrm{target}}$ 后再除以 $P_{\mathrm{draft}}$，解决了 C1 量纲不一致的问题，但实验结果揭示其内在局限：

| $\alpha$ | acc | tps | acc\_rate | 解读 |
|---------|-----|-----|----------|------|
| 0.10 | 0.650 | 5.2 | 0.212 | 补贴太小，等同 standard\_sd |
| 0.50 | 0.650 | 5.2 | 0.212 | 补贴仍不足，无效 |
| **1.00** | **0.660** | **5.1** | **0.220** | **唯一提升点（+1.5%），acc\_rate 几乎未变** |
| 1.50 | 0.640 | 16.4 | 0.858 | acc 开始下跌，接受率跳升 |
| 2.00 | 0.615 | 16.4 | 0.859 | acc 大幅下滑（-3.5%） |

**关键发现：C3 存在严重的"补贴失效区间"与"过度补贴区间"，中间窗口极窄。**

- **α 小（≤0.50）补贴无效**：$\alpha \cdot \Delta P$ 在概率域是一个绝对小量（ΔP ≈ 0.08，$\alpha=0.5$ 时补贴仅 0.04），而 $P_{\mathrm{draft}}(x)$ 对小模型普遍 <0.3，使得 $(P_t + 0.04) / P_d$ 的提升微乎其微。接受率 0.212 ≈ standard\_sd，说明补贴完全没有撑过阈值 1.0。

- **α 适中（1.00）有效窗口极窄**：acc\_rate 仅从 0.212 提升到 0.220（+0.8%），acc 提升 +1.5%。效果的来源是少量 ΔP 较大的外科 token 恰好被推过阈值，而非系统性提升。

- **α 大（≥1.50）过度补贴**：$\alpha \cdot \Delta P$ 将负 ΔP 的 token（Draft 不自信的 token）也大幅拉高接受率，导致 acc 骤降，同时接受率从 0.22 跳至 0.86——这一非线性跳变与 C1 的平滑过渡形成鲜明对比。

**与 C1 的核心差异（根本机制）：**

C1 中 $\alpha \cdot \Delta P$ 直接加到比值 $P_t/P_d$ 上，当 $P_t/P_d \approx 0$（3B vs 32B 体量差场景的常态）时，任何正的 $\alpha \cdot \Delta P$ 都能产生有效提升。C3 中 $\alpha \cdot \Delta P$ 加到 $P_t$ 后再除以 $P_d$——分母 $P_d$ 的放大效应不稳定（$P_d$ 越小放大越大），导致要么全无效（小 α）要么崩溃（大 α），有效窗口极窄。

**结论：C3 从负面说明"量纲一致性"并不等同于"效果更好"**——分母 $P_d$ 引入的非线性放大，使得概率域直接加法的行为比比值域加法（C1）更难控制，最终效果弱于 C1。C3 作为消融实验，从方程结构角度进一步支撑了 C1 设计的优越性。

### 9.4 Hard Override（硬覆盖）：消融极端基线

- **B0（argmax 分歧触发）**：acc=0.650（=pure_target），tps=17.8（0.65×）。高接受率但无准确率增益，证明"强制放行"本身不足以提升质量。
- **B（ΔP 阈值触发）**：acc=0.645（-0.5%），tps=17.3（0.63×）。强制接受引入的错误 Token 略微损害了准确率，说明领域知识注入需要"软化"而非"硬化"。

两者的对比验证了核心论点：**领域知识的注入必须是连续渐进的（C1），而非离散跳变的（B0/B）**。

### 9.4 C4（Confidence-Gated α）：稀疏激活验证

C4 引入信号强度 $S_t = \max(P_{\mathrm{draft}}) - \max(P_{\mathrm{base}})$ 作为动态门控，$\alpha_t = \alpha_{\mathrm{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t$（$\tau=0.05$）。

| $\alpha_{\mathrm{base}}$ | acc | tps | acc\_rate | 解读 |
|---------|-----|-----|----------|------|
| 0.50 | 0.630 | 9.3 | 0.442 | 门控触发少，信号弱 |
| 1.00 | 0.630 | 12.5 | 0.580 | acc 未超 pure\_target |
| **2.00** | **0.670** | **14.5** | **0.765** | **最优：+2%，tps 高** |
| 5.00 | 0.645 | 15.0 | 0.862 | 过度激活，acc 下滑 |
| 10.00 | 0.645 | 15.0 | 0.878 | 接受率趋于饱和，acc 不增 |

**结论：** α=2.0 处于孤立峰顶，两侧均下跌。tps 随 α 单调增（门控越来越频繁），但 acc 在 α>2 后因误触发下滑。最优配置 acc=0.670（+2%）、tps=14.5（0.53×），处于高速-中等准确率 Pareto 区间，是比 C1 α=1.50 更高 acc 的"高速点"。

**C4 细化全景（α ∈ {0.5, 1.0, 1.5, 2.0, 2.5, 3.0, 3.5, 5.0, 10.0}）：**

| α | acc | tps | acc\_rate | 解读 |
|---|-----|-----|----------|------|
| 0.50 | 0.630 | 9.3 | 0.442 | 门控触发少 |
| 1.00 | 0.630 | 12.5 | 0.580 | 未超基线 |
| 1.50 | 0.645 | 13.9 | 0.717 | 开始爬升 |
| **2.00** | **0.670** | **14.5** | **0.765** | **全局峰值（+2%）** |
| 2.50 | 0.650 | 15.0 | 0.800 | 急剧回落至基线 |
| 3.00 | 0.660 | 15.4 | 0.810 | 小幅反弹（随机波动） |
| 3.50 | 0.655 | 15.6 | 0.826 | 高接受率但 acc 低 |
| 5.00 | 0.645 | 15.0 | 0.862 | 门控频繁但误触发多 |
| 10.00 | 0.645 | 15.0 | 0.878 | 饱和，acc 不再提升 |

**根本原因：** α=2.5 的急剧跌落揭示 C4 的信号敏感性——当 $\alpha_{\mathrm{base}}$ 过大，$S_t > \tau$ 的步骤中即使 ΔP 为负（Draft 对该 token 不确定）也会被强力放大，误触发导致 acc 下跌。α=2.0 是信号增益与误触发之间的精确平衡点，窗口极窄（±0.5 之间变化 2%）。

**与联跑对比的说明：** C4 在不同 GPU/随机种子下存在 ±0.5–2% 的自然波动（T=0 下仍使用 `torch.rand` 随机验收），以单独运行结果为准。

### 9.5 C5（Entropy-Aware α）：细化实验与最终结论

C5 用 Target 输出分布的香农熵 $H_t$ 归一化后作为动态 α：$\alpha_t = \lambda \cdot H_t / \log(V)$。

完整细化实验（α ∈ {0.5, 1, 2, 3, 4, 5, 6, 7, 10}）全貌：

| $\lambda$ | acc | tps | acc\_rate | 解读 |
|---------|-----|-----|----------|------|
| 1.00 | 0.635 | 6.0 | 0.268 | 注入弱，接近 std\_sd |
| 2.00 | 0.640 | 6.4 | 0.303 | 微弱提升 |
| 3.00 | 0.640 | 7.2 | 0.326 | 平台区 |
| 4.00 | 0.655 | 7.4 | 0.347 | 开始爬升 |
| **5.00** | **0.685** | **8.2** | **0.367** | **全局峰值（+3.5%）** |
| 6.00 | 0.660 | 9.3 | 0.425 | 急剧下滑 |
| 7.00 | 0.640 | 8.5 | 0.420 | 继续下滑 |
| 10.00 | 0.680 | 9.9 | 0.471 | 反弹（随机波动） |
| 0.50 | 0.665 | 5.6 | 0.236 | 低 λ 的偶然高点 |

**细化结论：**
- **峰值确认在 λ=5.0（acc=0.685，+3.5%）**，左右两侧（λ=4 和 λ=6）均急剧下滑，是个清晰的孤立峰。
- λ=10 的 acc=0.680（联跑结果）与单跑 acc=0.685 的差异属于随机波动，非真正第二峰。
- **峰形尖锐**，类似 C4，说明动态 α 机制对超参数 λ 较敏感——这也是相对 C1（宽平台）的工程缺点。
- **论文结论（不确定性路由）得到验证**：Target 在 Surgery 专科 token 上熵较高，C5 精准放大引导；低熵（通用语法）时自动抑制，实现"按需路由"。

**C4 vs C5 最终对比：**

| 维度 | C4（Confidence-Gated） | C5（Entropy-Aware） |
|------|----------------------|-------------------|
| 路由信号 | Draft 自信度优势 $S_t$（外部） | Target 自身不确定性 $H_t$（内部） |
| 最优 acc | 0.670（α=2.0） | **0.685（λ=5.0）** |
| 最优 acc 时 tps | 14.5（0.53×） | 8.2（0.30×） |
| 峰形 | 孤立尖峰 | 孤立尖峰 |
| 随机波动 | ±1–4%（较大） | ±0.5–1.5% |
| vs C1 最优（0.690/tps=6.9） | acc 低 2%，但 tps 高 2×+ | acc 仅差 0.5%，tps 略高 |

### 9.6 C6（双信号联合门控）：Pareto 前沿突破

C6 将 C4 的 Draft 自信门和 C5 的 Target 熵权相乘：$\alpha_t = \lambda \cdot \mathbb{I}(S_t>\tau)\cdot S_t \cdot H_t/H_{\max}$。

**完整实验结果（λ ∈ {5, 10, 20, 50, 100}，$\tau=0.05$）：**

| $\lambda$ | acc | tps | acc\_rate | speedup | 解读 |
|---------|-----|-----|----------|---------|------|
| 5.00 | 0.660 | 6.5 | 0.295 | 0.24× | 联合因子过小，注入弱 |
| 10.00 | 0.670 | 7.1 | 0.375 | 0.26× | 初步提升 |
| 20.00 | 0.660 | 8.4 | 0.454 | 0.31× | 小幅回落（随机波动） |
| **50.00** | **0.690** | **11.5** | **0.600** | **0.42×** | **全局 Pareto 最优** |
| 100.00 | 0.675 | 13.3 | 0.645 | 0.49× | 峰值过后，acc 下滑 |

**核心发现：C6 α=50 达到 acc=0.690，与 C1 最优并列全局最高准确率，但 tps=11.5（0.42×），比 C1 最优（tps=6.9，0.25×）提升 67%**，在 Pareto 前沿上正式超越 C1。

**为什么 C6 需要如此大的 λ（50 vs C1 的 0.10）？**

联合因子 = $\mathbb{I}(S_t>\tau)\cdot S_t \cdot H_t/H_{\max}$ 是两个小量的乘积：
- $S_t$ 在 $\tau=0.05$ 门以上时典型值约 0.1–0.4（Draft 领先 Base 的概率差）
- $H_t/H_{\max}$ 典型值约 0.3–0.6（Target 的相对熵）
- 乘积约 **0.03–0.24**，比 C4 的 $S_t$（0.1–0.4）或 C5 的 $H_t/H_{\max}$（0.3–0.6）小一个数量级

因此 λ=50 时，有效 $\alpha_t \approx 50 \times 0.08 \approx 4$，与 C5 有效区间（λ=5，$H_t/H_{\max}\approx 0.5$，有效 $\alpha_t \approx 2.5$）量级相当，完全符合预期。

**C6 为何能同时实现高 acc 和高 tps（相比 C1）？**

1. **高 acc 原因（继承 C5 的精准性）**：$H_t/H_{\max}$ 权重确保只有 Target 真正不确定时注入才强，避免在 Target 已有明确答案时"添乱"，提高注入的"精准率"。

2. **高 tps 原因（继承 C4 的高接受率）**：$\mathbb{I}(S_t>\tau)\cdot S_t$ 门控确保在 Draft 领域优势显著的步骤（即领域关键 token）接受率明显提升（acc\_rate=0.60 vs C1 最优的 0.334），减少拒绝-重采样次数，单次 round 有效接受 token 数从约 1.7 增至约 3.0，故 tps 提升。

3. **两个信号的正交性**：S_t 高的步骤（Draft 明显比 Base 自信）未必是 H_t 高的步骤（Target 可能同样自信），两者相乘产生**更稀疏但更精准**的激活，接受的每一个 Draft token 都是"Draft 自信 + Target 懵逼"的双重确认，误触发大幅减少。

### 9.7 C7（C3 框架 + C6 双信号动态 α，消融 $P_d$ 隐式乘积）

**实验目的**：C6 的 bonus 项实际上隐含了一次 $P_d(x)$ 乘积（因 C1 框架本质是在"比值域"做加法，等价于 $P_t/P_d + \alpha_t \cdot \Delta P$，而 $\Delta P = P_d(x) - P_b(x)$，bonus 项中已含 $P_d$）。C7 将 C6 的动态 $\alpha_t$ 搬入 C3 的概率域直接加法框架，去掉这个隐式缩放：

$$P'_{\text{accept}} = \min\!\left(1, \frac{P_t(x) + \alpha_t \cdot \Delta P}{P_d(x)}\right), \quad \alpha_t = \lambda \cdot \mathbb{I}(S_t>\tau)\cdot S_t \cdot \frac{H_t}{H_{\max}}$$

这是一次"干净的消融"：唯一变量是 bonus 是否带 $P_d$ 的隐式缩放，其他（双信号门控、动态 α 公式）完全一致。

**实验结果（$\lambda \in \{5, 10, 20, 50\}$，$\tau=0.05$，T=0.0 贪婪，Surgery n=200）：**

| $\lambda$ | acc | tps | acc\_rate | speedup | vs pure\_target |
|----------|-----|-----|----------|---------|----------------|
| 5.00 | 0.650 | 5.0 | 0.212 | 0.18× | +0.0 pt |
| 10.00 | **0.660** | 5.3 | 0.221 | 0.19× | +1.0 pt |
| 20.00 | **0.660** | 5.7 | 0.271 | 0.21× | +1.0 pt |
| 50.00 | 0.655 | 8.2 | 0.413 | 0.30× | +0.5 pt |
| pure_target | 0.650 | 27.4 | 1.000 | 1.00× | baseline |

**C7 vs C6 核心对比：**

| 指标 | C6 最优（λ=50） | C7 最优（λ=10/20） | C7 差距 |
|------|-----------------|-------------------|---------|
| 最高 acc | **0.690** | 0.660 | **−3.0 pt** |
| 最高 tps | **11.5** | 5.7 | **−5.8** |
| 最高接受率 | **0.600** | 0.271 | **−0.329** |
| 最高 speedup | **0.42×** | 0.21× | 差距 2× |

**C7 明显弱于 C6 的原因分析：**

C7 使用了 C3 框架（概率域直接加法），而 C3 本身在大体量差（3B vs 32B）场景下就存在已知的固有弱点——在 § 9.8 的消融结论中已指出：

1. **分母放大效应**：$P'_{\text{accept}} = (P_t + \alpha_t \cdot \Delta P) / P_d$，分母 $P_d$ 的值通常很小（3B 对某词的概率往往远低于 32B），导致接受概率变化对 $\alpha_t$ 极度敏感——小 λ 几乎无效（acc\_rate ≈ standard\_sd 的 0.21），稍大则过度接受。C6 中 $\alpha_t \cdot \Delta P \cdot P_d$ 的 $P_d$ 缩放反而在这里起到了**自动稳压器**的作用：$P_d$ 小时，bonus 自动缩小，避免爆炸。

2. **C3 框架隐式去掉了一个有益的约束**：C7 删除了 $P_d(x)$ 隐式缩放，初衷是为了"公平比较"，但这个 $P_d$ 在 C6 中其实并非完全冗余——它让 Draft 实际把握不住的 token（$P_d$ 很小）自动得到更低的 bonus，是一种天然的二阶门控。去掉后，$\alpha_t \cdot \Delta P$ 直接加到 $P_t$ 上，缺少这层保护，反而引入了更多噪音注入。

3. **有效 α 量级差异**：C7 的 bonus = $\alpha_t \cdot \Delta P$（在分子加），而 C6 的 bonus = $\alpha_t \cdot \Delta P \cdot P_d$（在比值上加）。前者实际量级比后者大得多（$P_d$ 是个小量），因此 C7 用同样的 λ 范围扫描，已经进入了过注入区，acc 没有继续上升的空间。

**是否需要继续扫描更多 α（λ）寻找 C7 真正最优点？**

**建议不作为主实验进一步扫描**，理由有三：

1. **C3 框架的固有弱点已充分论证**：§ 9.8 中 C3（固定 α 版本）的消融已证明，在大体量差场景下 C3 框架在 α 小时无效、α 大时骤跌，有效窗口极窄。C7 作为 C3 + C6 的融合，复现了同样的规律（α=5 无效、α=50 开始回落），说明问题来自框架本身而非 λ 选择。

2. **C7 的理论贡献已经实现**：C7 的价值是作为**消融实验对照**——"去掉 $P_d$ 隐式乘积后效果如何"，实验结果清楚地告诉我们：**$P_d$ 的隐式缩放在 C6 中不是冗余，而是有益的**，这正是我们要的消融结论。

3. **优先级让位于主实验**：当前最高价值的工作是通用能力验证（GSM8K）和 Pareto 图绘制，C7 的消融意义已经明确，不需要继续耗费 GPU 资源寻找其"峰值"。

**消融结论（论文可引用）：** C7 vs C6 的对比证明，C1 框架中 $P_d(x)$ 的隐式缩放并非冗余——它在概率域为 Draft 置信度提供了一层天然的"二阶门控"，去掉后（C3 框架）接受率和准确率均显著下滑。这从对立面验证了 **C6（C1 框架 + 双信号动态 α）是当前最优选择**。

### 9.8 C8（token 级双信号门控，消融全局 $S_t$ vs token 级 $\Delta P(x)$ 门控）

**实验目的**：C6 的门控信号 $S_t = \max(P_d) - \max(P_b)$ 是步级全局信号，两个 max 可能对应**不同 token**，存在"Draft 在 token A 上高置信却在验收 token B 时误触发"的风险。C8 将门控改为 token 级 $\Delta P(x)$，与 bonus 使用完全相同的 token，回答同一个问题："Draft 对这个具体 token $x$ 是否比 Base 更偏爱？"

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(\Delta P(x) > \tau) \cdot \Delta P(x)}_{\text{token 级门控}} \cdot \frac{H_t}{H_{\max}}$$

等价 bonus 含 $(\Delta P)^2$ 的**平方自放大效应**：$\text{bonus} \propto (\Delta P(x))^2 \cdot H_t/H_{\max} \cdot P_d(x)$

**实验结果（$\lambda \in \{5, 10, 20, 50, 100\}$，$\tau=0.05$，T=0.0 贪婪，Surgery n=200）：**

| $\lambda$ | acc | tps | acc\_rate | speedup | vs pure\_target |
|----------|-----|-----|----------|---------|----------------|
| 5.00 | 0.645 | 7.7 | 0.376 | 0.28× | +0.0 pt |
| 10.00 | 0.690 | 10.4 | 0.486 | 0.38× | +4.0 pt |
| **20.00** | **0.700** | **12.2** | **0.591** | **0.44×** | **+5.0 pt ★** |
| 50.00 | 0.685 | 14.8 | 0.682 | 0.54× | +3.5 pt |
| 100.00 | 0.665 | 15.7 | 0.738 | 0.57× | +1.5 pt |
| pure_target | 0.650 | 27.5 | 1.000 | 1.00× | baseline |

**C8 vs C6 核心对比（消融结论）：**

| 指标 | C6 最优（λ=50） | C8 最优（λ=20） | C8 提升 |
|------|-----------------|-----------------|---------|
| 最高 acc | 0.690 | **0.700** | **+1.0 pt ★** |
| 对应 tps | 11.5 | **12.2** | **+0.7** |
| 对应 acc\_rate | 0.600 | **0.591** | 相当 |
| 最高 speedup | 0.49× | 0.57× | C8 更高（但 acc 已下滑） |

**C8 明显优于 C6，原因深度分析：**

1. **token 级门控消除了误触发噪音**：C6 的步级 $S_t$ 可能在 Draft 对某个领域词高置信时，也对同步骤里其他 token（如连接词、标点）施加额外增强——这些位置 $\Delta P(x)$ 未必大。C8 的门控 $\mathbb{I}(\Delta P(x)>\tau)$ 只在被验收 token 本身有明确领域优势时才激活，**将误触发率压缩到最低**。

2. **$(\Delta P)^2$ 平方自放大的精准性**：C8 的 bonus $\propto (\Delta P(x))^2$，是一个**超线性响应函数**：当 $\Delta P(x)$ 很小时（通用词），bonus 几乎为零（$\Delta P^2 \approx 0$）；当 $\Delta P(x)$ 较大时（真实领域词），bonus 二次放大——比 C6 的线性 $S_t \cdot \Delta P(x)$ 更能将增强能量集中在真正的领域词上。

3. **门控粒度与验收决策的天然对齐**：C8 回答的问题（"Draft 对 $x$ 更偏爱吗？"）与验收决策的问题（"是否接受 $x$？"）完全一致，而 C6 的 $S_t$ 回答的是步级问题（"这一步 Draft 总体上更专业吗？"），存在语义错位。

4. **最优 $\lambda$ 量级更小（20 vs 50）**：平方项 $(\Delta P)^2$ 已提供了额外的增强幅度，因此需要更小的 $\lambda$ 即可达到最优注入强度，超参数更鲁棒、搜索空间更小。

**C8 在 Pareto 前沿上的新地位：**

- **acc=0.700，tps=12.2**：全局最高准确率（超越 C6 的 0.690 和 C1 的 0.690），同时维持了与 C6 相当的吞吐量
- **消融结论**：token 级门控（C8）优于步级门控（C6），证明"门控信号与验收 token 对齐"是设计最优门控机制的关键原则；步级 $S_t$ 的误触发带来了额外噪音，即使它能捕捉"即将到来的领域词"的前兆信息，也不足以抵消误触发的代价

**待完成**：C8 在 Pharmacology、Anatomy 等非外科领域的泛化测试，验证 token 级门控的自适应退化能力。

### 9.9 C9（二值 token 级门控 + 线性 ΔP，消融 C8 的隐式平方）

**实验目的**：C8 的 bonus $\propto (\Delta P(x))^2$ 是代数副产品（用 $\Delta P(x)$ 同时作门控强度和 bonus 幅度），并非主动设计。C9 将门控改为纯二值（$\mathbb{I}(\Delta P(x) > \tau)$，只判断"是否是领域词"，不把 $\Delta P(x)$ 大小带入 $\alpha_t$），让 $\Delta P(x)$ 只以线性形式出现一次：

$$\alpha_t = \lambda \cdot \mathbb{I}(\Delta P(x) > \tau) \cdot \frac{H_t}{H_{\max}}$$

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_t(x)}{P_d(x)} + \alpha_t \cdot \Delta P(x)\right)$$

bonus 展开后（线性，无平方）：

$$\text{bonus}_{C9} = \lambda \cdot \mathbb{I}(\Delta P(x) > \tau) \cdot \Delta P(x) \cdot \frac{H_t}{H_{\max}} \cdot P_d(x)$$

**C8 vs C9 的唯一区别：**

| | $\alpha_t$ 中 $\Delta P(x)$ 的次数 | bonus 中 $\Delta P(x)$ 的次数 | 门控特性 |
|---|---|---|---|
| C8 | 1 次（幅度门控） | 2 次（平方） | 连续门控，$\Delta P$ 越大 $\alpha_t$ 越大 |
| **C9** | **0 次（二值门控）** | **1 次（线性）** | **纯二值 on/off，$\Delta P$ 仅决定 bonus 幅度** |

**消融价值：**
- C9 > C8：线性 $\Delta P$ 已足够，平方放大过于激进（在大 $\Delta P$ 处过注入）
- C9 < C8：平方自放大的"超线性集中效应"本身有益，对大领域优势位置更精准

**实验结果（$\lambda \in \{5, 10, 20, 50, 100\}$，$\tau=0.05$，T=0.0 贪婪，Surgery n=200）：**

| $\lambda$ | acc | tps | acc\_rate | speedup | vs pure\_target |
|----------|-----|-----|----------|---------|----------------|
| 5.00 | 0.675 | 8.1 | 0.385 | 0.30× | +2.5 pt |
| 10.00 | 0.660 | 8.9 | 0.473 | 0.33× | +1.0 pt |
| 20.00 | 0.675 | 12.9 | 0.604 | 0.47× | +2.5 pt |
| 50.00 | 0.685 | 14.6 | 0.705 | 0.54× | +3.5 pt |
| **100.00** | **0.690** | **15.7** | **0.768** | **0.57×** | **+4.0 pt** |
| pure_target | 0.650 | 27.3 | 1.000 | 1.00× | baseline |

**C8 vs C9 核心对比（消融结论）：**

| 指标 | C8 最优（λ=20） | C9 最优（λ=100） | 说明 |
|------|-----------------|-----------------|------|
| 最高 acc | **0.700** | 0.690 | C8 高 1 pt（当前搜索范围） |
| 对应 tps | 12.2 | **15.7** | C9 高出 **28%** |
| 对应 acc\_rate | 0.591 | **0.768** | C9 接受率显著更高 |
| 最高 speedup | 0.44× | **0.57×** | C9 吞吐效率更优 |
| α 趋势 | acc 在 λ=20 达峰后下滑 | **acc 在 λ=100 仍单调上升** | C9 未饱和，峰值未知 |

**深度分析：**

1. **C9 的 acc 在 λ=100 处仍单调上升，峰值未探明**：C8 的 bonus $\propto (\Delta P)^2$ 使大 λ 时过注入加速，acc 在 λ=20 后迅速回落；C9 的线性 bonus 对 λ 的响应更温和——即使 λ=100，注入强度仍在安全范围内，acc 持续爬升。这意味着 **C9 的真实最优点可能在 λ=200–500 区间**，当前 +4 pt 是一个下界。

2. **C9 在 tps 和 acc\_rate 上全面领先 C8**：λ=100 时 acc\_rate=0.768（vs C8 峰值 0.591），意味着约四分之三的步都被 Draft token 接受，每轮推进的有效 token 数显著增加，tps=15.7（speedup=0.57×）是所有策略中**最高的吞吐量**。

3. **消融结论**：C9 < C8（当前搜索范围内 acc 差 1 pt），说明 C8 的 $(\Delta P)^2$ 平方自放大在**精准性**上有优势——它在 $\Delta P$ 大的位置超线性集中能量，使较小的 λ 即可达到更高 acc 峰值。但代价是响应函数过陡，λ 稍大就过注入。**C9 的线性响应更平缓，以更大 λ 换取更高接受率和吞吐量，适合对速度要求更高的场景**。

4. **两者互为 Pareto 互补**：C8（acc=0.700，tps=12.2）在准确率维度领先；C9（acc=0.690，tps=15.7）在吞吐量维度领先——两者覆盖不同的速度-质量权衡需求，共同丰富了 Pareto 前沿。

5. **扩搜潜力**：若进一步搜索 λ ∈ {200, 300, 500}，C9 有可能超越 C8 的 acc=0.700，实现同时更高 acc 和更高 tps 的完全 Pareto 优势。

### 9.10 C10（Logit 域 Product of Experts，最具理论基础的注入方式）

**实验目的**：验证"在 logit 域做差值注入"（对应 Bayesian PoE）是否比概率域/比值域方法更有效。

**核心公式**：

$$\text{logit}_{\text{steered}} = \text{logit}_T + \alpha \cdot (\text{logit}_D - \text{logit}_B) \quad\Rightarrow\quad P_{\text{steered}}(x) \propto P_T(x) \cdot \left(\frac{P_D(x)}{P_B(x)}\right)^\alpha$$

**待完成**：实验评测（$\alpha$ 建议初始搜索 $\{0.1, 0.3, 0.5, 1.0, 2.0, 5.0\}$，量纲与 C1 相近；$\tau$=无，C10 为固定 $\alpha$ 的全局注入）

**消融价值**：
- C10 > C1（固定 α，概率/比值域）：说明 logit 域注入（PoE）比概率域加法更精准，理论优势带来实际收益
- C10 < C1：说明在大体量差场景（3B vs 32B）中，logit 域的缩放行为不如比值域直接，概率域近似仍是更鲁棒的工程选择
- C10 vs C2（已有 Logit 注入）：C2 是 Z-score 标准化后注入，C10 是原始 logit 差直接注入；两者均在 logit 域操作但信号提取方式不同

### 9.11 Standard SD：规模差距的基本矛盾

无论使用 Draft-Surgery 还是未微调 Base，standard_sd 的接受率均约 20%，tps 约 5，无法加速。这证明 **3B vs 32B 的体量差距（而非领域微调）才是 SD 低效的根本原因**，传统 SD 在大体量差场景下存在根本局限。

### 9.12 初步结论

> **核心结论（已更新至 C9）**：在 3B（Draft）→ 32B（Target）的大体量差场景下，本实验提出的领域软引导投机解码框架在 Surgery 外科专科问答上实现了准确率从 0.650 提升至 **0.700（+5%）**，超越 pure_target 32B 基线。当前 Pareto 前沿由 **C8 与 C9 共同构成两个互补的最优配置**：
> - **C8（acc 优先）**：α=20 达到全局最高 acc=0.700，tps=12.2（0.44×）——牺牲少量 tps 换取最高准确率
> - **C9（tps 优先）**：α=100 达到 acc=0.690（+4%），tps=**15.7（0.57×，全系列最高吞吐）**，acc\_rate=0.768——适合对延迟更敏感的场景；且 C9 的 acc 在 λ=100 处仍单调上升，扩搜 λ∈{200,500} 有望进一步提升

> **动态 α 系列结论（完整渐进链条）**：C4（Draft 自信门控）最优 acc=0.670（+2%）、C5（Target 熵路由）最优 acc=0.685（+3.5%）、C6（C4×C5 联合，步级门控）最优 acc=0.690（+4%）、**C8（token 级双信号门控）最优 acc=0.700（+5%）**——四者依次递进，验证了"门控粒度越精准（从步级到 token 级），领域注入越有效"的核心假设。动态 α 系列的共同优势是**在实现高 acc 的同时，通过选择性激活将 tps 维持在 10–15 区间（0.38–0.57×），远优于 C1 固定 α 方案（tps 约 6–17，不稳定）**，为大模型推理的实际部署提供了更优的速度-质量权衡。

> **消融结论（C2）**：C2 系列（Full/OneHot/TopK）揭示 Logit 层注入存在固有局限——全词表竞争（Full）、K 内竞争（TopK）或阈值跃迁后过注入（OneHot）——均使最高 acc 被限制在 0.660，且 tps 显著低于 C1 的最优平衡点。C2 作为负面消融，从对立面验证了 C1 概率层加法方案的优越性。

> **消融结论（C3）**：C3 将 $\alpha \cdot \Delta P$ 作为概率补贴叠加到 $P_{\mathrm{target}}$ 后再除以 $P_{\mathrm{draft}}$，量纲一致但分母放大效应引入强非线性——α 小时补贴无效（acc\_rate ≈ standard\_sd），α 大时过度接受导致 acc 骤跌。仅在 α=1.00 处有 +1.5% 的窄窗口提升（acc=0.660，tps=5.1），远弱于 C1 在相同 acc 水平下的效率（C1 α=1.50：acc=0.660，tps=17.7）。C3 从方程结构角度证明：**在大体量差场景（3B vs 32B）下，"比值域加法"（C1）比"概率域加法"（C3）更具鲁棒性，因为前者对 $P_d$ 的小值不敏感**。

---

### 9.9 论文故事（Paper Story）

> 本节从"为何重要 / 为何困难 / 我们的创新 / 达成目标"四个维度，系统性地梳理本工作的论文叙事结构，供写作、汇报与答辩使用。

#### I. 为什么很重要（Importance / Motivation）

大型语言模型（LLM）的部署面临一个根本矛盾：**最有用的模型往往太慢**。一个 32B 的通用大模型，在单次前向传播中需要调度数百亿参数，每秒仅能生成约 27 个 token——对于需要实时响应的垂直领域应用（外科手术辅助、急诊诊断、法律条文解析），这一速度远不够用。

投机解码（Speculative Decoding）提供了一条理论上优雅的出路：用一个轻量的"草稿生成器"（Draft，3B）批量提案，再交由大模型（Target，32B）一次性并行验收。当草稿质量足够高时，可实现接近线性的加速。

**但这里埋藏着一个被长期忽视的根本矛盾：垂直领域恰恰是投机解码最需要的地方，也是它失效最严重的地方。**

外科领域的语言里充满了"laparoscopic cholecystectomy"、"peritoneum"这样的专业词汇——这些词在通用文本中出现频率极低，导致 32B 通用大模型的概率分布对其几乎为零。而一个经过外科 MedMCQA 专项微调的 3B 小模型，恰恰能精准预测这些词。结果是：**3B 提了正确的领域词，32B 因为自身分布的无知而拒绝了它。** 传统 SD 在我们的实验中接受率仅约 20%，tps 甚至低于单独运行 32B——你引入了一个领域专家，却被验收制度拒之门外。

这个问题在"大体量差"（3B vs 32B）+ "高度垂直化领域"的场景下极为普遍，且随着领域专家化程度越深，矛盾越尖锐。**解决它，不仅是一个工程问题，更是一个需要从数学接受机制层面重新设计的理论问题。**

#### II. 为什么很难（Difficulty / Challenge）

难点不在于"注入领域知识"这个想法，而在于**"如何注入才能既不损害 Target 的通用流利度，又能有效放行真正有价值的领域 Token"**。困难性体现在三个层次：

**难点一：信号提取的困难——如何知道哪个 Token 是"真正的领域知识"？**

直接用 Draft 的高概率 token 作为信号是不够的——Draft 可能对"the"也有很高的概率。真正的领域信号是 Draft 相对于"未微调的同体量 Base 模型"的**超额置信度** $\Delta P = P_{\mathrm{draft}}(x) - P_{\mathrm{base}}(x)$。引入 Base 作为"常识对照组"是关键设计，但这意味着在单卡上同时运行三个模型（Target 32B + Draft 3B + Base 3B），对显存管理和 KV Cache 共享提出了极苛刻的工程要求。

**难点二：注入机制的困难——如何在不破坏 SD 理论框架的前提下改变接受概率？**

标准 SD 的接受判据 $\min(1, P_t/P_d)$ 有其理论保证（无偏性）。任何改变接受概率的操作都相当于改写了系统的联合分布，可能引入偏差并损害 Target 的通用性。在概率域加法（C3）和比值域加法（C1）之间，量纲行为截然不同：C3 的分母放大效应导致有效注入窗口极窄；C2 的 Logit 层注入因全词表竞争而难以精准控制。找到一个在大体量差场景下**鲁棒、平滑、可控**的注入形式，经历了从 C1 到 C6 的全系列消融才得以确认。

**难点三：动态控制的困难——固定注入强度是一把双刃剑**

即使 C1 的比值域加法在数学上是鲁棒的，固定 α 的问题依然显著：α 过大（>1.5）会将 ΔP 为负的无关 token 也一并拉高接受率，导致 acc 下降；固定 α 无法区分"外科关键术语"和"连接词"——前者应该被强力放行，后者完全不需要干预。设计一个能动态感知"这一步是否值得注入"的路由机制，需要从外部信号（Draft 的置信度优势）和内部信号（Target 的不确定程度）两个维度联合决策，超参数的敏感性与信号设计的正交性使得系统复杂度大幅上升。

#### III. 我们做了什么创新（Innovation）

本工作的创新性体现在**一套系统性的、从单信号到双信号的渐进式软引导框架**，以及其背后的三项核心贡献：

**创新一：对比置信度信号 ΔP——用 Base 作为"知识对照组"**

将 $\Delta P = P_{\mathrm{draft}} - P_{\mathrm{base}}$ 定义为领域知识探针信号，是本工作的核心发明。利用同体量、同架构但未微调的 Base 模型作为"通用先验"对照，使 $\Delta P > 0$ 精确对应"领域微调带来的置信度增量"，而非通用词汇的噪声。这个信号的提取无需任何额外训练，仅需在推理时共轨运行 Base 模型，是一种**零成本的领域知识探针**。

**创新二：比值补偿验收机制（C1）——最简洁的鲁棒注入形式**

$$P'_{\mathrm{accept}} = \min\!\left(1,\ \frac{P_t}{P_d} + \alpha \cdot \Delta P\right)$$

经过 C1/C2/C3 三条路线的系统消融后确认：当 $P_t/P_d \approx 0$（大体量差的常态），比值域的加法补偿对 $P_d$ 的小值不敏感，任何正的 $\alpha \cdot \Delta P$ 均能产生有效提升而不被分母压制。这一形式不修改任何模型参数或分布，**是在 SD 验收机制层面注入领域先验的最小侵入设计**。

**创新三：双信号联合动态 α（C6）——从"全程注入"到"按需精准路由"**

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(S_t > \tau) \cdot S_t}_{\text{C4：Draft 自信门（外部）}} \cdot \underbrace{\frac{H_t}{H_{\max}}}_{\text{C5：Target 熵权（内部）}}$$

- **C4 外部信号**：$S_t = \max(P_d) - \max(P_b)$，Draft 领先 Base 的全局置信度优势，代表"专家主动举手"
- **C5 内部信号**：$H_t/H_{\max}$，Target 输出分布的归一化 Shannon 熵，代表"学生主动求助"

两者之积构成双重 AND 门：只有"Draft 有领域优势 **且** Target 真正不确定"时才触发强注入，否则退化为标准 SD，实现**稀疏、精准、双重确认的路由决策**。整个 C1→C3→C4→C5→C6 的推导链构成一套完整的消融体系，每一步变化均有明确的理论动机和实验验证。

#### IV. 实现了什么目标（Results / Impact）

**核心实验结果（MedMCQA Surgery，n=200，贪婪解码）：**

| 方法 | acc | tps | 相对 pure\_target |
|------|-----|-----|-----------------|
| pure\_target（32B 单独） | 0.650 | 27.3 | 基准 |
| standard\_sd（传统 SD） | 0.650 | 5.3 | 0.19×，**无 acc 增益** |
| **C1（α=0.10）** | **0.690** | 6.9 | **+4% acc**，0.25× |
| **C6（λ=50）** | **0.690** | **11.5** | **+4% acc**，**0.42×** |

**目标一：超越 Target 本身的准确率上限。**

在不改变任何模型权重的前提下，通过推理时的接受机制重设计，使系统准确率从 0.650 提升至 0.690（+4%）。这证明了 $\Delta P > 0$ 的位置确实对应着领域专知而非噪声，也证明了**"让大模型借到小模型的领域知识"是可行的、有效的**。

**目标二：在准确率提升的同时改善 Pareto 前沿。**

C6 在与 C1 并列最高 acc（0.690）的同时，将 tps 从 6.9 提升至 11.5（**+67%**）。双信号 AND 门使接受率从 C1 的 33.4% 提升到 C6 的 60.0%，每轮 SD 循环有效接受的 Draft token 数从约 1.7 提升至约 3.0，**领域知识的精准路由本身就是一种效率提升**。

**目标三：构建可解释、可复用的方法论体系。**

本工作通过 C1–C6 六条路线的系统消融，建立了完整的接受策略设计原则：Logit 层注入因尺度竞争失控（C2）；概率域加法因分母非线性不稳定（C3）；固定注入因盲注降低精准率（C1 大 α）；单一路由信号各有局限（C4/C5）；双信号乘法融合实现帕累托突破（C6）。该框架不依赖任何特定领域，可推广到法律、金融、代码等任意"大通用 + 小专家"组合，且**无需对任何模型进行额外微调，是即插即用的推理时领域知识注入方案**。

> **最终论断**：在大体量差的投机解码场景中，问题不在于"Draft 不够好"，而在于"Target 的接受机制不会用领域专知"——修复接受机制，比重新训练一个更好的 Draft 更高效、更轻量、更可扩展。

---

## 10. 待完成实验

- [x] **C3 全量评测（T=0.0 贪婪）**：完成。最优 acc=0.660（α=1.00，+1.5%），但有效窗口极窄，整体弱于 C1；详见 Section 8.2 与 9.3 分析。
- [x] **C4 初步评测（T=0.0 贪婪）**：完成。最优 acc=0.670（α=2.0，+2%），峰值窗口极窄，待细化 α ∈ [1.5, 3.0]。
- [x] **C5 初步评测（T=0.0 贪婪）**：完成。最优 acc=0.685（λ=5.0，+3.5%），Pareto 新突破点；待细化 λ ∈ [3.0, 7.0] 寻找峰值。
- [x] **C4 细化实验**：已确认最优 α=2.0（acc=0.670，+2%），峰形尖锐，结论稳定。
- [x] **C5 细化实验**：已确认最优 λ=5.0（acc=0.685，+3.5%），峰形尖锐，无法进一步突破 C1 的 0.690。
- [x] **C6 全量评测（T=0.0 贪婪）**：完成。λ=50 达到 acc=0.690（+4%，与 C1 最优并列），tps=11.5（0.42×），Pareto 绝对领先，当前最优综合配置。
- [x] **C7 全量评测（T=0.0 贪婪）**：已完成，λ ∈ {5,10,20,50}，最优 acc=0.660（λ=10/20），显著弱于 C6（acc=0.690）；消融结论：C3 框架固有弱点（分母放大）+ $P_d$ 隐式缩放有益，详见 § 9.7。
- [ ] **前期探针：Target 熵分布分析（`probe_entropy.py`）**：见第 11 节详细设计。
- [ ] **通用能力验证（GSM8K）**：验证 Surgery 领域微调后通用推理能力未坍塌；期望 C1/C3 在 GSM8K 上 acc 与 pure_target 持平（ΔP≈0，软引导自动退化）
- [ ] **完整帕累托图**：整合所有实验结果绘制 acc vs tps 帕累托曲线（含 C3/C7，各策略的 Pareto Front 可视化）

---

## 11. 前期探针实验：Target Shannon 熵分布分析

> **实验目的**：为 C5（Entropy-Aware α）和 C6（双信号路由）提供实证动机——证明 Target 大模型在遇到不熟悉的领域知识时，输出分布的不确定性（Shannon 熵）显著上升，从而验证"用熵作为路由信号"的合理性。

### 11.1 实验设计

**脚本**：`probe_entropy.py`（本地路径 `Domain-Steered Decoding/probe_entropy.py`）

**运行流程**：
1. 加载 Target（32B）+ Draft（Surgery 3B）+ Base（3B）三个模型
2. 对每道 Surgery MCQ，用 Target 贪婪解码生成最多 150 个 token
3. 在每个生成步骤 $t$，记录：
   - Target 输出分布的 Shannon 熵：$H_t = -\sum_x P_t(x) \log P_t(x)$（nats）
   - 归一化熵：$\hat{H}_t = H_t / \log V$，$V = 152064$
   - 领域知识信号：$\Delta P_t = P_{\mathrm{draft}}(x_t) - P_{\mathrm{base}}(x_t)$
4. 解析 Target 生成文本中的预测答案（A/B/C/D），判断是否正确
5. 分三个维度进行统计分析（见 11.2）

**远端运行命令**：

```bash
cd /data/ocean/decoding && git pull
export CUDA_VISIBLE_DEVICES=0 HF_DATASETS_OFFLINE=1

# 完整版（含 ΔP 计算，需 3 个模型同时在显存，耗时约 20-30 分钟 / 100 题）
python probe_entropy.py --limit 100 --max_new 150 --out_dir results/entropy_analysis

# 快速版（仅 Target，不计算 ΔP，速度约快 3×）
python probe_entropy.py --limit 100 --max_new 150 --no_delta --out_dir results/entropy_target_only
```

**结果查看命令**：

```bash
# 核心统计数字
cat results/entropy_analysis/summary.json

# 高熵领域候选 token Top-25
python -c "
import json
d = json.load(open('results/entropy_analysis/high_entropy_tokens.json'))
print(f'高熵阈值: {d[\"H_threshold\"]:.4f} nats')
print(f'\\nTop-25 高熵候选 token:')
for t in d['top_tokens'][:25]:
    print(f'  {t[\"token\"]:<25} count={t[\"count\"]:>4}  H={t[\"mean_H\"]:.4f}  H/Hmax={t[\"mean_Hn\"]:.4f}  ΔP={t[\"mean_dP\"]:.4f}')
"

# 可视化图
ls results/entropy_analysis/entropy_analysis.png
```

### 11.2 三项分析维度与预期结论

#### 分析一：序列平均熵——做对 vs 做错

对每道题目计算其生成序列的**平均归一化熵** $\bar{\hat{H}} = \frac{1}{T}\sum_{t=1}^{T} \hat{H}_t$，按答题正误分组比较。

**预期结论**（支持 C5 机制的证据）：

> 做错题目的序列平均熵 **显著高于** 做对题目（Mann-Whitney U 检验 $p < 0.05$）。

物理解释：Target 对该题涉及的医学领域知识掌握不足时，输出分布更"平坦"（高熵，"不知道说什么好"），最终选择错误答案；对熟悉领域的题目输出分布更"尖锐"（低熵，"心中有数"），正确选出答案。

| 分组 | 预期均值 $\bar{\hat{H}}$ | 解读 |
|------|------------------------|------|
| 做对题 | 较低（更确信） | Target 掌握该领域知识 |
| 做错题 | 较高（更迷茫） | Target 遇到知识盲区 |

#### 分析二：逐步熵——领域词位置 vs 通用词位置

将所有 token 位置按 $\Delta P_t$ 大小分为**领域词**（$\Delta P > 0.05$）和**通用词**（$\Delta P \leq 0.05$）两组，比较各组的逐步熵分布。

**预期结论**：

> 领域词位置的逐步熵 $H_t$ **显著高于** 通用词位置。

物理解释：$\Delta P > 0.05$ 的位置恰好是 Draft 微调带来增量知识的位置（外科术语、解剖词汇）。在这些位置，Target（通用大模型）对词汇的把握不如领域专家 Draft，输出分布更分散，熵更高。这直接证明了**C5 熵权的物理意义**：$H_t / H_{\max}$ 在"领域盲区"处自动放大，正确识别了需要 Draft 介入的时机。

#### 分析三：错题中高熵 token 的词汇分析

从做错题目的生成序列中，筛选满足 $H_t > \mu_H + \sigma_H$（高熵）且 $\Delta P_t > 0$（领域信号）的 token，统计 Top 出现频次，判断是否为医学/外科领域词汇。

**预期结论**：

> 错题高熵位置出现的高频 token，以**外科术语、解剖词汇、药物名称**为主，而非常见语法词。

物理解释：这些 token 是 Target "最不确定的同时 Draft 最有把握"的位置，即领域知识缺口最显著处。这为 C6 双信号路由的设计提供了**词汇级别的直接证据**：我们的机制恰好在这些最需要领域专知介入的位置触发注入。

### 11.3 输出文件说明

| 文件 | 内容 | 用途 |
|------|------|------|
| `entropy_records.json` | 每题的逐步熵序列、ΔP 序列、生成文本 | 后续细粒度分析的原始数据 |
| `summary.json` | 核心统计数字（均值、标准差、p 值） | 论文表格数据来源 |
| `high_entropy_tokens.json` | 错题高熵 token Top-100 + 上下文示例 | 词汇级证据，支撑论文分析 |
| `entropy_analysis.png` | 三张对比分布图（直方图） | 论文可视化图 |

### 11.4 实验结果（三科对比，已完成）

#### 核心数字汇总表

| 科目 | Target acc | 对题平均 H | 错题平均 H | 差值 | p 值 | 结论 |
|------|-----------|-----------|-----------|------|------|------|
| **Surgery**（专科，实验 A） | **59%** | 0.387 | **0.456** | **+0.069** | **0.0008** ✅ | 显著 |
| Pharmacology（对照，实验 B） | 76% | 0.321 | 0.350 | +0.029 | 0.1218 ❌ | 不显著 |
| Anatomy（对照，实验 C） | 74% | 0.309 | **0.409** | **+0.100** | **0.0004** ✅ | 显著 |

> 归一化熵 H/H_max（H_max = log 152064 ≈ 11.93 nats）：Surgery 对题 = 0.0324，错题 = 0.0382；Pharmacology 对题 = 0.0269，错题 = 0.0293；Anatomy 对题 = 0.0259，错题 = 0.0343。

#### 领域词位置 vs 通用词位置（实验 A，含 ΔP）

| Token 类型 | 位置数 | raw H | H/H_max | 对比 |
|-----------|--------|-------|---------|------|
| 通用词（ΔP ≤ 0.05） | 5633 | 0.390 | 0.0327 | 基准 |
| **领域词（ΔP > 0.05）** | 1110 | **0.611** | **0.0512** | **+57%** ↑ |

### 11.5 深度分析

#### 核心发现一：Target 熵是领域知识盲区的天然探测器（关键）

三科实验的对比揭示了一个清晰的规律：**Target 准确率越低的科目，整体熵越高**。

| 科目 | Target acc | 对题 H | 解读 |
|------|-----------|--------|------|
| Surgery | 59% | 0.387 | 最高：Target 对外科专知掌握最弱 |
| Pharmacology | 76% | 0.321 | 最低：Target 对药理学掌握最佳 |
| Anatomy | 74% | 0.309 | 最低：Target 对解剖学掌握较好 |

值得注意的是，**即使是做对的题目**，Surgery 的平均熵（0.387）也显著高于 Pharmacology（0.321）和 Anatomy（0.309）。这说明熵的差异不仅来自答题错误，更反映了 Target 在整个推理过程中对该领域的整体不确定程度——外科知识的专业性使 Target 在推理的每一步都更加迟疑，即便最终侥幸选对。

> **论文表述**：Shannon 熵在领域感知上具有"持续性检测"而非"事件性检测"的特性：不是在做错的瞬间熵才高，而是在整道 Surgery 题目的推理过程中，Target 都保持着更高的平均不确定性。这正是 C5 熵路由的物理基础——熵持续偏高的时段，正是 Draft 领域专知最有价值介入的时段。

#### 核心发现二：Pharmacology 的"非显著"恰恰是最好的自洽证据

Pharmacology 实验中，对题与错题的熵差异 p=0.1218，统计不显著。这不是失败，而是对 C5 机制**自我退化能力**的完美演示：

- Pharmacology Target acc=76%，说明模型对该领域知识掌握相当充分
- 充分掌握 → 整体熵较低（0.321）→ C5 中 $H_t/H_{\max}$ 权重自动降低 → $\alpha_t$ 自动趋零
- 结果：**在 Target 不需要帮助的领域，C5/C6 注入强度自动退化为零**，不会破坏通用能力

这是 C6 设计的一个关键论点：它是**领域自适应的**，不需要人工干预来区分"哪个领域该注入、哪个不该注入"，熵信号本身就是这个判断的代理变量。

#### 核心发现三：领域词位置熵比通用词高 57%（直接支撑 C5）

实验 A 的 ΔP 分析显示，在 Surgery 题目的生成序列中：

- 通用词位置（ΔP ≤ 0.05，如 "the"、"is"、连接词）：H = 0.390
- 领域词位置（ΔP > 0.05，外科术语等）：H = **0.611**，比通用词高 **57%**

这个数字直接证明：**$\Delta P > 0$ 的位置（Draft 相对 Base 超额置信的 token）恰好是 Target 最不确定的位置**。换言之，Draft 的领域优势信号（ΔP）和 Target 的不确定性信号（$H_t$）指向的是同一批 token——这从经验层面验证了 C6 将两个信号相乘的设计合理性：它们确实是对同一"知识盲区时刻"的双重刻画，而非互相独立的随机噪声。

#### 核心发现四：三科准确率与熵分布对应关系（跨领域泛化论据）

| 科目 | Target acc | 错题 H | 对题 H | H 差值 | p 值 |
|------|-----------|--------|--------|--------|------|
| Surgery | 59% | 0.456 | 0.387 | +0.069 | 0.0008 |
| Anatomy | 74% | 0.409 | 0.309 | +0.100 | 0.0004 |
| Pharmacology | 76% | 0.350 | 0.321 | +0.029 | 0.1218 |

规律清晰：**Target 准确率越低（领域专知缺口越大），错题与对题之间的熵差距越大，熵路由信号越有效**。Anatomy 虽然 acc 比 Surgery 高（74% vs 59%），但错题熵差（+0.100）反而比 Surgery（+0.069）还大，说明 Anatomy 错题中 Target 的困惑程度极其突出（当 Target 答错时，它非常迷茫）。

这为 C5/C6 的**领域自适应性**提供了跨科目的泛化论据：该机制不是针对 Surgery 领域硬编码的特化解法，而是能通用于任何 Target 存在真实知识盲区的垂直领域。

#### 结论小结（论文可直接引用）

> 1. **跨科对比**：Surgery 题目的整体序列平均熵（包括对题）显著高于 Pharmacology 和 Anatomy，证明熵是 Target 领域知识盲区的持续性检测信号，而非随机噪声。
>
> 2. **领域词位置**：ΔP > 0.05 的领域 token 位置，Target 熵比通用词位置高 57%（0.611 vs 0.390），ΔP 信号与熵信号在空间上高度重叠，从词汇层面验证 C6 双信号设计的内部一致性。
>
> 3. **自适应退化**：在 Target 掌握充分的 Pharmacology 科目（acc=76%），熵差异不显著（p=0.1218），C5/C6 的动态 α 自动收缩为零，**天然保护了 Target 在熟悉领域的通用能力**，无需额外设计"关闭开关"。

### 11.6 实验D：Draft 在 Target 高熵位置的延续词探针（`probe_entropy.py` Section 4）

**实验设计**：在 Target 贪婪生成过程中，对每道错题的高熵位置（H > μ+σ = 1.065 nats），让 Draft 从**完全相同的上下文**贪婪续写 3 个 token，记录 Draft 在这些"Target 决策分叉口"想输出的内容。关键在于：高熵位置本身输出的 token 不一定是领域词，但 **Draft 从该位置出发的延续词**才是领域知识的真实载体。

**工程实现**：对每道错题只做一次 Draft prefill，用逐步推进 KV Cache 的方式覆盖全部生成步，到高熵位置时 `copy.deepcopy` 克隆 KV，不影响主 cache。

**实验结果**（Surgery 100 题，41 道错题，共 355 个高熵探针事件）：

**Top-20 Draft 最常见延续（Target 困惑时 Draft 最想说什么）**：

| 排名 | Draft 延续（3 tokens） | 频次 | 类别 |
|---|---|---|---|
| 1 | `Final answer:` | 17 | 推理噪音 |
| 2 | `. Final answer` | 16 | 推理噪音 |
| 3 | `final answer:` | 6 | 推理噪音 |
| 4 | `condylar` | 4 | ★ 外科领域词（髁突） |
| 5 | `so the final` | 4 | 推理噪音 |
| 6 | `making the correct` | 3 | 推理噪音 |
| 7 | `displacement of the` | 2 | ★ 外科力学词 |
| 8 | `avoids the risks` | 2 | ★ 外科情境词 |
| 9 | `maxilla.` | 2 | ★ 颌面解剖词（上颌骨） |
| 10 | `ductal strict` | 2 | ★ 导管狭窄（ductal stricture） |

**Top 高熵位置详情（Target 最困惑时，Draft 想输出的内容）**：

| H_target | Target 输出 | 上文片段 | Draft 续词 | 解读 |
|---|---|---|---|---|
| 3.37 | `context` | `...on the given options and the` | `[principle, of, muscles]` | Draft 知道是"muscle principle"（肌肉解剖原理） |
| 2.43 | `direct` | `...synovial fluid, or` | `[cond, yl, ar]` | Draft 拼出 **condylar**（髁突），高专业度术语 |
| 2.44 | `mand` | `...not attached to the` | `[fracture, site, .]` | Draft 补全"mandible fracture site"（下颌骨骨折位点） |
| 2.53 | `par` | `...incision, commonly used in` | `[breast, surgery, .]` | Draft 知道该切口用于乳腺外科 |
| 2.77 | `bone` | `...remove without significant` | `[rotation, or, ang]` | Draft 续"bone rotation or angulation"（骨旋转/成角） |
| 2.69 | `repeated` | `...cause symptoms due to` | `[obstruction, of, the]` | Draft 续"obstruction"（气道/导管梗阻） |
| 2.96 | `reducing` | `...maintaining stability and` | `[minimizing, complications, is]` | Draft 知道"minimizing complications"是下颌骨手术用语 |

#### 11.6.1 归纳出的两大规律

**规律一：推理衔接噪音（~12% 事件）**

Top-20 中前三名合计 39 次，均为 "Final answer:" 或 "final answer:" 变体。原因：Target 在 CoT **推理链段落衔接点**高度不确定（大模型的推理结构多样），此时 3B Draft 小模型倾向于"跳到结论"。这属于**生成阶段的结构性噪音**，不是领域知识缺失导致的高熵，也不反映 Draft 的领域优势。

**规律二：真实领域知识出现（核心发现 ✅）**

过滤掉 "Final answer" 类噪音后，高熵位置剩余的 Draft 延续呈现明确的外科/颌面外科专知词汇：
- `condylar`（髁突，4 次）：颌骨外科核心解剖术语
- `mandible fracture site`（下颌骨骨折位点）
- `displacement of the`（骨位移，外科力学）
- `maxilla.`（上颌骨，颌面解剖）
- `ductal stricture`（导管狭窄）
- `obstruction`（梗阻，外科病理）

**直接结论**：在 Target 最困惑的高熵位置，Draft 确实拥有 Target 所缺失的领域知识——这为 C5/C6 的"熵驱动路由"设计提供了**最直接的因果证据**：Target 高熵 → 领域知识盲区 → Draft 趁虚而入填补，是有实验基础的。

#### 11.6.2 下一步因果验证实验

上述分析说明"Draft 有知识"，但**无法直接证明替换后准确率上升**。为此设计因果验证实验（`probe_substitution.py`）：

> **实验D-2（直接替换验证）**：在生成过程中，凡 Target 熵 H_t > 阈值的位置，直接用 Draft 贪婪预测 token 替换 Target 的选择，观察整体答题准确率的变化。
>
> - 若替换后准确率上升 → **直接因果证据**：Target 高熵位置就是领域盲区，Draft 能纠正
> - 若准确率下降 → 说明 Draft 的替换破坏了推理链（仍有参考价值：需要"软引导"而非"硬替换"）

#### 11.6.3 实验D-2 实测结果（MedMCQA Surgery，n=200，`probe_substitution.py`）

**设置**：Qwen2.5-32B-Instruct（Target）+ Qwen2.5-3B-Instruct-Surgery（Draft）；每题 `max_new=150`；当 $H_t > \tau$ 时用 Draft 贪婪 token 替换 Target 选择，否则沿用 Target。

| 策略 | 阈值 $\tau$ | 准确率 | 平均替换率 | vs pure_target |
|------|------------|--------|------------|----------------|
| pure_target | — | **0.645** | 0% | baseline |
| entropy_sub | 0.5 | 0.605 | 34.2% | **−4.0 pt** |
| entropy_sub | 1.0 | 0.660 | 17.6% | **+1.5 pt** |
| entropy_sub | 1.5 | 0.655 | 6.6% | **+1.0 pt** |
| entropy_sub | 2.0 | **0.670** | 1.6% | **+2.5 pt** |
| pure_draft | — | 0.565 | 100% | **−8.0 pt** |

**解读要点**：

1. **pure_draft 远低于 pure_target（56.5% vs 64.5%）**：单独用 3B 领域小模型写整条 CoT，通用推理与题面理解明显弱于 32B；主叙事应是「大模型为主体、小模型为补丁」，而非全盘替代。

2. **低阈值（$\tau=0.5$）硬替换有害（−4 pt）**：约三分之一 token 被 Draft 接管，大量触发点并非「领域知识盲区」，而是句式、衔接、标点和推理结构上的不确定；Draft token 打断 32B 的连贯链，与 Draft 延续词探针中的 **「Final answer」类噪音**一致：粗门控会把结构不确定性误判为知识缺口。

3. **阈值提高则收益单调变好（$\tau \uparrow$ → acc $\uparrow$，替换率 $\downarrow$）**：仅在 **极端高不确定步**（$\tau=2.0$，平均仅 **1.6%** token 被替换）时达到最佳 **67.0%**，相对 baseline **+2.5 个百分点**。与探针结论一致：**最尖锐的 Target 不确定处**，Draft 的局部 token 更可能对齐领域补位。

4. **与软引导（C3/C6）的叙事衔接**：硬替换在「稀疏触发、阈值较高」时尚能小幅涨分，门控略宽即伤分；投机解码中的 **概率域补贴 / 接受率重加权** 不必强行改写 token，可在保留 Target 主分布的前提下注入 Draft 信号，**避免整条链被 3B 拖垮**——本实验为「为何需要软引导而非硬替换」提供独立实证。

5. **统计备注**：200 题上约 5 道题量级的绝对差异；正式论文可报 95% 置信区间或 bootstrap，或扩样本至 500+ 以稳定结论。

---

## 12. Future Work（未来工作方向）

1. 大模型的综合能力如何保持，直观上可以解释吗
2. 我们的机制如何区分某个token是应该用target大模型的综合能力，还是draft model的领域专家能力。
3. 通用/领域问题上，targrt的熵是否有区别（可以做序列熵/平均熵）
4. 换领域做实验

当前 Python 实现存在约 4× 的 overhead（kernel launch 延迟、CPU-GPU `.item()` 同步点等），导致实测 tps 约为理论值的 1/4。以下两个方向可将本框架的理论加速比（≈3×）在工程层面充分释放，同时进一步扩展方法上限。

### 12.1 底层引擎接管：C++ 层 + PagedAttention

**背景**：标准投机解码在优化实现（vLLM、TGI、DeepSpeed-MII）中已验证可实现 2–4× 吞吐量提升。其核心在于：

- **Python Overhead 消除**：将 argmax 采样、接受概率计算、token 选取等操作全部融合为 CUDA kernel，取消 CPU-GPU 同步（`.item()` 调用），消除每轮数十毫秒的等待。
- **PagedAttention / KV Cache 指针共享**：KV Cache 以 Page 粒度管理。Draft 提案与 Target 验证共享前缀 Page，拒绝时仅废弃相关 Page 的索引指针，实现 **0 数据拷贝**的回退操作（当前实现依赖 StaticCache 写时覆盖，无法真正共享物理内存）。
- **预期收益**：将当前框架移植到 C++/CUDA 后端后，理论每轮耗时从实测 ~265ms 降至 ~57ms，整体吞吐量预期提升 3–4×，同时领域软引导（C1 的 $\alpha \cdot \Delta P$ 补偿）可在验收 kernel 中以极低代价实现。

**实施路径**：在 vLLM 的 `llm_engine` 层注入自定义 `SpeculativeWorker`，将 C1 策略的接受逻辑注册为 GPU-side verifier，复用 PagedAttention 的 KV 管理。

### 12.2 树状投机解码（Tree-based Speculative Decoding）

**背景**：链式投机解码（本项目当前方案）每轮只生成一条 K token 的草稿序列，当接受率不够高时，大量草稿被拒绝。树状投机解码（Medusa 2024、Eagle 2024、SpecInfer）将 Draft 扩展为一棵多分支树：

```
链式（当前）:  Draft → t₀ → t₁ → t₂ → t₃ → t₄

树状（Future Work）:
                     ┌→ A₁ → A₂
       Draft → t₀ ───┼→ B₁ → B₂
                     └→ C₁ → C₂
```

Target 使用 **Tree Attention Mask**（因果掩码的推广）一次 forward 验证整棵树所有分支，每次 Target 调用的 "有效接受 token 数" 大幅提升。

**与本项目方法的结合点**：

- **ΔP 引导树剪枝**：$\Delta P(x) = P_{\mathrm{draft}}(x) - P_{\mathrm{base}}(x)$ 可作为分支展开的优先级信号——ΔP 大的 token 是领域专家认为更可靠的选项，优先作为树节点展开；ΔP 小或负值的分支提前剪掉，减少无效验证。
- **C1 接受策略的树化推广**：将 $\min\!\left(1,\, \frac{P_{\mathrm{target}}(x)}{P_{\mathrm{draft}}(x)} + \alpha \cdot \Delta P\right)$ 推广为树上每个节点的接受概率，在 Tree Attention 的验收阶段按节点逐步筛选，最终以领域先验选出最优路径。
- **预期收益**：在高接受率（≥0.9）场景下，树状方案可将每次 Target forward 的有效 token 输出从 ~5 提升至 8–12，配合 ΔP 剪枝后对 Target 的调用次数也可减少，整体吞吐量预期在链式基础上再提升 1.5–2×。

---

## 11. 方法演进叙事：从基础公式到 C9 的逐步创新

> 本节以论文写作视角，从统一出发点出发，梳理每一步改动解决了什么核心难题、做了什么创新，供论文撰写与答辩使用。

### 11.1 出发点：领域知识注入的统一公式

所有策略的共同起点是一个直觉上极清晰的思想：

$$\boxed{P_{\text{steered}}(x) = P_{\text{target}}(x) + \alpha \cdot \underbrace{(P_{\text{expert}}(x) - P_{\text{base}}(x))}_{\Delta P(x)}}$$

**含义**：Target 对 token $x$ 的概率，在领域专家（微调小模型）相对常识基线（未微调小模型）的超额置信度 $\Delta P(x)$ 方向上做一次概率补贴。$\alpha$ 控制补贴强度。

**为什么用 $P_{\text{expert}} - P_{\text{base}}$ 而不是直接用 $P_{\text{expert}}$？**

这是第一个关键设计：引入未微调的 Base 模型作为"常识对照组"。$P_{\text{expert}}$ 对"the"和"surgery"都会给出高概率，无法区分领域知识与通用知识。$P_{\text{expert}} - P_{\text{base}}$ 精准地提取了**因领域微调而增强的那部分概率质量**，把常识成分消掉，只留下领域增量。这是整个系统的信号提取基础——没有 Base 对照，就没有精准的领域信号。

---

### 11.2 第一步改动（C1/C3）：嵌入投机解码验收框架

**面临的难题**：上述公式定义了一个修正后的 Target 分布 $P_{\text{steered}}$，但如何与投机解码的验收规则结合？标准投机解码要求给出"接受概率"，而 $P_{\text{steered}}$ 是一个完整分布，无法直接用于验收判断。

**创新**：将 $P_{\text{steered}}$ 嵌入投机解码的验收比值中：

**C3（概率域直接补贴）**：
$$P'_{\text{accept}} = \min\!\left(1,\ \frac{P_{\text{target}}(x) + \alpha \cdot \Delta P(x)}{P_{\text{draft}}(x)}\right)$$

分子就是 $P_{\text{steered}}(x)$，精确匹配出发公式。

**C1（比值域加法，更鲁棒的工程实现）**：
$$P'_{\text{accept}} = \min\!\left(1,\ \frac{P_{\text{target}}(x)}{P_{\text{draft}}(x)} + \alpha \cdot \Delta P(x)\right)$$

两者的区别：C3 在分子做加法（等价于定义 $P_{\text{steered}}$），C1 在比值上做加法（额外引入了 $P_d(x)$ 的隐式缩放）。实验表明：在 3B vs 32B 的大体量差场景下，C3 的分母放大效应使有效注入窗口极窄；C1 的比值域加法对 $P_d$ 小值不敏感，鲁棒性更强。

**重要意义**：这一步回答了"如何在不打破投机解码理论框架的前提下注入领域知识"，是整个系统的工程基础。

---

### 11.3 第二步改动（C4）：静态 $\alpha$ → 基于 Draft 自信度的动态门控

**面临的难题**：固定 $\alpha$ 对所有 token 一视同仁——在生成"the"、","、"is"这类通用连接词时，$\Delta P$ 通常接近零，加法几乎无效；但当 $\alpha$ 调大时，对少数 $\Delta P$ 为负的词（Base 反而比 Expert 更有把握的词）会产生反向干扰。固定 $\alpha$ 无法区分"需要注入"和"不需要注入"的位置。

**创新（Sparse Activation，稀疏激活）**：

$$\alpha_t = \alpha_{\text{base}} \cdot \mathbb{I}(S_t > \tau) \cdot S_t, \quad S_t = \max(P_{\text{expert}}) - \max(P_{\text{base}})$$

只有当 Draft 整体上比 Base 更自信（步级信号 $S_t$ 超过阈值 $\tau$）时，才触发注入；否则 $\alpha_t = 0$，策略完全退化为标准投机解码，保护 Target 的通用流利度。$S_t$ 越大，注入越强，信号自适应于"这一步领域知识的重要程度"。

**重要意义**：这是从"全程注入"到"按需注入"的关键跳跃，解决了固定 $\alpha$ 的普适性问题，同时大幅提高了接受率（减少了无效注入导致的拒绝），是 tps 开始提升的关键一步。

---

### 11.4 第三步改动（C5）：增加 Target 内部不确定性信号

**面临的难题**：C4 只考虑了 Draft 端的信号（"专家有没有把握"），但没有考虑 Target 端的状态——即使 Draft 很自信，如果 Target 本身对这一步也很确定（比如句法规则决定的 token），注入依然是多余的干扰。我们缺少一个"Target 是否真的需要帮助"的信号。

**创新（Uncertainty-Driven Routing，不确定性驱动路由）**：

$$\alpha_t = \lambda \cdot \frac{H_t}{H_{\max}}, \quad H_t = -\sum_x P_{\text{target}}(x) \log P_{\text{target}}(x)$$

Target 的输出熵 $H_t$ 是其自我不确定性的天然量化：熵低（分布尖锐）表示 Target 胸有成竹，无需外援；熵高（分布平缓）表示 Target 遇到了知识盲区，正是 Draft 介入的最佳时机。$H_t/H_{\max}$ 归一化后作为 $\alpha_t$ 的连续权重，实现了"Target 越懵逼，注入越强"的自适应路由。

**前期探针实验验证**：`probe_entropy.py` 实验证明，在 Surgery 做错题中，Target 平均熵显著高于做对题（p=0.0008），且 $\Delta P > 0.05$ 的领域 token 位置熵比通用 token 高 57%——直接支持"熵是领域盲区探测器"的假设。

**重要意义**：引入了来自 Target 内部的自适应信号，让系统不再单边依赖 Draft 的外部评判，具备了"内外双感知"的雏形。

---

### 11.5 第四步改动（C6）：双信号正交组合，实现最精准的门控

**面临的难题**：C4 和 C5 各自只有单一信号。C4 的步级 Draft 信号可能在 Target 很确定时仍然触发（Draft 自信但 Target 不需要帮助）；C5 的熵信号可能在 Target 困惑但 Draft 也没有优势时虚触发（Target 懵逼但 Draft 也不知道）。两者单独使用都有误触发风险。

**创新（Dual-Signal Gated α，双信号联合门控）**：

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(S_t > \tau) \cdot S_t}_{\text{C4：专家主动举手}} \cdot \underbrace{\frac{H_t}{H_{\max}}}_{\text{C5：学生主动求助}}$$

两个信号相乘构成逻辑"AND"门：**"Draft 自信"AND"Target 懵逼"同时成立时，才强力注入**。任一条件不满足，$\alpha_t$ 自动降低。这大幅减少了误触发——生成通用词时 Target 熵低（C5 权重近零）；生成陌生领域词时 Draft 无优势（C4 门关闭）。只有"领域急转弯"处，两信号才同时激活。

**实验结果**：C6 以 $\lambda=50$ 达到 acc=0.690（+4%），tps=11.5（比 C1 提升 67%），在 Pareto 前沿上超越所有前序方法。

**重要意义**：将两个正交外部/内部信号的乘积组合，解决了单信号误触发问题，是"稀疏且精准的按需注入"的完整实现。

---

### 11.6 第五步改动（C8）：从步级门控到 token 级门控，消除误触发根源

**面临的难题**：C6 的门控信号 $S_t = \max(P_{\text{expert}}) - \max(P_{\text{base}})$ 是一个**步级（step-level）**信号——它衡量 Draft 在这一步整体上是否比 Base 更自信，但这个 max 对应的可能是 token A，而我们正在验收的是 token B。当 Draft 对 token A（比如"mandibular"）高置信时，对 token B（比如"the"）的验收也被激活，这是一种结构性的跨 token 误触发。

**创新（Token-Level Gate，token 级门控）**：

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(\Delta P(x) > \tau) \cdot \Delta P(x)}_{\text{token 级门控：与被验收 token } x \text{ 完全对齐}} \cdot \frac{H_t}{H_{\max}}$$

将 $S_t$（步级，与 $x$ 无关）替换为 $\Delta P(x)$（token 级，直接衡量 Draft 对 $x$ 本身的优势）。门控信号与验收决策完美对齐，回答的问题从"这一步 Draft 整体有多自信？"变为"Draft 对这个具体 token $x$ 是否比 Base 更偏爱？"——后者才是验收决策真正需要的信息。

> **代数副产品**：由于 $\Delta P(x)$ 同时出现在 $\alpha_t$ 中（作为门控强度）和 bonus 项 $\alpha_t \cdot \Delta P(x)$ 中，最终 bonus $\propto (\Delta P(x))^2$，形成无意为之的"平方自放大效应"，使领域词处的增强超线性集中。

**实验结果**：C8 以 $\lambda=20$ 达到 acc=0.700（+5%，全局新高），tps=12.2，全面超越 C6。

**重要意义**：回答了"门控信号的粒度应该与验收决策的粒度对齐"这一设计原则，从步级到 token 级是门控机制的本质提升。

---

### 11.7 第六步改动（C9）：将门控与幅度解耦，恢复线性 $\Delta P$

**面临的难题**：C8 中 $\Delta P(x)$ 在 $\alpha_t$ 里承担了双重角色——既是"是否激活"的门控强度（$\Delta P(x) > \tau$），又是 $\alpha_t$ 的幅度因子，导致 bonus 里 $\Delta P$ 出现两次（隐式平方）。这个平方效应并非主动设计，而是代数副产品。当 $\Delta P(x)$ 较大时，平方放大可能导致过注入；当 $\Delta P(x)$ 较小（但超过 $\tau$）时，平方使 bonus 被进一步抑制。

**创新（Binary Gate + Linear ΔP，二值门控 + 线性幅度解耦）**：

$$\alpha_t = \lambda \cdot \underbrace{\mathbb{I}(\Delta P(x) > \tau)}_{\text{纯二值门控，只判断是否激活}} \cdot \frac{H_t}{H_{\max}}$$

$$\text{bonus}_{C9} = \lambda \cdot \mathbb{I}(\Delta P(x) > \tau) \cdot \underbrace{\Delta P(x)}_{\text{线性出现一次}} \cdot \frac{H_t}{H_{\max}} \cdot P_d(x)$$

门控信号（$\mathbb{I}(\Delta P(x) > \tau)$，回答"是否激活"）与幅度信号（$\Delta P(x)$，回答"激活多强"）彻底解耦。前者是纯二值 on/off，后者是线性 $\Delta P$。两个角色由同一个量的不同使用方式承担，设计更清晰，消除了隐式平方的不确定性。

**消融价值**：C9 与 C8 的对比直接量化了"$\Delta P$ 线性 vs 平方"的效果差异，为门控机制的最优设计提供实证依据。

---

### 11.8 演进全景与设计原则总结

$$P_{\text{steered}}(x) = P_{\text{target}}(x) + \alpha \cdot (P_{\text{expert}}(x) - P_{\text{base}}(x))$$

| 版本 | 核心改动 | 解决的难题 | 创新关键词 |
|------|---------|-----------|-----------|
| **C3** | 将 $P_{\text{steered}}$ 嵌入 SD 验收分子 | 如何与 SD 框架结合 | 概率域补贴 |
| **C1** | 移至比值域做加法 | 分母放大效应、大体量差鲁棒性 | 比值域加法 |
| **C4** | $\alpha$ 改为 $\mathbb{I}(S_t>\tau)\cdot S_t$（步级 Draft 信号） | 固定 $\alpha$ 全程注入 | 稀疏激活 |
| **C5** | $\alpha$ 改为 $H_t/H_{\max}$（Target 熵信号） | 不知道 Target 是否需要帮助 | 不确定性路由 |
| **C6** | C4 × C5 相乘组合 | 单信号误触发 | 双信号 AND 门控 |
| **C8** | 步级 $S_t$ → token 级 $\Delta P(x)$（C8） | 跨 token 结构性误触发 | token 级门控对齐 |
| **C9** | 门控与幅度解耦，$\Delta P$ 线性出现一次 | $(\Delta P)^2$ 平方副产品 | 二值门控 + 线性解耦 |

**三条贯穿始终的设计原则：**

1. **信号精准性原则**：领域信号 $\Delta P = P_{\text{expert}} - P_{\text{base}}$ 比单纯使用 $P_{\text{expert}}$ 更精准，因为 Base 消掉了通用成分。
2. **双向感知原则**：有效的注入需要同时感知"外部专家是否有优势"（$\Delta P$/$S_t$）和"内部 Target 是否有盲区"（$H_t$），单一信号均有系统性误触发风险。
3. **粒度对齐原则**：门控信号的粒度（步级 vs token 级）必须与验收决策的粒度（token 级）对齐，否则会引入结构性误触发；角色解耦（判断是否激活 vs 决定激活幅度）使系统可控性更强。

## 12. 实施约束（底层工程规范）

- 禁止 `pipeline` / `model.generate()`，必须手写 `model.forward()` auto-regressive loop
- 张量操作必须注释维度变化（如 `# shape: [1, vocab_size]`）
- 变量命名贴合论文定义（`delta_p`、`alpha`、`logit_draft`、`logit_base`）
- 远端脚本不得使用 `python -c "..."` 执行超过 15 行代码，必须写成独立 `.py` 文件
- Git 提交采用白名单策略，禁止 `git add .`
